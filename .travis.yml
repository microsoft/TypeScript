language: node_js

node_js:
  - '10'
  - '8'

sudo: false

env:
  - workerCount=3 timeout=600000

matrix:
  fast_finish: true

branches:
  only:
  - master
  - /^release-.*/

install:
  - npm uninstall typescript --no-save
  - npm uninstall tslint --no-save
  - npm ci

cache:
  directories:
    - node_modules

git:
  depth: 1
