/// <reference path="..\compiler\types.ts"/>

module WScript {
    export module Arguments {
        export var length = 0;
    }

    export module StdOut {
        export function Write() {
        }
    }

    export var FileName = "."
}