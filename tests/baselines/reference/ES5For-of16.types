=== tests/cases/conformance/statements/for-ofStatements/ES5For-of16.ts ===
for (let v of []) {
>v : undefined
>[] : undefined[]

    v;
>v : undefined

    for (let v of []) {
>v : undefined
>[] : undefined[]

        var x = v;
>x : undefined
>v : undefined

        v++;
>v++ : number
>v : undefined
    }
}
