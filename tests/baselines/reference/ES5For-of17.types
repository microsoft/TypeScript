=== tests/cases/conformance/statements/for-ofStatements/ES5For-of17.ts ===
for (let v of []) {
>v : undefined
>[] : undefined[]

    v;
>v : undefined

    for (let v of [v]) {
>v : any
>[v] : any[]
>v : any

        var x = v;
>x : any
>v : any

        v++;
>v++ : number
>v : any
    }
}
