=== tests/cases/conformance/statements/for-ofStatements/ES5For-of20.ts ===
for (let v of []) {
>v : undefined
>[] : undefined[]

    let v;
>v : any

    for (let v of [v]) {
>v : any
>[v] : any[]
>v : any

        const v;
>v : any
    }
}
