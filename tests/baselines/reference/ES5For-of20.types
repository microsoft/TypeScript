=== tests/cases/conformance/statements/for-ofStatements/ES5For-of20.ts ===
for (let v of []) {
>v : any
>[] : undefined[]

    let v;
>v : any

    for (let v of [v]) {
>v : any
>[v] : any (error)[]
>v : any (error)

        const v;
>v : any
    }
}
