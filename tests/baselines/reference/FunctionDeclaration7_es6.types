=== tests/cases/conformance/es6/functionDeclarations/FunctionDeclaration7_es6.ts ===
function*bar() {
>bar : () => Iterator<any>

  // 'yield' here is an identifier, and not a yield expression.
  function*foo(a = yield) {
>foo : (a?: any) => Iterator<any>
>a : any
>yield : any
  }
}
