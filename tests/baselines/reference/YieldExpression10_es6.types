=== tests/cases/conformance/es6/yieldExpressions/YieldExpression10_es6.ts ===
var v = { * foo() {
>v : { foo(): IterableIterator<any (error)>; }
>{ * foo() {    yield(foo);  }} : { foo(): IterableIterator<any (error)>; }
>foo : () => IterableIterator<any (error)>

    yield(foo);
>yield(foo) : any
>(foo) : any (error)
>foo : any (error)
  }
}

