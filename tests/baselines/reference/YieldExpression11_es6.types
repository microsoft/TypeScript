=== tests/cases/conformance/es6/yieldExpressions/YieldExpression11_es6.ts ===
class C {
>C : C

  *foo() {
>foo : () => IterableIterator<any (error)>

    yield(foo);
>yield(foo) : any
>(foo) : any (error)
>foo : any (error)
  }
}
