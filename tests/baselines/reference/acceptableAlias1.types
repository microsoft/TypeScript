=== tests/cases/compiler/acceptableAlias1.ts ===
module M {
>M : typeof M

    export module N {
    }
    export import X = N;
>X : any (error)
>N : any (error)
}

import r = M.X;
>r : any (error)
>M : typeof M
>X : any (error)

