=== tests/cases/compiler/aliasesInSystemModule2.ts ===
import {alias} from "foo";
>alias : any (error)

import cls = alias.Class;
>cls : any (error)
>alias : any (error)
>Class : any (error)

export import cls2 = alias.Class;
>cls2 : any (error)
>alias : any (error)
>Class : any (error)

let x = new alias.Class();
>x : any (error)
>new alias.Class() : any (error)
>alias.Class : any (error)
>alias : any (error)
>Class : any (error)

let y = new cls();
>y : any (error)
>new cls() : any (error)
>cls : any (error)

let z = new cls2();
>z : any (error)
>new cls2() : any (error)
>cls2 : any (error)

module M {
>M : typeof M

  export import cls = alias.Class;
>cls : any (error)
>alias : any (error)
>Class : any (error)

  let x = new alias.Class();
>x : any (error)
>new alias.Class() : any (error)
>alias.Class : any (error)
>alias : any (error)
>Class : any (error)

  let y = new cls(); 
>y : any (error)
>new cls() : any (error)
>cls : any (error)

  let z = new cls2();
>z : any (error)
>new cls2() : any (error)
>cls2 : any (error)
}
