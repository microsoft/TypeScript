=== tests/cases/compiler/b.d.ts ===
export function foo();
>foo : () => any

export function bar();
>bar : () => any

=== tests/cases/compiler/a.ts ===
import { default as Foo } from "./b";
>default : any (error)
>Foo : any (error)

Foo.bar();
>Foo.bar() : any (error)
>Foo.bar : any (error)
>Foo : any (error)
>bar : any (error)

Foo.foo();
>Foo.foo() : any (error)
>Foo.foo : any (error)
>Foo : any (error)
>foo : any (error)

