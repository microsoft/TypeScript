=== tests/cases/compiler/bases.ts ===
interface I {
    x;
>x : any
}

class B {
>B : B

    constructor() {
        this.y: any;
>this.y : any (error)
>this : this
>y : any (error)
>any : any (error)
    }
}

class C extends B implements I {
>C : C
>B : B

    constructor() {
        this.x: any;
>this.x : any (error)
>this : this
>x : any (error)
>any : any (error)
    }
}

new C().x;
>new C().x : any (error)
>new C() : C
>C : typeof C
>x : any (error)

new C().y;
>new C().y : any (error)
>new C() : C
>C : typeof C
>y : any (error)


