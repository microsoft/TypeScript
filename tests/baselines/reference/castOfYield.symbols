=== tests/cases/compiler/castOfYield.ts ===
function* f() {
>f : Symbol(f, Decl(castOfYield.ts, 0, 0))

    <number> (yield 0);
    // Unlike await, yield is not allowed to appear in a simple unary expression.
    <number> yield 0;
}

