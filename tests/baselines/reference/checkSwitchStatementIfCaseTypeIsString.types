=== tests/cases/compiler/checkSwitchStatementIfCaseTypeIsString.ts ===
declare function use(a: any): void;
>use : (a: any) => void
>a : any

class A {
>A : A

    doIt(x: Array<string>): void {
>doIt : (x: string[]) => void
>x : string[]
>Array : T[]

        x.forEach((v) => {
>x.forEach((v) => {            switch(v) {                case "test": use(this);            }        }) : void
>x.forEach : <U>(callbackfn: (this: U, value: string, index: number, array: string[]) => void, thisArg?: U) => void
>x : string[]
>forEach : <U>(callbackfn: (this: U, value: string, index: number, array: string[]) => void, thisArg?: U) => void
>(v) => {            switch(v) {                case "test": use(this);            }        } : (v: string) => void
>v : string

            switch(v) {
>v : string

                case "test": use(this);
>"test" : string
>use(this) : void
>use : (a: any) => void
>this : this
            }
        });
    }
}
