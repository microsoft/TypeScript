=== tests/cases/compiler/commentsOnObjectLiteral1.ts ===
var Person = makeClass( 
>Person : any (error)
>makeClass(    /**      @scope Person    */    {   } ) : any (error)
>makeClass : any (error)

   /** 
     @scope Person 
   */ 
   {
>{   } : {}
   } 
);
