=== tests/cases/conformance/es6/computedProperties/computedPropertyNamesContextualType2_ES6.ts ===
interface I {
    [s: string]: (x: any) => number; // Doesn't get hit
>s : string
>x : any

    [s: number]: (x: string) => number;
>s : number
>x : string
}

var o: I = {
>o : I
>{    [+"foo"](y) { return y.length; },    [+"bar"]: y => y.length} : { [x: number]: (y: any) => any; }

    [+"foo"](y) { return y.length; },
>[+"foo"] : (y: any) => any
>+"foo" : number
>"foo" : "foo"
>y : any
>y.length : any
>y : any
>length : any

    [+"bar"]: y => y.length
>[+"bar"] : (y: any) => any
>+"bar" : number
>"bar" : "bar"
>y => y.length : (y: any) => any
>y : any
>y.length : any
>y : any
>length : any
}
