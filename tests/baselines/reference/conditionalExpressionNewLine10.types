=== tests/cases/compiler/conditionalExpressionNewLine10.ts ===
var v = a 
>v : any
>a   ? b    ? d    : e  : c    ? f    : g : any
>a : any (error)

  ? b
>b    ? d    : e : any
>b : any (error)

    ? d
>d : any (error)

    : e
>e : any (error)

  : c
>c    ? f    : g : any
>c : any (error)

    ? f
>f : any (error)

    : g;
>g : any (error)

