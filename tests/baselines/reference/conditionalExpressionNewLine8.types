=== tests/cases/compiler/conditionalExpressionNewLine8.ts ===
var v = a 
>v : any
>a   ? b ? d : e  : c ? f : g : any
>a : any (error)

  ? b ? d : e
>b ? d : e : any
>b : any (error)
>d : any (error)
>e : any (error)

  : c ? f : g;
>c ? f : g : any
>c : any (error)
>f : any (error)
>g : any (error)

