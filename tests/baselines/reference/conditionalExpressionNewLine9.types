=== tests/cases/compiler/conditionalExpressionNewLine9.ts ===
var v = a 
>v : any
>a   ? b    ? d : e  : c    ? f : g : any
>a : any (error)

  ? b
>b    ? d : e : any
>b : any (error)

    ? d : e
>d : any (error)
>e : any (error)

  : c
>c    ? f : g : any
>c : any (error)

    ? f : g;
>f : any (error)
>g : any (error)

