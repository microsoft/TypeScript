=== tests/cases/compiler/constructorParametersInVariableDeclarations.ts ===
class A {
>A : A

    private a = x;
>a : any (error)
>x : any (error)

    private b = { p: x };
>b : { p: any (error); }
>{ p: x } : { p: any (error); }
>p : any (error)
>x : any (error)

    private c = () => x;
>c : () => any (error)
>() => x : () => any (error)
>x : any (error)

    constructor(x: number) {
>x : number
    }
}

class B {
>B : B

    private a = x;
>a : any (error)
>x : any (error)

    private b = { p: x };
>b : { p: any (error); }
>{ p: x } : { p: any (error); }
>p : any (error)
>x : any (error)

    private c = () => x;
>c : () => any (error)
>() => x : () => any (error)
>x : any (error)

    constructor() {
        var x = 1;
>x : number
>1 : 1
    }
}
