=== tests/cases/compiler/copyrightWithNewLine1.ts ===
/*****************************
* (c) Copyright - Important
****************************/

import model = require("./greeter")
>model : any (error)

var el = document.getElementById('content');
>el : HTMLElement
>document.getElementById('content') : HTMLElement
>document.getElementById : (elementId: string) => HTMLElement
>document : Document
>getElementById : (elementId: string) => HTMLElement
>'content' : "content"

var greeter = new model.Greeter(el);
>greeter : any (error)
>new model.Greeter(el) : any (error)
>model.Greeter : any (error)
>model : any (error)
>Greeter : any (error)
>el : HTMLElement

/** things */
greeter.start();
>greeter.start() : any (error)
>greeter.start : any (error)
>greeter : any (error)
>start : any (error)

