=== tests/cases/compiler/declarationEmitKeywordDestructuring.ts ===
type P = {
>P : { enum: boolean; function: boolean; abstract: boolean; one: boolean; }

    enum: boolean;
>enum : boolean

    function: boolean;
>function : boolean

    abstract: boolean;
>abstract : boolean

    one: boolean;
>one : boolean

};

function f1({ enum: _enum, ...rest }: P) {
>f1 : ({ enum: _enum, ...rest }: P) => { function: boolean; abstract: boolean; one: boolean; }
>enum : any
>_enum : boolean
>rest : { function: boolean; abstract: boolean; one: boolean; }

    return rest;
>rest : { function: boolean; abstract: boolean; one: boolean; }
}

function f2({ function: _function, ...rest }: P) {
>f2 : ({ function: _function, ...rest }: P) => { enum: boolean; abstract: boolean; one: boolean; }
>function : any
>_function : boolean
>rest : { enum: boolean; abstract: boolean; one: boolean; }

    return rest;
>rest : { enum: boolean; abstract: boolean; one: boolean; }
}

function f3({ abstract: _abstract, ...rest }: P) {
>f3 : ({ abstract: _abstract, ...rest }: P) => { enum: boolean; function: boolean; one: boolean; }
>abstract : any
>_abstract : boolean
>rest : { enum: boolean; function: boolean; one: boolean; }

    return rest;
>rest : { enum: boolean; function: boolean; one: boolean; }
}

