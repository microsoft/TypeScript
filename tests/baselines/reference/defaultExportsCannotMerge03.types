=== tests/cases/conformance/es6/modules/m1.ts ===
export default class Decl {
>Decl : Decl
}

interface Decl {
    p1: number;
>p1 : number

    p2: number;
>p2 : number
}

namespace Decl {
    interface I {
    }
}

=== tests/cases/conformance/es6/modules/m2.ts ===
import Entity from "m1"
>Entity : any (error)

Entity();
>Entity() : any (error)
>Entity : any (error)

var x: Entity;
>x : any (error)

var y: Entity.I;
>y : any (error)
>Entity : any (error)

var z = new Entity();
>z : any (error)
>new Entity() : any (error)
>Entity : any (error)

var sum = z.p1 + z.p2
>sum : any (error)
>z.p1 + z.p2 : any (error)
>z.p1 : any (error)
>z : any (error)
>p1 : any (error)
>z.p2 : any (error)
>z : any (error)
>p2 : any (error)

