=== tests/cases/compiler/emptyMemberAccess.ts ===
function getObj() {
>getObj : () => void

   ().toString();
>().toString() : any (error)
>().toString : any (error)
>() : any (error)
> : any (error)
>toString : any (error)

}
 
