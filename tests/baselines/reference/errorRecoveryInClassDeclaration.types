=== tests/cases/compiler/errorRecoveryInClassDeclaration.ts ===
class C {
>C : C

    public bar() {
>bar : () => void

        var v = foo(
>v : any (error)
>foo(            public blaz() {}            ) : any (error)
>foo : any (error)

            public blaz() {}
>public : any (error)
>blaz() : any (error)
>blaz : any (error)
>{} : {}

            );
    }
}
