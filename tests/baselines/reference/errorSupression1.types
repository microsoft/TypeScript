=== tests/cases/compiler/errorSupression1.ts ===
class Foo { static bar() { return "x"; } }
>Foo : Foo
>bar : () => string
>"x" : "x"

var baz = Foo.b;
>baz : any (error)
>Foo.b : any (error)
>Foo : typeof Foo
>b : any (error)

  // Foo.b won't bind. 
baz.concat("y");
>baz.concat("y") : any (error)
>baz.concat : any (error)
>baz : any (error)
>concat : any (error)
>"y" : "y"

  // So we don't want an error on 'concat'.
