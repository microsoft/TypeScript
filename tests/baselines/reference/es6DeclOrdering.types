=== tests/cases/compiler/es6DeclOrdering.ts ===
class Bar {
>Bar : Bar

   //public bar() { }

   public foo() {
>foo : () => any (error)

       return this._store.length;
>this._store.length : any (error)
>this._store : any (error)
>this : this
>_store : any (error)
>length : any (error)
 
   }

   constructor(store: string) {
>store : string

       this._store = store; // this is an error for some reason? Unresolved symbol store
>this._store = store : string
>this._store : any (error)
>this : this
>_store : any (error)
>store : string
 
   } 
}


