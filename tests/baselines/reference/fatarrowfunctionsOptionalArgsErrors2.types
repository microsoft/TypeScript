=== tests/cases/compiler/fatarrowfunctionsOptionalArgsErrors2.ts ===
var tt1 = (a, (b, c)) => a+b+c;
>tt1 : any (error)
>(a, (b, c)) : any (error)
>a, (b, c) : any (error)
>a : any (error)
>(b, c) : any (error)
>b, c : any (error)
>b : any (error)
>c : any (error)
>a+b+c : any (error)
>a+b : any (error)
>a : any (error)
>b : any (error)
>c : any (error)

var tt2 = ((a), b, c) => a+b+c;
>tt2 : any (error)
>((a), b, c) : any (error)
>(a), b, c : any (error)
>(a), b : any (error)
>(a) : any (error)
>a : any (error)
>b : any (error)
>c : any (error)
>a+b+c : any (error)
>a+b : any (error)
>a : any (error)
>b : any (error)
>c : any (error)

var tt3 = ((a)) => a;
>tt3 : any (error)
>((a)) : any (error)
>(a) : any (error)
>a : any (error)
>a : any (error)

