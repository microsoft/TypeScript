=== tests/cases/compiler/functionTypesLackingReturnTypes.ts ===
// Error (no '=>')
function f(x: ()) {
>f : (x: () => ) => void
>x : () => 
}

// Error (no '=>')
var g: (param);
>g : any

// Okay
var h: { () }
>h : () => any

// Okay
var i: { new () }
>i : new () => any

