=== tests/cases/compiler/functionTypesLackingReturnTypes.ts ===
// Error (no '=>')
function f(x: ()) {
>f : (x: () => any (error)) => void
>x : () => any (error)
}

// Error (no '=>')
var g: (param);
>g : any (error)

// Okay
var h: { () }
>h : () => any

// Okay
var i: { new () }
>i : new () => any

