=== tests/cases/compiler/functionWithNoBestCommonType1.ts ===

function foo() {
>foo : () => boolean | void

    return true;
>true : boolean

    return bar();
>bar() : void
>bar : () => void
}

function bar(): void {
>bar : () => void
}
