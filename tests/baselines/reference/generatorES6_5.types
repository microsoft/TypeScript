=== tests/cases/compiler/generatorES6_5.ts ===
function* foo() {
>foo : () => IterableIterator<any>

    yield a ? b : c;
>yield a ? b : c : any
>a ? b : c : any
>a : any (error)
>b : any (error)
>c : any (error)
}
