=== tests/cases/conformance/es6/yieldExpressions/generatorTypeCheck43.ts ===
function* g() {
>g : () => Iterator<0>

    let x = {
>x : { [x: number]: () => Iterator<any>; }
>{        *[yield 0]() {        }    } : { [x: number]: () => Iterator<any>; }

        *[yield 0]() {
>yield 0 : any
>0 : 0

        }
    }
}
