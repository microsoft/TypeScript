=== tests/cases/compiler/genericClassWithStaticsUsingTypeArguments.ts ===
// Should be error to use 'T' in all declarations within Foo.
class Foo<T> {
>Foo : Foo<T>

    static a = (n: T) => { };
>a : (n: any (error)) => void
>(n: T) => { } : (n: any (error)) => void
>n : any (error)

    static b: T;
>b : any (error)

    static c: T[] = [];
>c : any (error)[]
>[] : undefined[]

    static d = false || ((x: T) => x || undefined)(null)
>d : any
>false || ((x: T) => x || undefined)(null) : any
>false : false
>((x: T) => x || undefined)(null) : any
>((x: T) => x || undefined) : (x: any (error)) => any
>(x: T) => x || undefined : (x: any (error)) => any
>x : any (error)
>x || undefined : any
>x : any (error)
>undefined : undefined
>null : null

    static e = function (x: T) { return null; }
>e : (x: any (error)) => any
>function (x: T) { return null; } : (x: any (error)) => any
>x : any (error)
>null : null

    static f(xs: T[]): T[] {
>f : (xs: any (error)[]) => any (error)[]
>xs : any (error)[]

        return xs.reverse();
>xs.reverse() : any (error)[]
>xs.reverse : () => any (error)[]
>xs : any (error)[]
>reverse : () => any (error)[]
    }
}

