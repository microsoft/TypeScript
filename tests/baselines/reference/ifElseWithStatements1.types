=== tests/cases/compiler/ifElseWithStatements1.ts ===
if (true)
>true : true

    f();
>f() : any (error)
>f : any (error)

else
    f();
>f() : any (error)
>f : any (error)

function foo(): boolean {
>foo : () => boolean

    if (true)
>true : true

        return true;
>true : true

    else
        return false;
>false : false
}

