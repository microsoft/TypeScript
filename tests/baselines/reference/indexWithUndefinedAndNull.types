=== tests/cases/compiler/indexWithUndefinedAndNull.ts ===
interface N {
    [n: number]: string;
>n : number
}
interface S {
    [s: string]: number;
>s : string
}
let n: N;
>n : N

let s: S;
>s : S

let str: string = n[undefined];
>str : string
>n[undefined] : any (error)
>n : N
>undefined : undefined

str = n[null];
>str = n[null] : any (error)
>str : string
>n[null] : any (error)
>n : N
>null : null

let num: number = s[undefined];
>num : number
>s[undefined] : any (error)
>s : S
>undefined : undefined

num = s[null];
>num = s[null] : any (error)
>num : number
>s[null] : any (error)
>s : S
>null : null

