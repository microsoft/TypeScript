=== tests/cases/compiler/interfaceNameAsIdentifier.ts ===
interface C {
    (): void;
}
C();
>C() : any (error)
>C : any (error)

module m2 {
    export interface C {
        (): void;
    }
}

m2.C();
>m2.C() : any (error)
>m2.C : any (error)
>m2 : any (error)
>C : any (error)

