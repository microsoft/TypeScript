=== tests/cases/compiler/jsxAttributeWithoutExpressionReact.tsx ===
declare var React: any;
>React : any

<View>
><View>    <ListView refreshControl={        <RefreshControl onRefresh={} refreshing={} />    } dataSource={this.state.ds} renderRow={}>    </ListView></View> : any (error)
>View : any (error)

    <ListView refreshControl={
><ListView refreshControl={        <RefreshControl onRefresh={} refreshing={} />    } dataSource={this.state.ds} renderRow={}>    </ListView> : any (error)
>ListView : any (error)
>refreshControl : any (error)

        <RefreshControl onRefresh={} refreshing={} />
><RefreshControl onRefresh={} refreshing={} /> : any (error)
>RefreshControl : any (error)
>onRefresh : any (error)
>refreshing : any (error)

    } dataSource={this.state.ds} renderRow={}>
>dataSource : any
>this.state.ds : any
>this.state : any
>this : any
>state : any
>ds : any
>renderRow : any (error)

    </ListView>
>ListView : any (error)

</View>
>View : any (error)

