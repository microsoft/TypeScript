=== tests/cases/compiler/missingTypeArguments2.ts ===
class A<T> { }
>A : A<T>

var x: () => A;
>x : () => any (error)

(a: A) => { };
>(a: A) => { } : (a: any (error)) => void
>a : any (error)

var y: A<A>;
>y : A<any (error)>

(): A => null;
>(): A => null : () => any (error)
>null : null

