=== tests/cases/compiler/moduleAssignmentCompat2.ts ===
module A {
>A : typeof A

    export class C { }
>C : C
}
module B {
>B : typeof B

    export class C { }
>C : C

    export class D { }
>D : D
}

var a: A;
>a : any (error)

var b: B;
>b : any (error)

a = b;
>a = b : any (error)
>a : any (error)
>b : any (error)

b = a; // error
>b = a : any (error)
>b : any (error)
>a : any (error)

