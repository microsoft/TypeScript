=== tests/cases/compiler/noImplicitAnyForwardReferencedInterface.ts ===
declare var x: Entry;
>x : Entry
>Entry : Entry

interface Entry {
>Entry : Entry

    // Should return error for implicit any.
    new ();
}
