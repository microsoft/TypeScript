=== tests/cases/compiler/noUnusedLocals_destructuringAssignment.ts ===
class C {
>C : C

    private x = 0;
>x : number
>0 : 0

    m(): number {
>m : () => number

        let x: number;
>x : number

        ({ x } = this);
>({ x } = this) : C
>{ x } = this : C
>{ x } : { x: number; }
>x : number
>this : C

        return x;
>x : number
    }

    private f(): Function {
>f : () => Function

        let f: Function;
>f : Function

        ({ f } = this);
>({ f } = this) : C
>{ f } = this : C
>{ f } : { f: Function; }
>f : Function
>this : C

        return f;
>f : Function
    }
}

