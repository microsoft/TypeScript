=== tests/cases/conformance/types/nonPrimitive/nonPrimitiveAccessProperty.ts ===
var a: object;
>a : object

a.toString();
>a.toString() : string
>a.toString : () => string
>a : object
>toString : () => string

a.nonExist(); // error
>a.nonExist() : any (error)
>a.nonExist : any (error)
>a : object
>nonExist : any (error)

var { destructuring } = a; // error
>destructuring : any (error)
>a : object

var { ...rest } = a; // ok
>rest : {}
>a : object

