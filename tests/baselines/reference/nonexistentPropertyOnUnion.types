=== tests/cases/compiler/nonexistentPropertyOnUnion.ts ===
function f(x: string | Promise<string>) {
>f : (x: string | Promise<string>) => void
>x : string | Promise<string>

    x.toLowerCase();
>x.toLowerCase() : any (error)
>x.toLowerCase : any (error)
>x : string | Promise<string>
>toLowerCase : any (error)
}

