=== tests/cases/conformance/es6/shorthandPropertyAssignment/objectLiteralShorthandPropertiesErrorFromNotUsingIdentifier.ts ===
// errors
var y = {
>y : { "stringLiteral": any (error); 42: any (error); readonly e: void; f: any; this: any (error); super: any (error); var: any (error); class: any (error); typeof: any (error); }
>{    "stringLiteral",    42,    get e,    set f,    this,    super,    var,    class,    typeof} : { "stringLiteral": any (error); 42: any (error); readonly e: void; f: any; this: any (error); super: any (error); var: any (error); class: any (error); typeof: any (error); }

    "stringLiteral",
>"stringLiteral" : any (error)
> : any (error)

    42,
>42 : any (error)
> : any (error)

    get e,
>e : void

    set f,
>f : any

    this,
>this : any (error)
> : any (error)

    super,
>super : any (error)
> : any (error)

    var,
>var : any (error)
> : any (error)

    class,
>class : any (error)
> : any (error)

    typeof
>typeof : any (error)

};
> : any (error)

var x = {
>x : { a: number[]; }
>{    a.b,    a["ss"],    a[1],} : { a: number[]; }

    a.b,
>a : any (error)
>.b : any (error)
> : any (error)
>b : any (error)

    a["ss"],
>a : any (error)
>["ss"] : string[]
>"ss" : "ss"

    a[1],
>a : any (error)
>[1] : number[]
>1 : 1

};

var v = { class };  // error
>v : { class: any (error); }
>{ class } : { class: any (error); }
>class : any (error)
> : any (error)

