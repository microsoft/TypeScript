=== tests/cases/compiler/objectLiteralWithSemicolons2.ts ===
var v = {
>v : { a: any (error); b: any (error); c: any (error); }
>{  a;  b;  c} : { a: any (error); b: any (error); c: any (error); }

  a;
>a : any (error)
> : any (error)

  b;
>b : any (error)
> : any (error)

  c
>c : any (error)
}
