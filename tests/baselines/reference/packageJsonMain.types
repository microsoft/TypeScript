=== /a.ts ===
import foo = require("foo");
>foo : any (error)

import bar = require("bar");
>bar : any (error)

import baz = require("baz");
>baz : any (error)

foo + bar + baz;
>foo + bar + baz : any (error)
>foo + bar : any (error)
>foo : any (error)
>bar : any (error)
>baz : any (error)

