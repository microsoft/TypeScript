=== parserArrowFunctionExpression7.ts ===
({
>({	async m() {		for (;;) {		}	}}) : { m(): Promise<never>; }
>{	async m() {		for (;;) {		}	}} : { m(): Promise<never>; }

	async m() {
>m : () => Promise<never>

		for (;;) {
		}
	}
});

