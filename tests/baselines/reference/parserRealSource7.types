=== tests/cases/conformance/parser/ecmascript5/parserRealSource7.ts ===
// Copyright (c) Microsoft. All rights reserved. Licensed under the Apache License, Version 2.0. 
// See LICENSE.txt in the project root for complete license information.

///<reference path='typescript.ts' />

module TypeScript {
>TypeScript : typeof TypeScript

    export class Continuation {
>Continuation : Continuation

        public exceptionBlock = -1;
>exceptionBlock : number
>-1 : -1
>1 : 1

        constructor (public normalBlock: number) { }
>normalBlock : number
    }

    function getBaseTypeLinks(bases: ASTList, baseTypeLinks: TypeLink[]) {
>getBaseTypeLinks : (bases: any (error), baseTypeLinks: any (error)[]) => any (error)[]
>bases : any (error)
>baseTypeLinks : any (error)[]

        if (bases) {
>bases : any (error)

            var len = bases.members.length;
>len : any (error)
>bases.members.length : any (error)
>bases.members : any (error)
>bases : any (error)
>members : any (error)
>length : any (error)

            if (baseTypeLinks == null) {
>baseTypeLinks == null : boolean
>baseTypeLinks : any (error)[]
>null : null

                baseTypeLinks = new TypeLink[];
>baseTypeLinks = new TypeLink[] : any (error)
>baseTypeLinks : any (error)[]
>new TypeLink[] : any (error)
>TypeLink[] : any (error)
>TypeLink : any (error)
> : any (error)
            }
            for (var i = 0; i < len; i++) {
>i : number
>0 : 0
>i < len : boolean
>i : number
>len : any (error)
>i++ : number
>i : number

                var baseExpr = bases.members[i];
>baseExpr : any (error)
>bases.members[i] : any (error)
>bases.members : any (error)
>bases : any (error)
>members : any (error)
>i : number

                var name = baseExpr;
>name : any (error)
>baseExpr : any (error)

                var typeLink = new TypeLink();
>typeLink : any (error)
>new TypeLink() : any (error)
>TypeLink : any (error)

                typeLink.ast = name;
>typeLink.ast = name : any (error)
>typeLink.ast : any (error)
>typeLink : any (error)
>ast : any (error)
>name : any (error)

                baseTypeLinks[baseTypeLinks.length] = typeLink;
>baseTypeLinks[baseTypeLinks.length] = typeLink : any (error)
>baseTypeLinks[baseTypeLinks.length] : any (error)
>baseTypeLinks : any (error)[]
>baseTypeLinks.length : number
>baseTypeLinks : any (error)[]
>length : number
>typeLink : any (error)
            }
        }
        return baseTypeLinks;
>baseTypeLinks : any (error)[]
    }

    function getBases(type: Type, typeDecl: TypeDeclaration) {
>getBases : (type: any (error), typeDecl: any (error)) => void
>type : any (error)
>typeDecl : any (error)

        type.extendsTypeLinks = getBaseTypeLinks(typeDecl.extendsList, type.extendsTypeLinks);
>type.extendsTypeLinks = getBaseTypeLinks(typeDecl.extendsList, type.extendsTypeLinks) : any (error)[]
>type.extendsTypeLinks : any (error)
>type : any (error)
>extendsTypeLinks : any (error)
>getBaseTypeLinks(typeDecl.extendsList, type.extendsTypeLinks) : any (error)[]
>getBaseTypeLinks : (bases: any (error), baseTypeLinks: any (error)[]) => any (error)[]
>typeDecl.extendsList : any (error)
>typeDecl : any (error)
>extendsList : any (error)
>type.extendsTypeLinks : any (error)
>type : any (error)
>extendsTypeLinks : any (error)

        type.implementsTypeLinks = getBaseTypeLinks(typeDecl.implementsList, type.implementsTypeLinks);
>type.implementsTypeLinks = getBaseTypeLinks(typeDecl.implementsList, type.implementsTypeLinks) : any (error)[]
>type.implementsTypeLinks : any (error)
>type : any (error)
>implementsTypeLinks : any (error)
>getBaseTypeLinks(typeDecl.implementsList, type.implementsTypeLinks) : any (error)[]
>getBaseTypeLinks : (bases: any (error), baseTypeLinks: any (error)[]) => any (error)[]
>typeDecl.implementsList : any (error)
>typeDecl : any (error)
>implementsList : any (error)
>type.implementsTypeLinks : any (error)
>type : any (error)
>implementsTypeLinks : any (error)
    }

    function addPrototypeField(classType: Type, ast: AST, context: TypeCollectionContext) {
>addPrototypeField : (classType: any (error), ast: any (error), context: any (error)) => void
>classType : any (error)
>ast : any (error)
>context : any (error)

        var field = new ValueLocation();
>field : any (error)
>new ValueLocation() : any (error)
>ValueLocation : any (error)

        field.typeLink = new TypeLink();
>field.typeLink = new TypeLink() : any (error)
>field.typeLink : any (error)
>field : any (error)
>typeLink : any (error)
>new TypeLink() : any (error)
>TypeLink : any (error)

        field.typeLink.ast = ast;
>field.typeLink.ast = ast : any (error)
>field.typeLink.ast : any (error)
>field.typeLink : any (error)
>field : any (error)
>typeLink : any (error)
>ast : any (error)
>ast : any (error)

        field.typeLink.type = classType.instanceType;
>field.typeLink.type = classType.instanceType : any (error)
>field.typeLink.type : any (error)
>field.typeLink : any (error)
>field : any (error)
>typeLink : any (error)
>type : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)

        var fieldSymbol =
>fieldSymbol : any (error)

            new FieldSymbol("prototype", ast.minChar,
>new FieldSymbol("prototype", ast.minChar,                            context.checker.locationInfo.unitIndex, true, field) : any (error)
>FieldSymbol : any (error)
>"prototype" : "prototype"
>ast.minChar : any (error)
>ast : any (error)
>minChar : any (error)

                            context.checker.locationInfo.unitIndex, true, field);
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)
>true : true
>field : any (error)

        fieldSymbol.flags |= (SymbolFlags.Property | SymbolFlags.BuiltIn);
>fieldSymbol.flags |= (SymbolFlags.Property | SymbolFlags.BuiltIn) : number
>fieldSymbol.flags : any (error)
>fieldSymbol : any (error)
>flags : any (error)
>(SymbolFlags.Property | SymbolFlags.BuiltIn) : number
>SymbolFlags.Property | SymbolFlags.BuiltIn : number
>SymbolFlags.Property : any (error)
>SymbolFlags : any (error)
>Property : any (error)
>SymbolFlags.BuiltIn : any (error)
>SymbolFlags : any (error)
>BuiltIn : any (error)

        field.symbol = fieldSymbol;
>field.symbol = fieldSymbol : any (error)
>field.symbol : any (error)
>field : any (error)
>symbol : any (error)
>fieldSymbol : any (error)

        fieldSymbol.declAST = ast;
>fieldSymbol.declAST = ast : any (error)
>fieldSymbol.declAST : any (error)
>fieldSymbol : any (error)
>declAST : any (error)
>ast : any (error)

        classType.members.addPublicMember("prototype", fieldSymbol);
>classType.members.addPublicMember("prototype", fieldSymbol) : any (error)
>classType.members.addPublicMember : any (error)
>classType.members : any (error)
>classType : any (error)
>members : any (error)
>addPublicMember : any (error)
>"prototype" : "prototype"
>fieldSymbol : any (error)
    }

    export function createNewConstructGroupForType(type: Type) {
>createNewConstructGroupForType : (type: any (error)) => void
>type : any (error)

        var signature = new Signature();
>signature : any (error)
>new Signature() : any (error)
>Signature : any (error)

        signature.returnType = new TypeLink();
>signature.returnType = new TypeLink() : any (error)
>signature.returnType : any (error)
>signature : any (error)
>returnType : any (error)
>new TypeLink() : any (error)
>TypeLink : any (error)

        signature.returnType.type = type.instanceType;
>signature.returnType.type = type.instanceType : any (error)
>signature.returnType.type : any (error)
>signature.returnType : any (error)
>signature : any (error)
>returnType : any (error)
>type : any (error)
>type.instanceType : any (error)
>type : any (error)
>instanceType : any (error)

        signature.parameters = [];
>signature.parameters = [] : undefined[]
>signature.parameters : any (error)
>signature : any (error)
>parameters : any (error)
>[] : undefined[]

        type.construct = new SignatureGroup();
>type.construct = new SignatureGroup() : any (error)
>type.construct : any (error)
>type : any (error)
>construct : any (error)
>new SignatureGroup() : any (error)
>SignatureGroup : any (error)

        type.construct.addSignature(signature);     
>type.construct.addSignature(signature) : any (error)
>type.construct.addSignature : any (error)
>type.construct : any (error)
>type : any (error)
>construct : any (error)
>addSignature : any (error)
>signature : any (error)
    }

    export function cloneParentConstructGroupForChildType(child: Type, parent: Type) {
>cloneParentConstructGroupForChildType : (child: any (error), parent: any (error)) => void
>child : any (error)
>parent : any (error)

        child.construct = new SignatureGroup();
>child.construct = new SignatureGroup() : any (error)
>child.construct : any (error)
>child : any (error)
>construct : any (error)
>new SignatureGroup() : any (error)
>SignatureGroup : any (error)

        var sig: Signature = null;
>sig : any (error)
>null : null

        if (!parent.construct) {
>!parent.construct : boolean
>parent.construct : any (error)
>parent : any (error)
>construct : any (error)

            createNewConstructGroupForType(parent);
>createNewConstructGroupForType(parent) : void
>createNewConstructGroupForType : (type: any (error)) => void
>parent : any (error)
        }

        for (var i = 0; i < parent.construct.signatures.length; i++) { 
>i : number
>0 : 0
>i < parent.construct.signatures.length : boolean
>i : number
>parent.construct.signatures.length : any (error)
>parent.construct.signatures : any (error)
>parent.construct : any (error)
>parent : any (error)
>construct : any (error)
>signatures : any (error)
>length : any (error)
>i++ : number
>i : number

            sig = new Signature();
>sig = new Signature() : any (error)
>sig : any (error)
>new Signature() : any (error)
>Signature : any (error)

            sig.parameters = parent.construct.signatures[i].parameters;
>sig.parameters = parent.construct.signatures[i].parameters : any (error)
>sig.parameters : any (error)
>sig : any (error)
>parameters : any (error)
>parent.construct.signatures[i].parameters : any (error)
>parent.construct.signatures[i] : any (error)
>parent.construct.signatures : any (error)
>parent.construct : any (error)
>parent : any (error)
>construct : any (error)
>signatures : any (error)
>i : number
>parameters : any (error)

            sig.nonOptionalParameterCount = parent.construct.signatures[i].nonOptionalParameterCount;
>sig.nonOptionalParameterCount = parent.construct.signatures[i].nonOptionalParameterCount : any (error)
>sig.nonOptionalParameterCount : any (error)
>sig : any (error)
>nonOptionalParameterCount : any (error)
>parent.construct.signatures[i].nonOptionalParameterCount : any (error)
>parent.construct.signatures[i] : any (error)
>parent.construct.signatures : any (error)
>parent.construct : any (error)
>parent : any (error)
>construct : any (error)
>signatures : any (error)
>i : number
>nonOptionalParameterCount : any (error)

            sig.typeCheckStatus = parent.construct.signatures[i].typeCheckStatus;
>sig.typeCheckStatus = parent.construct.signatures[i].typeCheckStatus : any (error)
>sig.typeCheckStatus : any (error)
>sig : any (error)
>typeCheckStatus : any (error)
>parent.construct.signatures[i].typeCheckStatus : any (error)
>parent.construct.signatures[i] : any (error)
>parent.construct.signatures : any (error)
>parent.construct : any (error)
>parent : any (error)
>construct : any (error)
>signatures : any (error)
>i : number
>typeCheckStatus : any (error)

            sig.declAST = parent.construct.signatures[i].declAST;
>sig.declAST = parent.construct.signatures[i].declAST : any (error)
>sig.declAST : any (error)
>sig : any (error)
>declAST : any (error)
>parent.construct.signatures[i].declAST : any (error)
>parent.construct.signatures[i] : any (error)
>parent.construct.signatures : any (error)
>parent.construct : any (error)
>parent : any (error)
>construct : any (error)
>signatures : any (error)
>i : number
>declAST : any (error)

            sig.returnType = new TypeLink();
>sig.returnType = new TypeLink() : any (error)
>sig.returnType : any (error)
>sig : any (error)
>returnType : any (error)
>new TypeLink() : any (error)
>TypeLink : any (error)

            sig.returnType.type = child.instanceType;
>sig.returnType.type = child.instanceType : any (error)
>sig.returnType.type : any (error)
>sig.returnType : any (error)
>sig : any (error)
>returnType : any (error)
>type : any (error)
>child.instanceType : any (error)
>child : any (error)
>instanceType : any (error)

            child.construct.addSignature(sig);
>child.construct.addSignature(sig) : any (error)
>child.construct.addSignature : any (error)
>child.construct : any (error)
>child : any (error)
>construct : any (error)
>addSignature : any (error)
>sig : any (error)
        }

    }

    export var globalId = "__GLO";
>globalId : string
>"__GLO" : "__GLO"

    export interface IAliasScopeContext {
        topLevelScope: ScopeChain;
>topLevelScope : any (error)

        members: IHashTable;
>members : any (error)

        tcContext: TypeCollectionContext;
>tcContext : any (error)
    }

    function findTypeSymbolInScopeChain(name: string, scopeChain: ScopeChain): Symbol {
>findTypeSymbolInScopeChain : (name: string, scopeChain: any (error)) => Symbol
>name : string
>scopeChain : any (error)

        var symbol = scopeChain.scope.find(name, false, true);
>symbol : any (error)
>scopeChain.scope.find(name, false, true) : any (error)
>scopeChain.scope.find : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>find : any (error)
>name : string
>false : false
>true : true

        if (symbol == null && scopeChain.previous) {
>symbol == null && scopeChain.previous : any
>symbol == null : boolean
>symbol : any (error)
>null : null
>scopeChain.previous : any (error)
>scopeChain : any (error)
>previous : any (error)

            symbol = findTypeSymbolInScopeChain(name, scopeChain.previous);
>symbol = findTypeSymbolInScopeChain(name, scopeChain.previous) : Symbol
>symbol : any (error)
>findTypeSymbolInScopeChain(name, scopeChain.previous) : Symbol
>findTypeSymbolInScopeChain : (name: string, scopeChain: any (error)) => Symbol
>name : string
>scopeChain.previous : any (error)
>scopeChain : any (error)
>previous : any (error)
        }

        return symbol;
>symbol : any (error)
    }

    function findSymbolFromAlias(alias: AST, context: IAliasScopeContext): Symbol {
>findSymbolFromAlias : (alias: any (error), context: IAliasScopeContext) => Symbol
>alias : any (error)
>context : IAliasScopeContext

        var symbol: Symbol = null;
>symbol : Symbol
>null : null

        switch (alias.nodeType) {
>alias.nodeType : any (error)
>alias : any (error)
>nodeType : any (error)

            case NodeType.Name:
>NodeType.Name : any (error)
>NodeType : any (error)
>Name : any (error)

                var name = (<Identifier>alias).text;
>name : any (error)
>(<Identifier>alias).text : any (error)
>(<Identifier>alias) : any (error)
><Identifier>alias : any (error)
>alias : any (error)
>text : any (error)

                var isDynamic = isQuoted(name);
>isDynamic : any (error)
>isQuoted(name) : any (error)
>isQuoted : any (error)
>name : any (error)

                var findSym = (id: string) => {
>findSym : (id: string) => any
>(id: string) => {                    if (context.members) {                        return context.members.lookup(name);                    }                    else {                        return findTypeSymbolInScopeChain(name, context.topLevelScope);                    }                } : (id: string) => any
>id : string

                    if (context.members) {
>context.members : any (error)
>context : IAliasScopeContext
>members : any (error)

                        return context.members.lookup(name);
>context.members.lookup(name) : any (error)
>context.members.lookup : any (error)
>context.members : any (error)
>context : IAliasScopeContext
>members : any (error)
>lookup : any (error)
>name : any (error)
                    }
                    else {
                        return findTypeSymbolInScopeChain(name, context.topLevelScope);
>findTypeSymbolInScopeChain(name, context.topLevelScope) : Symbol
>findTypeSymbolInScopeChain : (name: string, scopeChain: any (error)) => Symbol
>name : any (error)
>context.topLevelScope : any (error)
>context : IAliasScopeContext
>topLevelScope : any (error)
                    }
                }

                if (isDynamic) {
>isDynamic : any (error)

                    symbol = context.tcContext.checker.findSymbolForDynamicModule(name, context.tcContext.script.locationInfo.filename, findSym);
>symbol = context.tcContext.checker.findSymbolForDynamicModule(name, context.tcContext.script.locationInfo.filename, findSym) : any (error)
>symbol : Symbol
>context.tcContext.checker.findSymbolForDynamicModule(name, context.tcContext.script.locationInfo.filename, findSym) : any (error)
>context.tcContext.checker.findSymbolForDynamicModule : any (error)
>context.tcContext.checker : any (error)
>context.tcContext : any (error)
>context : IAliasScopeContext
>tcContext : any (error)
>checker : any (error)
>findSymbolForDynamicModule : any (error)
>name : any (error)
>context.tcContext.script.locationInfo.filename : any (error)
>context.tcContext.script.locationInfo : any (error)
>context.tcContext.script : any (error)
>context.tcContext : any (error)
>context : IAliasScopeContext
>tcContext : any (error)
>script : any (error)
>locationInfo : any (error)
>filename : any (error)
>findSym : (id: string) => any
                }
                else {
                    symbol = findSym(name);
>symbol = findSym(name) : any
>symbol : Symbol
>findSym(name) : any
>findSym : (id: string) => any
>name : any (error)
                }

                break;

            case NodeType.Dot:
>NodeType.Dot : any (error)
>NodeType : any (error)
>Dot : any (error)

                var dottedExpr = <BinaryExpression>alias;
>dottedExpr : any (error)
><BinaryExpression>alias : any (error)
>alias : any (error)

                var op1Sym = findSymbolFromAlias(dottedExpr.operand1, context);
>op1Sym : Symbol
>findSymbolFromAlias(dottedExpr.operand1, context) : Symbol
>findSymbolFromAlias : (alias: any (error), context: IAliasScopeContext) => Symbol
>dottedExpr.operand1 : any (error)
>dottedExpr : any (error)
>operand1 : any (error)
>context : IAliasScopeContext

                if (op1Sym && op1Sym.getType()) {
>op1Sym && op1Sym.getType() : any
>op1Sym : Symbol
>op1Sym.getType() : any (error)
>op1Sym.getType : any (error)
>op1Sym : Symbol
>getType : any (error)

                    symbol = findSymbolFromAlias(dottedExpr.operand2, context);
>symbol = findSymbolFromAlias(dottedExpr.operand2, context) : Symbol
>symbol : Symbol
>findSymbolFromAlias(dottedExpr.operand2, context) : Symbol
>findSymbolFromAlias : (alias: any (error), context: IAliasScopeContext) => Symbol
>dottedExpr.operand2 : any (error)
>dottedExpr : any (error)
>operand2 : any (error)
>context : IAliasScopeContext
                }

                break;

            default:
                break;
        }

        if (symbol) {
>symbol : Symbol

            var symType = symbol.getType();
>symType : any (error)
>symbol.getType() : any (error)
>symbol.getType : any (error)
>symbol : Symbol
>getType : any (error)

            if (symType) {
>symType : any (error)

                var members = symType.members;
>members : any (error)
>symType.members : any (error)
>symType : any (error)
>members : any (error)

                if (members) {
>members : any (error)

                    context.members = members.publicMembers;
>context.members = members.publicMembers : any (error)
>context.members : any (error)
>context : IAliasScopeContext
>members : any (error)
>members.publicMembers : any (error)
>members : any (error)
>publicMembers : any (error)
                }
            }
        }

        return symbol;
>symbol : Symbol
    }

    export function preCollectImportTypes(ast: AST, parent: AST, context: TypeCollectionContext) {
>preCollectImportTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        var typeSymbol: TypeSymbol = null;
>typeSymbol : any (error)
>null : null

        var modType: ModuleType = null;
>modType : any (error)
>null : null

        var importDecl = <ImportDeclaration>ast;
>importDecl : any (error)
><ImportDeclaration>ast : any (error)
>ast : any (error)

        var isExported = hasFlag(importDecl.varFlags, VarFlags.Exported);
>isExported : any (error)
>hasFlag(importDecl.varFlags, VarFlags.Exported) : any (error)
>hasFlag : any (error)
>importDecl.varFlags : any (error)
>importDecl : any (error)
>varFlags : any (error)
>VarFlags.Exported : any (error)
>VarFlags : any (error)
>Exported : any (error)

        // REVIEW: technically, this call isn't strictly necessary, since we'll find the type during the call to resolveTypeMembers
        var aliasedModSymbol = findSymbolFromAlias(importDecl.alias, { topLevelScope: scopeChain, members: null, tcContext: context });
>aliasedModSymbol : Symbol
>findSymbolFromAlias(importDecl.alias, { topLevelScope: scopeChain, members: null, tcContext: context }) : Symbol
>findSymbolFromAlias : (alias: any (error), context: IAliasScopeContext) => Symbol
>importDecl.alias : any (error)
>importDecl : any (error)
>alias : any (error)
>{ topLevelScope: scopeChain, members: null, tcContext: context } : { topLevelScope: any (error); members: null; tcContext: any (error); }
>topLevelScope : any (error)
>scopeChain : any (error)
>members : null
>null : null
>tcContext : any (error)
>context : any (error)

        var isGlobal = context.scopeChain.container == context.checker.gloMod;
>isGlobal : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

        if (aliasedModSymbol) {
>aliasedModSymbol : Symbol

            var aliasedModType = aliasedModSymbol.getType();
>aliasedModType : any (error)
>aliasedModSymbol.getType() : any (error)
>aliasedModSymbol.getType : any (error)
>aliasedModSymbol : Symbol
>getType : any (error)

            if (aliasedModType) {
>aliasedModType : any (error)

                modType = <ModuleType>aliasedModType;
>modType = <ModuleType>aliasedModType : any (error)
>modType : any (error)
><ModuleType>aliasedModType : any (error)
>aliasedModType : any (error)
            }
        }

        typeSymbol = new TypeSymbol(importDecl.id.text, importDecl.minChar,
>typeSymbol = new TypeSymbol(importDecl.id.text, importDecl.minChar,                                    context.checker.locationInfo.unitIndex, modType) : any (error)
>typeSymbol : any (error)
>new TypeSymbol(importDecl.id.text, importDecl.minChar,                                    context.checker.locationInfo.unitIndex, modType) : any (error)
>TypeSymbol : any (error)
>importDecl.id.text : any (error)
>importDecl.id : any (error)
>importDecl : any (error)
>id : any (error)
>text : any (error)
>importDecl.minChar : any (error)
>importDecl : any (error)
>minChar : any (error)

                                    context.checker.locationInfo.unitIndex, modType);
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)
>modType : any (error)

        typeSymbol.aliasLink = importDecl;
>typeSymbol.aliasLink = importDecl : any (error)
>typeSymbol.aliasLink : any (error)
>typeSymbol : any (error)
>aliasLink : any (error)
>importDecl : any (error)

        if (context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

            typeSymbol.declModule = context.scopeChain.moduleDecl;
>typeSymbol.declModule = context.scopeChain.moduleDecl : any (error)
>typeSymbol.declModule : any (error)
>typeSymbol : any (error)
>declModule : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
        }
        typeSymbol.declAST = importDecl;
>typeSymbol.declAST = importDecl : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>importDecl : any (error)

        importDecl.id.sym = typeSymbol;
>importDecl.id.sym = typeSymbol : any (error)
>importDecl.id.sym : any (error)
>importDecl.id : any (error)
>importDecl : any (error)
>id : any (error)
>sym : any (error)
>typeSymbol : any (error)

        scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,
>scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,                                context.checker.errorReporter, isExported || isGlobal, true, false) : any (error)
>scopeChain.scope.enter : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>scopeChain.container : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)
>typeSymbol : any (error)

                                context.checker.errorReporter, isExported || isGlobal, true, false);
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isExported || isGlobal : any
>isExported : any (error)
>isGlobal : boolean
>true : true
>false : false

        scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,
>scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,                                context.checker.errorReporter, isExported || isGlobal, false, false) : any (error)
>scopeChain.scope.enter : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>scopeChain.container : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)
>typeSymbol : any (error)

                                context.checker.errorReporter, isExported || isGlobal, false, false);
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isExported || isGlobal : any
>isExported : any (error)
>isGlobal : boolean
>false : false
>false : false

        return true;
>true : true
    }

    export function preCollectModuleTypes(ast: AST, parent: AST, context: TypeCollectionContext) {
>preCollectModuleTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        var moduleDecl: ModuleDeclaration = <ModuleDeclaration>ast;
>moduleDecl : any (error)
><ModuleDeclaration>ast : any (error)
>ast : any (error)

        var isAmbient = hasFlag(moduleDecl.modFlags, ModuleFlags.Ambient);
>isAmbient : any (error)
>hasFlag(moduleDecl.modFlags, ModuleFlags.Ambient) : any (error)
>hasFlag : any (error)
>moduleDecl.modFlags : any (error)
>moduleDecl : any (error)
>modFlags : any (error)
>ModuleFlags.Ambient : any (error)
>ModuleFlags : any (error)
>Ambient : any (error)

        var isEnum = hasFlag(moduleDecl.modFlags, ModuleFlags.IsEnum);
>isEnum : any (error)
>hasFlag(moduleDecl.modFlags, ModuleFlags.IsEnum) : any (error)
>hasFlag : any (error)
>moduleDecl.modFlags : any (error)
>moduleDecl : any (error)
>modFlags : any (error)
>ModuleFlags.IsEnum : any (error)
>ModuleFlags : any (error)
>IsEnum : any (error)

        var isGlobal = context.scopeChain.container == context.checker.gloMod;
>isGlobal : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

        var isExported = hasFlag(moduleDecl.modFlags, ModuleFlags.Exported);
>isExported : any (error)
>hasFlag(moduleDecl.modFlags, ModuleFlags.Exported) : any (error)
>hasFlag : any (error)
>moduleDecl.modFlags : any (error)
>moduleDecl : any (error)
>modFlags : any (error)
>ModuleFlags.Exported : any (error)
>ModuleFlags : any (error)
>Exported : any (error)

        var modName = (<Identifier>moduleDecl.name).text;
>modName : any (error)
>(<Identifier>moduleDecl.name).text : any (error)
>(<Identifier>moduleDecl.name) : any (error)
><Identifier>moduleDecl.name : any (error)
>moduleDecl.name : any (error)
>moduleDecl : any (error)
>name : any (error)
>text : any (error)

        var isDynamic = isQuoted(modName);
>isDynamic : any (error)
>isQuoted(modName) : any (error)
>isQuoted : any (error)
>modName : any (error)

        var symbol = scopeChain.scope.findLocal(modName, false, false);
>symbol : any (error)
>scopeChain.scope.findLocal(modName, false, false) : any (error)
>scopeChain.scope.findLocal : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>findLocal : any (error)
>modName : any (error)
>false : false
>false : false

        var typeSymbol: TypeSymbol = null;
>typeSymbol : any (error)
>null : null

        var modType: ModuleType = null;
>modType : any (error)
>null : null

        if ((symbol == null) || (symbol.kind() != SymbolKind.Type)) {
>(symbol == null) || (symbol.kind() != SymbolKind.Type) : boolean
>(symbol == null) : boolean
>symbol == null : boolean
>symbol : any (error)
>null : null
>(symbol.kind() != SymbolKind.Type) : boolean
>symbol.kind() != SymbolKind.Type : boolean
>symbol.kind() : any (error)
>symbol.kind : any (error)
>symbol : any (error)
>kind : any (error)
>SymbolKind.Type : any (error)
>SymbolKind : any (error)
>Type : any (error)

            if (modType == null) {
>modType == null : boolean
>modType : any (error)
>null : null

                var enclosedTypes = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>enclosedTypes : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

                var ambientEnclosedTypes = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>ambientEnclosedTypes : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

                modType = new ModuleType(enclosedTypes, ambientEnclosedTypes);
>modType = new ModuleType(enclosedTypes, ambientEnclosedTypes) : any (error)
>modType : any (error)
>new ModuleType(enclosedTypes, ambientEnclosedTypes) : any (error)
>ModuleType : any (error)
>enclosedTypes : any (error)
>ambientEnclosedTypes : any (error)

                if (isEnum) {
>isEnum : any (error)

                    modType.typeFlags |= TypeFlags.IsEnum;
>modType.typeFlags |= TypeFlags.IsEnum : number
>modType.typeFlags : any (error)
>modType : any (error)
>typeFlags : any (error)
>TypeFlags.IsEnum : any (error)
>TypeFlags : any (error)
>IsEnum : any (error)
                }
                modType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>modType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>modType.members : any (error)
>modType : any (error)
>members : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

                modType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>modType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>modType.ambientMembers : any (error)
>modType : any (error)
>ambientMembers : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

                modType.setHasImplementation();
>modType.setHasImplementation() : any (error)
>modType.setHasImplementation : any (error)
>modType : any (error)
>setHasImplementation : any (error)
            }

            typeSymbol = new TypeSymbol(modName, moduleDecl.minChar,
>typeSymbol = new TypeSymbol(modName, moduleDecl.minChar,                                        context.checker.locationInfo.unitIndex, modType) : any (error)
>typeSymbol : any (error)
>new TypeSymbol(modName, moduleDecl.minChar,                                        context.checker.locationInfo.unitIndex, modType) : any (error)
>TypeSymbol : any (error)
>modName : any (error)
>moduleDecl.minChar : any (error)
>moduleDecl : any (error)
>minChar : any (error)

                                        context.checker.locationInfo.unitIndex, modType);
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)
>modType : any (error)

            if (context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

                typeSymbol.declModule = context.scopeChain.moduleDecl;
>typeSymbol.declModule = context.scopeChain.moduleDecl : any (error)
>typeSymbol.declModule : any (error)
>typeSymbol : any (error)
>declModule : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
            }
            typeSymbol.declAST = moduleDecl;
>typeSymbol.declAST = moduleDecl : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>moduleDecl : any (error)

            typeSymbol.prettyName = moduleDecl.prettyName;
>typeSymbol.prettyName = moduleDecl.prettyName : any (error)
>typeSymbol.prettyName : any (error)
>typeSymbol : any (error)
>prettyName : any (error)
>moduleDecl.prettyName : any (error)
>moduleDecl : any (error)
>prettyName : any (error)

            scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,
>scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,                                    context.checker.errorReporter, isExported || isGlobal, true, isAmbient) : any (error)
>scopeChain.scope.enter : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>scopeChain.container : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)
>typeSymbol : any (error)

                                    context.checker.errorReporter, isExported || isGlobal, true, isAmbient);
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isExported || isGlobal : any
>isExported : any (error)
>isGlobal : boolean
>true : true
>isAmbient : any (error)

            scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,
>scopeChain.scope.enter(scopeChain.container, ast, typeSymbol,                                    context.checker.errorReporter, isExported || isGlobal, false, isAmbient) : any (error)
>scopeChain.scope.enter : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>scopeChain.container : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)
>typeSymbol : any (error)

                                    context.checker.errorReporter, isExported || isGlobal, false, isAmbient);
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isExported || isGlobal : any
>isExported : any (error)
>isGlobal : boolean
>false : false
>isAmbient : any (error)

            modType.symbol = typeSymbol;
>modType.symbol = typeSymbol : any (error)
>modType.symbol : any (error)
>modType : any (error)
>symbol : any (error)
>typeSymbol : any (error)
        }
        else {
            if (symbol && symbol.declAST && symbol.declAST.nodeType != NodeType.ModuleDeclaration) {
>symbol && symbol.declAST && symbol.declAST.nodeType != NodeType.ModuleDeclaration : boolean
>symbol && symbol.declAST : any
>symbol : any (error)
>symbol.declAST : any (error)
>symbol : any (error)
>declAST : any (error)
>symbol.declAST.nodeType != NodeType.ModuleDeclaration : boolean
>symbol.declAST.nodeType : any (error)
>symbol.declAST : any (error)
>symbol : any (error)
>declAST : any (error)
>nodeType : any (error)
>NodeType.ModuleDeclaration : any (error)
>NodeType : any (error)
>ModuleDeclaration : any (error)

                context.checker.errorReporter.simpleError(moduleDecl, "Conflicting symbol name for module '" + modName + "'");
>context.checker.errorReporter.simpleError(moduleDecl, "Conflicting symbol name for module '" + modName + "'") : any (error)
>context.checker.errorReporter.simpleError : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>simpleError : any (error)
>moduleDecl : any (error)
>"Conflicting symbol name for module '" + modName + "'" : string
>"Conflicting symbol name for module '" + modName : string
>"Conflicting symbol name for module '" : "Conflicting symbol name for module '"
>modName : any (error)
>"'" : "'"
            }
            typeSymbol = <TypeSymbol>symbol;
>typeSymbol = <TypeSymbol>symbol : any (error)
>typeSymbol : any (error)
><TypeSymbol>symbol : any (error)
>symbol : any (error)

            // initialize new private scope for the type
            var publicEnclosedTypes = typeSymbol.type.getAllEnclosedTypes().publicMembers;
>publicEnclosedTypes : any (error)
>typeSymbol.type.getAllEnclosedTypes().publicMembers : any (error)
>typeSymbol.type.getAllEnclosedTypes() : any (error)
>typeSymbol.type.getAllEnclosedTypes : any (error)
>typeSymbol.type : any (error)
>typeSymbol : any (error)
>type : any (error)
>getAllEnclosedTypes : any (error)
>publicMembers : any (error)

            var publicEnclosedTypesTable = (publicEnclosedTypes == null) ? new StringHashTable() : publicEnclosedTypes;
>publicEnclosedTypesTable : any
>(publicEnclosedTypes == null) ? new StringHashTable() : publicEnclosedTypes : any
>(publicEnclosedTypes == null) : boolean
>publicEnclosedTypes == null : boolean
>publicEnclosedTypes : any (error)
>null : null
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>publicEnclosedTypes : any (error)

            var enclosedTypes = new ScopedMembers(new DualStringHashTable(publicEnclosedTypesTable, new StringHashTable()));
>enclosedTypes : any (error)
>new ScopedMembers(new DualStringHashTable(publicEnclosedTypesTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(publicEnclosedTypesTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>publicEnclosedTypesTable : any
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            var publicEnclosedAmbientTypes = typeSymbol.type.getAllAmbientEnclosedTypes().publicMembers;
>publicEnclosedAmbientTypes : any (error)
>typeSymbol.type.getAllAmbientEnclosedTypes().publicMembers : any (error)
>typeSymbol.type.getAllAmbientEnclosedTypes() : any (error)
>typeSymbol.type.getAllAmbientEnclosedTypes : any (error)
>typeSymbol.type : any (error)
>typeSymbol : any (error)
>type : any (error)
>getAllAmbientEnclosedTypes : any (error)
>publicMembers : any (error)

            var publicAmbientEnclosedTypesTable = (publicEnclosedAmbientTypes == null) ? new StringHashTable() : publicEnclosedAmbientTypes;
>publicAmbientEnclosedTypesTable : any
>(publicEnclosedAmbientTypes == null) ? new StringHashTable() : publicEnclosedAmbientTypes : any
>(publicEnclosedAmbientTypes == null) : boolean
>publicEnclosedAmbientTypes == null : boolean
>publicEnclosedAmbientTypes : any (error)
>null : null
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>publicEnclosedAmbientTypes : any (error)

            var ambientEnclosedTypes = new ScopedMembers(new DualStringHashTable(publicAmbientEnclosedTypesTable, new StringHashTable()));
>ambientEnclosedTypes : any (error)
>new ScopedMembers(new DualStringHashTable(publicAmbientEnclosedTypesTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(publicAmbientEnclosedTypesTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>publicAmbientEnclosedTypesTable : any
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            var publicMembers = typeSymbol.type.members.publicMembers;
>publicMembers : any (error)
>typeSymbol.type.members.publicMembers : any (error)
>typeSymbol.type.members : any (error)
>typeSymbol.type : any (error)
>typeSymbol : any (error)
>type : any (error)
>members : any (error)
>publicMembers : any (error)

            var publicMembersTable = (publicMembers == null) ? new StringHashTable() : publicMembers;
>publicMembersTable : any
>(publicMembers == null) ? new StringHashTable() : publicMembers : any
>(publicMembers == null) : boolean
>publicMembers == null : boolean
>publicMembers : any (error)
>null : null
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>publicMembers : any (error)

            var members = new ScopedMembers(new DualStringHashTable(publicMembersTable, new StringHashTable()));
>members : any (error)
>new ScopedMembers(new DualStringHashTable(publicMembersTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(publicMembersTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>publicMembersTable : any
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            var publicAmbientMembers = typeSymbol.type.ambientMembers.publicMembers;
>publicAmbientMembers : any (error)
>typeSymbol.type.ambientMembers.publicMembers : any (error)
>typeSymbol.type.ambientMembers : any (error)
>typeSymbol.type : any (error)
>typeSymbol : any (error)
>type : any (error)
>ambientMembers : any (error)
>publicMembers : any (error)

            var publicAmbientMembersTable = (publicAmbientMembers == null) ? new StringHashTable() : publicAmbientMembers;
>publicAmbientMembersTable : any
>(publicAmbientMembers == null) ? new StringHashTable() : publicAmbientMembers : any
>(publicAmbientMembers == null) : boolean
>publicAmbientMembers == null : boolean
>publicAmbientMembers : any (error)
>null : null
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>publicAmbientMembers : any (error)

            var ambientMembers = new ScopedMembers(new DualStringHashTable(publicAmbientMembersTable, new StringHashTable()));
>ambientMembers : any (error)
>new ScopedMembers(new DualStringHashTable(publicAmbientMembersTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(publicAmbientMembersTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>publicAmbientMembersTable : any
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            modType = new ModuleType(enclosedTypes, ambientEnclosedTypes);
>modType = new ModuleType(enclosedTypes, ambientEnclosedTypes) : any (error)
>modType : any (error)
>new ModuleType(enclosedTypes, ambientEnclosedTypes) : any (error)
>ModuleType : any (error)
>enclosedTypes : any (error)
>ambientEnclosedTypes : any (error)

            if (isEnum) {
>isEnum : any (error)

                modType.typeFlags |= TypeFlags.IsEnum;
>modType.typeFlags |= TypeFlags.IsEnum : number
>modType.typeFlags : any (error)
>modType : any (error)
>typeFlags : any (error)
>TypeFlags.IsEnum : any (error)
>TypeFlags : any (error)
>IsEnum : any (error)
            }
            modType.members = members;
>modType.members = members : any (error)
>modType.members : any (error)
>modType : any (error)
>members : any (error)
>members : any (error)

            modType.ambientMembers = ambientMembers;
>modType.ambientMembers = ambientMembers : any (error)
>modType.ambientMembers : any (error)
>modType : any (error)
>ambientMembers : any (error)
>ambientMembers : any (error)

            modType.setHasImplementation();
>modType.setHasImplementation() : any (error)
>modType.setHasImplementation : any (error)
>modType : any (error)
>setHasImplementation : any (error)

            modType.symbol = typeSymbol;
>modType.symbol = typeSymbol : any (error)
>modType.symbol : any (error)
>modType : any (error)
>symbol : any (error)
>typeSymbol : any (error)

            typeSymbol.addLocation(moduleDecl.minChar);
>typeSymbol.addLocation(moduleDecl.minChar) : any (error)
>typeSymbol.addLocation : any (error)
>typeSymbol : any (error)
>addLocation : any (error)
>moduleDecl.minChar : any (error)
>moduleDecl : any (error)
>minChar : any (error)

            typeSymbol.expansions.push(modType);
>typeSymbol.expansions.push(modType) : any (error)
>typeSymbol.expansions.push : any (error)
>typeSymbol.expansions : any (error)
>typeSymbol : any (error)
>expansions : any (error)
>push : any (error)
>modType : any (error)

        }
        if (context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

            context.scopeChain.moduleDecl.recordNonInterface();
>context.scopeChain.moduleDecl.recordNonInterface() : any (error)
>context.scopeChain.moduleDecl.recordNonInterface : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
>recordNonInterface : any (error)
        }
        // REVIEW: If multiple disparate module decls for the same module don't agree
        // in export privileges, how should we handle it?
        if (isExported) {
>isExported : any (error)

            typeSymbol.flags |= SymbolFlags.Exported;
>typeSymbol.flags |= SymbolFlags.Exported : number
>typeSymbol.flags : any (error)
>typeSymbol : any (error)
>flags : any (error)
>SymbolFlags.Exported : any (error)
>SymbolFlags : any (error)
>Exported : any (error)
        }
        if ((context.scopeChain.moduleDecl) ||
>(context.scopeChain.moduleDecl) ||            (context.scopeChain.container == context.checker.gloMod) : any
>(context.scopeChain.moduleDecl) : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

            (context.scopeChain.container == context.checker.gloMod)) {
>(context.scopeChain.container == context.checker.gloMod) : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

            typeSymbol.flags |= SymbolFlags.ModuleMember;
>typeSymbol.flags |= SymbolFlags.ModuleMember : number
>typeSymbol.flags : any (error)
>typeSymbol : any (error)
>flags : any (error)
>SymbolFlags.ModuleMember : any (error)
>SymbolFlags : any (error)
>ModuleMember : any (error)
        }

        moduleDecl.mod = modType;
>moduleDecl.mod = modType : any (error)
>moduleDecl.mod : any (error)
>moduleDecl : any (error)
>mod : any (error)
>modType : any (error)

        pushTypeCollectionScope(typeSymbol, modType.members,
>pushTypeCollectionScope(typeSymbol, modType.members,                                modType.ambientMembers,                                modType.enclosedTypes,                                modType.ambientEnclosedTypes,                                context, null, null, moduleDecl) : any (error)
>pushTypeCollectionScope : any (error)
>typeSymbol : any (error)
>modType.members : any (error)
>modType : any (error)
>members : any (error)

                                modType.ambientMembers,
>modType.ambientMembers : any (error)
>modType : any (error)
>ambientMembers : any (error)

                                modType.enclosedTypes,
>modType.enclosedTypes : any (error)
>modType : any (error)
>enclosedTypes : any (error)

                                modType.ambientEnclosedTypes,
>modType.ambientEnclosedTypes : any (error)
>modType : any (error)
>ambientEnclosedTypes : any (error)

                                context, null, null, moduleDecl);
>context : any (error)
>null : null
>null : null
>moduleDecl : any (error)

        return true;
>true : true
    }

    export function preCollectClassTypes(ast: AST, parent: AST, context: TypeCollectionContext) {
>preCollectClassTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        var classDecl = <ClassDeclaration>ast;
>classDecl : any (error)
><ClassDeclaration>ast : any (error)
>ast : any (error)

        var classType: Type;
>classType : any (error)

        var instanceType: Type;
>instanceType : any (error)

        var typeSymbol: TypeSymbol = null;
>typeSymbol : any (error)
>null : null

        var className = (<Identifier>classDecl.name).text;
>className : any (error)
>(<Identifier>classDecl.name).text : any (error)
>(<Identifier>classDecl.name) : any (error)
><Identifier>classDecl.name : any (error)
>classDecl.name : any (error)
>classDecl : any (error)
>name : any (error)
>text : any (error)

        var alreadyInScope = false;
>alreadyInScope : boolean
>false : false

        var isAmbient = hasFlag(classDecl.varFlags, VarFlags.Ambient);
>isAmbient : any (error)
>hasFlag(classDecl.varFlags, VarFlags.Ambient) : any (error)
>hasFlag : any (error)
>classDecl.varFlags : any (error)
>classDecl : any (error)
>varFlags : any (error)
>VarFlags.Ambient : any (error)
>VarFlags : any (error)
>Ambient : any (error)

        var isExported = hasFlag(classDecl.varFlags, VarFlags.Exported);
>isExported : any (error)
>hasFlag(classDecl.varFlags, VarFlags.Exported) : any (error)
>hasFlag : any (error)
>classDecl.varFlags : any (error)
>classDecl : any (error)
>varFlags : any (error)
>VarFlags.Exported : any (error)
>VarFlags : any (error)
>Exported : any (error)

        var isGlobal = context.scopeChain.container == context.checker.gloMod;
>isGlobal : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

        var containerMod = <TypeSymbol>scopeChain.container;
>containerMod : any (error)
><TypeSymbol>scopeChain.container : any (error)
>scopeChain.container : any (error)
>scopeChain : any (error)
>container : any (error)

        var foundValSymbol = false;
>foundValSymbol : boolean
>false : false

        typeSymbol = <TypeSymbol>scopeChain.scope.findLocal(className, false, true);
>typeSymbol = <TypeSymbol>scopeChain.scope.findLocal(className, false, true) : any (error)
>typeSymbol : any (error)
><TypeSymbol>scopeChain.scope.findLocal(className, false, true) : any (error)
>scopeChain.scope.findLocal(className, false, true) : any (error)
>scopeChain.scope.findLocal : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>findLocal : any (error)
>className : any (error)
>false : false
>true : true
        
        // check the value space, since an override may have been declared with the type's name
        // REVIEW-CLASSES
        if (!typeSymbol) {
>!typeSymbol : boolean
>typeSymbol : any (error)

            var valTypeSymbol = scopeChain.scope.findLocal(className, false, false);
>valTypeSymbol : any (error)
>scopeChain.scope.findLocal(className, false, false) : any (error)
>scopeChain.scope.findLocal : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>findLocal : any (error)
>className : any (error)
>false : false
>false : false
            
            if (valTypeSymbol &&
>valTypeSymbol &&                valTypeSymbol.isType() &&                valTypeSymbol.declAST &&                valTypeSymbol.declAST.nodeType == NodeType.FuncDecl &&                (<FuncDecl>valTypeSymbol.declAST).isSignature() : any
>valTypeSymbol &&                valTypeSymbol.isType() &&                valTypeSymbol.declAST &&                valTypeSymbol.declAST.nodeType == NodeType.FuncDecl : boolean
>valTypeSymbol &&                valTypeSymbol.isType() &&                valTypeSymbol.declAST : any
>valTypeSymbol &&                valTypeSymbol.isType() : any
>valTypeSymbol : any (error)

                valTypeSymbol.isType() &&
>valTypeSymbol.isType() : any (error)
>valTypeSymbol.isType : any (error)
>valTypeSymbol : any (error)
>isType : any (error)

                valTypeSymbol.declAST &&
>valTypeSymbol.declAST : any (error)
>valTypeSymbol : any (error)
>declAST : any (error)

                valTypeSymbol.declAST.nodeType == NodeType.FuncDecl &&
>valTypeSymbol.declAST.nodeType == NodeType.FuncDecl : boolean
>valTypeSymbol.declAST.nodeType : any (error)
>valTypeSymbol.declAST : any (error)
>valTypeSymbol : any (error)
>declAST : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)

                (<FuncDecl>valTypeSymbol.declAST).isSignature()) {
>(<FuncDecl>valTypeSymbol.declAST).isSignature() : any (error)
>(<FuncDecl>valTypeSymbol.declAST).isSignature : any (error)
>(<FuncDecl>valTypeSymbol.declAST) : any (error)
><FuncDecl>valTypeSymbol.declAST : any (error)
>valTypeSymbol.declAST : any (error)
>valTypeSymbol : any (error)
>declAST : any (error)
>isSignature : any (error)
                
                typeSymbol = <TypeSymbol>valTypeSymbol;
>typeSymbol = <TypeSymbol>valTypeSymbol : any (error)
>typeSymbol : any (error)
><TypeSymbol>valTypeSymbol : any (error)
>valTypeSymbol : any (error)

                foundValSymbol = true;
>foundValSymbol = true : true
>foundValSymbol : boolean
>true : true
                
                if (isExported) {
>isExported : any (error)

                    typeSymbol.flags |= SymbolFlags.Exported;
>typeSymbol.flags |= SymbolFlags.Exported : number
>typeSymbol.flags : any (error)
>typeSymbol : any (error)
>flags : any (error)
>SymbolFlags.Exported : any (error)
>SymbolFlags : any (error)
>Exported : any (error)
                }
            
                if (isAmbient) {
>isAmbient : any (error)

                    typeSymbol.flags |= SymbolFlags.Ambient;
>typeSymbol.flags |= SymbolFlags.Ambient : number
>typeSymbol.flags : any (error)
>typeSymbol : any (error)
>flags : any (error)
>SymbolFlags.Ambient : any (error)
>SymbolFlags : any (error)
>Ambient : any (error)
                }                
                
                // the class was never entered into type space, so add it
                context.scopeChain.scope.enter(context.scopeChain.container, ast, typeSymbol,
>context.scopeChain.scope.enter(context.scopeChain.container, ast, typeSymbol,                                            context.checker.errorReporter, isExported || isGlobal, true, isAmbient) : any (error)
>context.scopeChain.scope.enter : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)
>typeSymbol : any (error)

                                            context.checker.errorReporter, isExported || isGlobal, true, isAmbient);                
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isExported || isGlobal : any
>isExported : any (error)
>isGlobal : boolean
>true : true
>isAmbient : any (error)
            }
        }
        
        if (typeSymbol && !foundValSymbol && (typeSymbol.declAST != classDecl)) {
>typeSymbol && !foundValSymbol && (typeSymbol.declAST != classDecl) : boolean
>typeSymbol && !foundValSymbol : boolean
>typeSymbol : any (error)
>!foundValSymbol : boolean
>foundValSymbol : boolean
>(typeSymbol.declAST != classDecl) : boolean
>typeSymbol.declAST != classDecl : boolean
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>classDecl : any (error)

            typeSymbol = null;
>typeSymbol = null : null
>typeSymbol : any (error)
>null : null
        }

        if (typeSymbol == null) {
>typeSymbol == null : boolean
>typeSymbol : any (error)
>null : null

            var valueSymbol = scopeChain.scope.findLocal(className, false, false);
>valueSymbol : any (error)
>scopeChain.scope.findLocal(className, false, false) : any (error)
>scopeChain.scope.findLocal : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>findLocal : any (error)
>className : any (error)
>false : false
>false : false

            classType = new Type();
>classType = new Type() : any (error)
>classType : any (error)
>new Type() : any (error)
>Type : any (error)

            classType.setHasImplementation();
>classType.setHasImplementation() : any (error)
>classType.setHasImplementation : any (error)
>classType : any (error)
>setHasImplementation : any (error)

            instanceType = new Type();
>instanceType = new Type() : any (error)
>instanceType : any (error)
>new Type() : any (error)
>Type : any (error)

            instanceType.setHasImplementation();
>instanceType.setHasImplementation() : any (error)
>instanceType.setHasImplementation : any (error)
>instanceType : any (error)
>setHasImplementation : any (error)

            classType.instanceType = instanceType;
>classType.instanceType = instanceType : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)
>instanceType : any (error)

            classType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>classType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>classType.members : any (error)
>classType : any (error)
>members : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            classType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>classType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>classType.ambientMembers : any (error)
>classType : any (error)
>ambientMembers : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            addPrototypeField(classType, classDecl, context);
>addPrototypeField(classType, classDecl, context) : void
>addPrototypeField : (classType: any (error), ast: any (error), context: any (error)) => void
>classType : any (error)
>classDecl : any (error)
>context : any (error)

            instanceType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>instanceType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>instanceType.members : any (error)
>instanceType : any (error)
>members : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            instanceType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>instanceType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>instanceType.ambientMembers : any (error)
>instanceType : any (error)
>ambientMembers : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            typeSymbol = new TypeSymbol(className, classDecl.minChar,
>typeSymbol = new TypeSymbol(className, classDecl.minChar,                                        context.checker.locationInfo.unitIndex, classType) : any (error)
>typeSymbol : any (error)
>new TypeSymbol(className, classDecl.minChar,                                        context.checker.locationInfo.unitIndex, classType) : any (error)
>TypeSymbol : any (error)
>className : any (error)
>classDecl.minChar : any (error)
>classDecl : any (error)
>minChar : any (error)

                                        context.checker.locationInfo.unitIndex, classType);
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)
>classType : any (error)

            typeSymbol.declAST = classDecl;
>typeSymbol.declAST = classDecl : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>classDecl : any (error)

            typeSymbol.instanceType = instanceType;
>typeSymbol.instanceType = instanceType : any (error)
>typeSymbol.instanceType : any (error)
>typeSymbol : any (error)
>instanceType : any (error)
>instanceType : any (error)

            classType.symbol = typeSymbol;
>classType.symbol = typeSymbol : any (error)
>classType.symbol : any (error)
>classType : any (error)
>symbol : any (error)
>typeSymbol : any (error)

            instanceType.symbol = typeSymbol;
>instanceType.symbol = typeSymbol : any (error)
>instanceType.symbol : any (error)
>instanceType : any (error)
>symbol : any (error)
>typeSymbol : any (error)

            if (context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

                context.scopeChain.moduleDecl.recordNonInterface();
>context.scopeChain.moduleDecl.recordNonInterface() : any (error)
>context.scopeChain.moduleDecl.recordNonInterface : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
>recordNonInterface : any (error)

                typeSymbol.declModule = context.scopeChain.moduleDecl;
>typeSymbol.declModule = context.scopeChain.moduleDecl : any (error)
>typeSymbol.declModule : any (error)
>typeSymbol : any (error)
>declModule : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

                typeSymbol.flags |= SymbolFlags.ModuleMember;
>typeSymbol.flags |= SymbolFlags.ModuleMember : number
>typeSymbol.flags : any (error)
>typeSymbol : any (error)
>flags : any (error)
>SymbolFlags.ModuleMember : any (error)
>SymbolFlags : any (error)
>ModuleMember : any (error)
            }

            if (isExported) {
>isExported : any (error)

                typeSymbol.flags |= SymbolFlags.Exported;
>typeSymbol.flags |= SymbolFlags.Exported : number
>typeSymbol.flags : any (error)
>typeSymbol : any (error)
>flags : any (error)
>SymbolFlags.Exported : any (error)
>SymbolFlags : any (error)
>Exported : any (error)
            }
            
            if (isAmbient) {
>isAmbient : any (error)

                typeSymbol.flags |= SymbolFlags.Ambient;
>typeSymbol.flags |= SymbolFlags.Ambient : number
>typeSymbol.flags : any (error)
>typeSymbol : any (error)
>flags : any (error)
>SymbolFlags.Ambient : any (error)
>SymbolFlags : any (error)
>Ambient : any (error)
            }

            ast.type = classType;
>ast.type = classType : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)
>classType : any (error)

            // class in both name spaces (type for instance type; constructor representative in value space)
            context.scopeChain.scope.enter(context.scopeChain.container, ast, typeSymbol,
>context.scopeChain.scope.enter(context.scopeChain.container, ast, typeSymbol,                                            context.checker.errorReporter, isExported || isGlobal, true, isAmbient) : any (error)
>context.scopeChain.scope.enter : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)
>typeSymbol : any (error)

                                            context.checker.errorReporter, isExported || isGlobal, true, isAmbient);
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isExported || isGlobal : any
>isExported : any (error)
>isGlobal : boolean
>true : true
>isAmbient : any (error)

            if (valueSymbol == null) {
>valueSymbol == null : boolean
>valueSymbol : any (error)
>null : null

                context.scopeChain.scope.enter(context.scopeChain.container, ast, typeSymbol,
>context.scopeChain.scope.enter(context.scopeChain.container, ast, typeSymbol,                                            context.checker.errorReporter, isExported || isGlobal, false, isAmbient) : any (error)
>context.scopeChain.scope.enter : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)
>typeSymbol : any (error)

                                            context.checker.errorReporter, isExported || isGlobal, false, isAmbient);
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isExported || isGlobal : any
>isExported : any (error)
>isGlobal : boolean
>false : false
>isAmbient : any (error)
            }
        }
        else {                            
            classType = typeSymbol.type;
>classType = typeSymbol.type : any (error)
>classType : any (error)
>typeSymbol.type : any (error)
>typeSymbol : any (error)
>type : any (error)
            
            // If the instance type is null, a call overload was likely declared before the class constructor
            if (classType.instanceType == null) {
>classType.instanceType == null : boolean
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)
>null : null

                classType.instanceType = new Type();
>classType.instanceType = new Type() : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)
>new Type() : any (error)
>Type : any (error)

                classType.instanceType.setHasImplementation();
>classType.instanceType.setHasImplementation() : any (error)
>classType.instanceType.setHasImplementation : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)
>setHasImplementation : any (error)

                classType.instanceType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>classType.instanceType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>classType.instanceType.members : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)
>members : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

                classType.instanceType.symbol = classType.symbol;
>classType.instanceType.symbol = classType.symbol : any (error)
>classType.instanceType.symbol : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)
>symbol : any (error)
>classType.symbol : any (error)
>classType : any (error)
>symbol : any (error)

                classType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>classType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>classType.members : any (error)
>classType : any (error)
>members : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

                classType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>classType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>classType.ambientMembers : any (error)
>classType : any (error)
>ambientMembers : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
            }
            
            instanceType = classType.instanceType;
>instanceType = classType.instanceType : any (error)
>instanceType : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)

            ast.type = classType;
>ast.type = classType : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)
>classType : any (error)
        }
        
        // if the class has no declared constructor, either create a default signature or adapt 
        // it's base class's signature group
        if (!classDecl.constructorDecl) {
>!classDecl.constructorDecl : boolean
>classDecl.constructorDecl : any (error)
>classDecl : any (error)
>constructorDecl : any (error)

            if (typeSymbol && typeSymbol.declAST && typeSymbol.declAST.type && typeSymbol.declAST.type.call && !(<FuncDecl>typeSymbol.declAST).isOverload) {
>typeSymbol && typeSymbol.declAST && typeSymbol.declAST.type && typeSymbol.declAST.type.call && !(<FuncDecl>typeSymbol.declAST).isOverload : boolean
>typeSymbol && typeSymbol.declAST && typeSymbol.declAST.type && typeSymbol.declAST.type.call : any
>typeSymbol && typeSymbol.declAST && typeSymbol.declAST.type : any
>typeSymbol && typeSymbol.declAST : any
>typeSymbol : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>typeSymbol.declAST.type : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>type : any (error)
>typeSymbol.declAST.type.call : any (error)
>typeSymbol.declAST.type : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>type : any (error)
>call : any (error)
>!(<FuncDecl>typeSymbol.declAST).isOverload : boolean
>(<FuncDecl>typeSymbol.declAST).isOverload : any (error)
>(<FuncDecl>typeSymbol.declAST) : any (error)
><FuncDecl>typeSymbol.declAST : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>isOverload : any (error)

                context.checker.errorReporter.duplicateIdentifier(typeSymbol.declAST, typeSymbol.name);
>context.checker.errorReporter.duplicateIdentifier(typeSymbol.declAST, typeSymbol.name) : any (error)
>context.checker.errorReporter.duplicateIdentifier : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>duplicateIdentifier : any (error)
>typeSymbol.declAST : any (error)
>typeSymbol : any (error)
>declAST : any (error)
>typeSymbol.name : any (error)
>typeSymbol : any (error)
>name : any (error)
            }

            createNewConstructGroupForType(classDecl.type);
>createNewConstructGroupForType(classDecl.type) : void
>createNewConstructGroupForType : (type: any (error)) => void
>classDecl.type : any (error)
>classDecl : any (error)
>type : any (error)
        }

        classType.typeFlags |= TypeFlags.IsClass;
>classType.typeFlags |= TypeFlags.IsClass : number
>classType.typeFlags : any (error)
>classType : any (error)
>typeFlags : any (error)
>TypeFlags.IsClass : any (error)
>TypeFlags : any (error)
>IsClass : any (error)

        instanceType.typeFlags |= TypeFlags.IsClass;
>instanceType.typeFlags |= TypeFlags.IsClass : number
>instanceType.typeFlags : any (error)
>instanceType : any (error)
>typeFlags : any (error)
>TypeFlags.IsClass : any (error)
>TypeFlags : any (error)
>IsClass : any (error)

        getBases(instanceType, classDecl);
>getBases(instanceType, classDecl) : void
>getBases : (type: any (error), typeDecl: any (error)) => void
>instanceType : any (error)
>classDecl : any (error)

        pushTypeCollectionScope(typeSymbol, instanceType.members, instanceType.ambientMembers, null, null,
>pushTypeCollectionScope(typeSymbol, instanceType.members, instanceType.ambientMembers, null, null,                                context, instanceType, classType, null) : any (error)
>pushTypeCollectionScope : any (error)
>typeSymbol : any (error)
>instanceType.members : any (error)
>instanceType : any (error)
>members : any (error)
>instanceType.ambientMembers : any (error)
>instanceType : any (error)
>ambientMembers : any (error)
>null : null
>null : null

                                context, instanceType, classType, null);
>context : any (error)
>instanceType : any (error)
>classType : any (error)
>null : null

        return true;
>true : true
    }

    export function preCollectInterfaceTypes(ast: AST, parent: AST, context: TypeCollectionContext) {
>preCollectInterfaceTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        var interfaceDecl = <InterfaceDeclaration>ast;
>interfaceDecl : any (error)
><InterfaceDeclaration>ast : any (error)
>ast : any (error)

        var interfaceSymbol: TypeSymbol = null;
>interfaceSymbol : any (error)
>null : null

        var interfaceType: Type = null;
>interfaceType : any (error)
>null : null

        var isExported = hasFlag(interfaceDecl.varFlags, VarFlags.Exported);
>isExported : any (error)
>hasFlag(interfaceDecl.varFlags, VarFlags.Exported) : any (error)
>hasFlag : any (error)
>interfaceDecl.varFlags : any (error)
>interfaceDecl : any (error)
>varFlags : any (error)
>VarFlags.Exported : any (error)
>VarFlags : any (error)
>Exported : any (error)

        var isGlobal = context.scopeChain.container == context.checker.gloMod;
>isGlobal : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

        var alreadyInScope = true;
>alreadyInScope : boolean
>true : true

        alreadyInScope = false;
>alreadyInScope = false : false
>alreadyInScope : boolean
>false : false

        var interfaceName = (<Identifier>interfaceDecl.name).text;
>interfaceName : any (error)
>(<Identifier>interfaceDecl.name).text : any (error)
>(<Identifier>interfaceDecl.name) : any (error)
><Identifier>interfaceDecl.name : any (error)
>interfaceDecl.name : any (error)
>interfaceDecl : any (error)
>name : any (error)
>text : any (error)

        interfaceSymbol = <TypeSymbol>scopeChain.scope.findLocal(interfaceName, false, true);
>interfaceSymbol = <TypeSymbol>scopeChain.scope.findLocal(interfaceName, false, true) : any (error)
>interfaceSymbol : any (error)
><TypeSymbol>scopeChain.scope.findLocal(interfaceName, false, true) : any (error)
>scopeChain.scope.findLocal(interfaceName, false, true) : any (error)
>scopeChain.scope.findLocal : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>findLocal : any (error)
>interfaceName : any (error)
>false : false
>true : true

        if (interfaceSymbol == null) {
>interfaceSymbol == null : boolean
>interfaceSymbol : any (error)
>null : null

            interfaceType = new Type();
>interfaceType = new Type() : any (error)
>interfaceType : any (error)
>new Type() : any (error)
>Type : any (error)

            interfaceSymbol = new TypeSymbol(interfaceName,
>interfaceSymbol = new TypeSymbol(interfaceName,                                        ast.minChar,                                        context.checker.locationInfo.unitIndex,                                        interfaceType) : any (error)
>interfaceSymbol : any (error)
>new TypeSymbol(interfaceName,                                        ast.minChar,                                        context.checker.locationInfo.unitIndex,                                        interfaceType) : any (error)
>TypeSymbol : any (error)
>interfaceName : any (error)

                                        ast.minChar,
>ast.minChar : any (error)
>ast : any (error)
>minChar : any (error)

                                        context.checker.locationInfo.unitIndex,
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)

                                        interfaceType);
>interfaceType : any (error)

            interfaceType.symbol = interfaceSymbol;
>interfaceType.symbol = interfaceSymbol : any (error)
>interfaceType.symbol : any (error)
>interfaceType : any (error)
>symbol : any (error)
>interfaceSymbol : any (error)

            // REVIEW: Shouldn't allocate another table for interface privates
            interfaceType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>interfaceType.members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>interfaceType.members : any (error)
>interfaceType : any (error)
>members : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            interfaceType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>interfaceType.ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>interfaceType.ambientMembers : any (error)
>interfaceType : any (error)
>ambientMembers : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            interfaceSymbol.declAST = interfaceDecl;
>interfaceSymbol.declAST = interfaceDecl : any (error)
>interfaceSymbol.declAST : any (error)
>interfaceSymbol : any (error)
>declAST : any (error)
>interfaceDecl : any (error)

            interfaceSymbol.declModule = context.scopeChain.moduleDecl;
>interfaceSymbol.declModule = context.scopeChain.moduleDecl : any (error)
>interfaceSymbol.declModule : any (error)
>interfaceSymbol : any (error)
>declModule : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
        }
        else {
            alreadyInScope = true;
>alreadyInScope = true : true
>alreadyInScope : boolean
>true : true

            interfaceType = interfaceSymbol.type;
>interfaceType = interfaceSymbol.type : any (error)
>interfaceType : any (error)
>interfaceSymbol.type : any (error)
>interfaceSymbol : any (error)
>type : any (error)
        }

        if (!interfaceType) {
>!interfaceType : boolean
>interfaceType : any (error)

            interfaceType = context.checker.anyType;
>interfaceType = context.checker.anyType : any (error)
>interfaceType : any (error)
>context.checker.anyType : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>anyType : any (error)
        }

        ast.type = interfaceType;
>ast.type = interfaceType : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)
>interfaceType : any (error)

        getBases(interfaceType, interfaceDecl);
>getBases(interfaceType, interfaceDecl) : void
>getBases : (type: any (error), typeDecl: any (error)) => void
>interfaceType : any (error)
>interfaceDecl : any (error)

        if (isExported) {
>isExported : any (error)

            interfaceSymbol.flags |= SymbolFlags.Exported;
>interfaceSymbol.flags |= SymbolFlags.Exported : number
>interfaceSymbol.flags : any (error)
>interfaceSymbol : any (error)
>flags : any (error)
>SymbolFlags.Exported : any (error)
>SymbolFlags : any (error)
>Exported : any (error)
        }

        if (context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

            interfaceSymbol.flags |= SymbolFlags.ModuleMember;
>interfaceSymbol.flags |= SymbolFlags.ModuleMember : number
>interfaceSymbol.flags : any (error)
>interfaceSymbol : any (error)
>flags : any (error)
>SymbolFlags.ModuleMember : any (error)
>SymbolFlags : any (error)
>ModuleMember : any (error)
        }

        if (!alreadyInScope) {
>!alreadyInScope : boolean
>alreadyInScope : boolean

            context.scopeChain.scope.enter(context.scopeChain.container, ast,
>context.scopeChain.scope.enter(context.scopeChain.container, ast,                                            interfaceSymbol, context.checker.errorReporter, isGlobal || isExported, true, false) : any (error)
>context.scopeChain.scope.enter : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)

                                            interfaceSymbol, context.checker.errorReporter, isGlobal || isExported, true, false); // REVIEW: Technically, interfaces should be ambient
>interfaceSymbol : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>isGlobal || isExported : any
>isGlobal : boolean
>isExported : any (error)
>true : true
>false : false
        }
        pushTypeCollectionScope(interfaceSymbol, interfaceType.members, interfaceType.ambientMembers, null, null,
>pushTypeCollectionScope(interfaceSymbol, interfaceType.members, interfaceType.ambientMembers, null, null,                                context, interfaceType, null, null) : any (error)
>pushTypeCollectionScope : any (error)
>interfaceSymbol : any (error)
>interfaceType.members : any (error)
>interfaceType : any (error)
>members : any (error)
>interfaceType.ambientMembers : any (error)
>interfaceType : any (error)
>ambientMembers : any (error)
>null : null
>null : null

                                context, interfaceType, null, null);
>context : any (error)
>interfaceType : any (error)
>null : null
>null : null

        return true;
>true : true
    }

    export function preCollectArgDeclTypes(ast: AST, parent: AST, context: TypeCollectionContext) {
>preCollectArgDeclTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        var argDecl = <ArgDecl>ast;
>argDecl : any (error)
><ArgDecl>ast : any (error)
>ast : any (error)

        if (hasFlag(argDecl.varFlags, VarFlags.Public | VarFlags.Private)) {
>hasFlag(argDecl.varFlags, VarFlags.Public | VarFlags.Private) : any (error)
>hasFlag : any (error)
>argDecl.varFlags : any (error)
>argDecl : any (error)
>varFlags : any (error)
>VarFlags.Public | VarFlags.Private : number
>VarFlags.Public : any (error)
>VarFlags : any (error)
>Public : any (error)
>VarFlags.Private : any (error)
>VarFlags : any (error)
>Private : any (error)

            var field = new ValueLocation();
>field : any (error)
>new ValueLocation() : any (error)
>ValueLocation : any (error)

            var isPrivate = hasFlag(argDecl.varFlags, VarFlags.Private);
>isPrivate : any (error)
>hasFlag(argDecl.varFlags, VarFlags.Private) : any (error)
>hasFlag : any (error)
>argDecl.varFlags : any (error)
>argDecl : any (error)
>varFlags : any (error)
>VarFlags.Private : any (error)
>VarFlags : any (error)
>Private : any (error)

            var fieldSymbol =
>fieldSymbol : any (error)

                new FieldSymbol(argDecl.id.text, argDecl.minChar,
>new FieldSymbol(argDecl.id.text, argDecl.minChar,                                context.checker.locationInfo.unitIndex,                                !hasFlag(argDecl.varFlags, VarFlags.Readonly),                                field) : any (error)
>FieldSymbol : any (error)
>argDecl.id.text : any (error)
>argDecl.id : any (error)
>argDecl : any (error)
>id : any (error)
>text : any (error)
>argDecl.minChar : any (error)
>argDecl : any (error)
>minChar : any (error)

                                context.checker.locationInfo.unitIndex,
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)

                                !hasFlag(argDecl.varFlags, VarFlags.Readonly),
>!hasFlag(argDecl.varFlags, VarFlags.Readonly) : boolean
>hasFlag(argDecl.varFlags, VarFlags.Readonly) : any (error)
>hasFlag : any (error)
>argDecl.varFlags : any (error)
>argDecl : any (error)
>varFlags : any (error)
>VarFlags.Readonly : any (error)
>VarFlags : any (error)
>Readonly : any (error)

                                field);
>field : any (error)

            fieldSymbol.transferVarFlags(argDecl.varFlags);
>fieldSymbol.transferVarFlags(argDecl.varFlags) : any (error)
>fieldSymbol.transferVarFlags : any (error)
>fieldSymbol : any (error)
>transferVarFlags : any (error)
>argDecl.varFlags : any (error)
>argDecl : any (error)
>varFlags : any (error)

            field.symbol = fieldSymbol;
>field.symbol = fieldSymbol : any (error)
>field.symbol : any (error)
>field : any (error)
>symbol : any (error)
>fieldSymbol : any (error)

            fieldSymbol.declAST = ast;
>fieldSymbol.declAST = ast : any (error)
>fieldSymbol.declAST : any (error)
>fieldSymbol : any (error)
>declAST : any (error)
>ast : any (error)

            argDecl.parameterPropertySym = fieldSymbol;
>argDecl.parameterPropertySym = fieldSymbol : any (error)
>argDecl.parameterPropertySym : any (error)
>argDecl : any (error)
>parameterPropertySym : any (error)
>fieldSymbol : any (error)

            context.scopeChain.scope.enter(context.scopeChain.container, ast,
>context.scopeChain.scope.enter(context.scopeChain.container, ast,                                            fieldSymbol, context.checker.errorReporter, !isPrivate, false, false) : any (error)
>context.scopeChain.scope.enter : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>ast : any (error)

                                            fieldSymbol, context.checker.errorReporter, !isPrivate, false, false);
>fieldSymbol : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>!isPrivate : boolean
>isPrivate : any (error)
>false : false
>false : false

            field.typeLink = getTypeLink(argDecl.typeExpr, context.checker, argDecl.init == null);
>field.typeLink = getTypeLink(argDecl.typeExpr, context.checker, argDecl.init == null) : any (error)
>field.typeLink : any (error)
>field : any (error)
>typeLink : any (error)
>getTypeLink(argDecl.typeExpr, context.checker, argDecl.init == null) : any (error)
>getTypeLink : any (error)
>argDecl.typeExpr : any (error)
>argDecl : any (error)
>typeExpr : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>argDecl.init == null : boolean
>argDecl.init : any (error)
>argDecl : any (error)
>init : any (error)
>null : null

            argDecl.sym = fieldSymbol;
>argDecl.sym = fieldSymbol : any (error)
>argDecl.sym : any (error)
>argDecl : any (error)
>sym : any (error)
>fieldSymbol : any (error)
        }
        return false;
>false : false
    }

    export function preCollectVarDeclTypes(ast: AST, parent: AST, context: TypeCollectionContext) {
>preCollectVarDeclTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        var varDecl = <VarDecl>ast;
>varDecl : any (error)
><VarDecl>ast : any (error)
>ast : any (error)

        var isAmbient = hasFlag(varDecl.varFlags, VarFlags.Ambient);
>isAmbient : any (error)
>hasFlag(varDecl.varFlags, VarFlags.Ambient) : any (error)
>hasFlag : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Ambient : any (error)
>VarFlags : any (error)
>Ambient : any (error)

        var isExported = hasFlag(varDecl.varFlags, VarFlags.Exported);
>isExported : any (error)
>hasFlag(varDecl.varFlags, VarFlags.Exported) : any (error)
>hasFlag : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Exported : any (error)
>VarFlags : any (error)
>Exported : any (error)

        var isGlobal = context.scopeChain.container == context.checker.gloMod;
>isGlobal : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

        var isProperty = hasFlag(varDecl.varFlags, VarFlags.Property);
>isProperty : any (error)
>hasFlag(varDecl.varFlags, VarFlags.Property) : any (error)
>hasFlag : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Property : any (error)
>VarFlags : any (error)
>Property : any (error)

        var isStatic = hasFlag(varDecl.varFlags, VarFlags.Static);
>isStatic : any (error)
>hasFlag(varDecl.varFlags, VarFlags.Static) : any (error)
>hasFlag : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Static : any (error)
>VarFlags : any (error)
>Static : any (error)

        var isPrivate = hasFlag(varDecl.varFlags, VarFlags.Private);
>isPrivate : any (error)
>hasFlag(varDecl.varFlags, VarFlags.Private) : any (error)
>hasFlag : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Private : any (error)
>VarFlags : any (error)
>Private : any (error)

        var isOptional = hasFlag(varDecl.id.flags, ASTFlags.OptionalName);
>isOptional : any (error)
>hasFlag(varDecl.id.flags, ASTFlags.OptionalName) : any (error)
>hasFlag : any (error)
>varDecl.id.flags : any (error)
>varDecl.id : any (error)
>varDecl : any (error)
>id : any (error)
>flags : any (error)
>ASTFlags.OptionalName : any (error)
>ASTFlags : any (error)
>OptionalName : any (error)

        if (context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

            context.scopeChain.moduleDecl.recordNonInterface();
>context.scopeChain.moduleDecl.recordNonInterface() : any (error)
>context.scopeChain.moduleDecl.recordNonInterface : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
>recordNonInterface : any (error)
        }
        if (isProperty ||
>isProperty ||            isExported ||            (context.scopeChain.container == context.checker.gloMod) ||            context.scopeChain.moduleDecl : any
>isProperty ||            isExported ||            (context.scopeChain.container == context.checker.gloMod) : any
>isProperty ||            isExported : any
>isProperty : any (error)

            isExported ||
>isExported : any (error)

            (context.scopeChain.container == context.checker.gloMod) ||
>(context.scopeChain.container == context.checker.gloMod) : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

            context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

            if (isAmbient) {
>isAmbient : any (error)

                var existingSym =
>existingSym : any (error)

                    <FieldSymbol>scopeChain.scope.findLocal(varDecl.id.text, false, false);
><FieldSymbol>scopeChain.scope.findLocal(varDecl.id.text, false, false) : any (error)
>scopeChain.scope.findLocal(varDecl.id.text, false, false) : any (error)
>scopeChain.scope.findLocal : any (error)
>scopeChain.scope : any (error)
>scopeChain : any (error)
>scope : any (error)
>findLocal : any (error)
>varDecl.id.text : any (error)
>varDecl.id : any (error)
>varDecl : any (error)
>id : any (error)
>text : any (error)
>false : false
>false : false

                if (existingSym) {
>existingSym : any (error)

                    varDecl.sym = existingSym;
>varDecl.sym = existingSym : any (error)
>varDecl.sym : any (error)
>varDecl : any (error)
>sym : any (error)
>existingSym : any (error)

                    return false;
>false : false
                }
            }

            // Defensive error detection...
            if (varDecl.id == null) {
>varDecl.id == null : boolean
>varDecl.id : any (error)
>varDecl : any (error)
>id : any (error)
>null : null

                context.checker.errorReporter.simpleError(varDecl, "Expected variable identifier at this location");
>context.checker.errorReporter.simpleError(varDecl, "Expected variable identifier at this location") : any (error)
>context.checker.errorReporter.simpleError : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>simpleError : any (error)
>varDecl : any (error)
>"Expected variable identifier at this location" : "Expected variable identifier at this location"

                return false;
>false : false
            }

            var field = new ValueLocation();
>field : any (error)
>new ValueLocation() : any (error)
>ValueLocation : any (error)

            var fieldSymbol =
>fieldSymbol : any (error)

                new FieldSymbol(varDecl.id.text, varDecl.minChar,
>new FieldSymbol(varDecl.id.text, varDecl.minChar,                                context.checker.locationInfo.unitIndex,                                (varDecl.varFlags & VarFlags.Readonly) == VarFlags.None,                                field) : any (error)
>FieldSymbol : any (error)
>varDecl.id.text : any (error)
>varDecl.id : any (error)
>varDecl : any (error)
>id : any (error)
>text : any (error)
>varDecl.minChar : any (error)
>varDecl : any (error)
>minChar : any (error)

                                context.checker.locationInfo.unitIndex,
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)

                                (varDecl.varFlags & VarFlags.Readonly) == VarFlags.None,
>(varDecl.varFlags & VarFlags.Readonly) == VarFlags.None : boolean
>(varDecl.varFlags & VarFlags.Readonly) : number
>varDecl.varFlags & VarFlags.Readonly : number
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Readonly : any (error)
>VarFlags : any (error)
>Readonly : any (error)
>VarFlags.None : any (error)
>VarFlags : any (error)
>None : any (error)

                                field);
>field : any (error)

            fieldSymbol.transferVarFlags(varDecl.varFlags);
>fieldSymbol.transferVarFlags(varDecl.varFlags) : any (error)
>fieldSymbol.transferVarFlags : any (error)
>fieldSymbol : any (error)
>transferVarFlags : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)

            if (isOptional) {
>isOptional : any (error)

                fieldSymbol.flags |= SymbolFlags.Optional;
>fieldSymbol.flags |= SymbolFlags.Optional : number
>fieldSymbol.flags : any (error)
>fieldSymbol : any (error)
>flags : any (error)
>SymbolFlags.Optional : any (error)
>SymbolFlags : any (error)
>Optional : any (error)
            }
            field.symbol = fieldSymbol;
>field.symbol = fieldSymbol : any (error)
>field.symbol : any (error)
>field : any (error)
>symbol : any (error)
>fieldSymbol : any (error)

            fieldSymbol.declAST = ast;
>fieldSymbol.declAST = ast : any (error)
>fieldSymbol.declAST : any (error)
>fieldSymbol : any (error)
>declAST : any (error)
>ast : any (error)

            if ((context.scopeChain.moduleDecl) ||
>(context.scopeChain.moduleDecl) ||                (context.scopeChain.container == context.checker.gloMod) : any
>(context.scopeChain.moduleDecl) : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

                (context.scopeChain.container == context.checker.gloMod)) {
>(context.scopeChain.container == context.checker.gloMod) : boolean
>context.scopeChain.container == context.checker.gloMod : boolean
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

                fieldSymbol.flags |= SymbolFlags.ModuleMember;
>fieldSymbol.flags |= SymbolFlags.ModuleMember : number
>fieldSymbol.flags : any (error)
>fieldSymbol : any (error)
>flags : any (error)
>SymbolFlags.ModuleMember : any (error)
>SymbolFlags : any (error)
>ModuleMember : any (error)

                fieldSymbol.declModule = context.scopeChain.moduleDecl;
>fieldSymbol.declModule = context.scopeChain.moduleDecl : any (error)
>fieldSymbol.declModule : any (error)
>fieldSymbol : any (error)
>declModule : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
            }

            // if it's static, enter it into the class's member list directly
            if (hasFlag(varDecl.varFlags, VarFlags.Property) && isStatic && context.scopeChain.classType) {
>hasFlag(varDecl.varFlags, VarFlags.Property) && isStatic && context.scopeChain.classType : any
>hasFlag(varDecl.varFlags, VarFlags.Property) && isStatic : any
>hasFlag(varDecl.varFlags, VarFlags.Property) : any (error)
>hasFlag : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Property : any (error)
>VarFlags : any (error)
>Property : any (error)
>isStatic : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>classType : any (error)

                if (!context.scopeChain.classType.members.publicMembers.add(varDecl.id.text, fieldSymbol)) {
>!context.scopeChain.classType.members.publicMembers.add(varDecl.id.text, fieldSymbol) : boolean
>context.scopeChain.classType.members.publicMembers.add(varDecl.id.text, fieldSymbol) : any (error)
>context.scopeChain.classType.members.publicMembers.add : any (error)
>context.scopeChain.classType.members.publicMembers : any (error)
>context.scopeChain.classType.members : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>classType : any (error)
>members : any (error)
>publicMembers : any (error)
>add : any (error)
>varDecl.id.text : any (error)
>varDecl.id : any (error)
>varDecl : any (error)
>id : any (error)
>text : any (error)
>fieldSymbol : any (error)

                    context.checker.errorReporter.duplicateIdentifier(ast, fieldSymbol.name);
>context.checker.errorReporter.duplicateIdentifier(ast, fieldSymbol.name) : any (error)
>context.checker.errorReporter.duplicateIdentifier : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>duplicateIdentifier : any (error)
>ast : any (error)
>fieldSymbol.name : any (error)
>fieldSymbol : any (error)
>name : any (error)
                }
                fieldSymbol.container = context.scopeChain.classType.symbol;
>fieldSymbol.container = context.scopeChain.classType.symbol : any (error)
>fieldSymbol.container : any (error)
>fieldSymbol : any (error)
>container : any (error)
>context.scopeChain.classType.symbol : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>classType : any (error)
>symbol : any (error)
            }
            else {
                context.scopeChain.scope.enter(context.scopeChain.container,
>context.scopeChain.scope.enter(context.scopeChain.container,                                                ast,                                                fieldSymbol,                                                context.checker.errorReporter,                                                !isPrivate && (isProperty || isExported || isGlobal || isStatic),                                                false,                                                isAmbient) : any (error)
>context.scopeChain.scope.enter : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>scope : any (error)
>enter : any (error)
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)

                                                ast,
>ast : any (error)

                                                fieldSymbol,
>fieldSymbol : any (error)

                                                context.checker.errorReporter,
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)

                                                !isPrivate && (isProperty || isExported || isGlobal || isStatic),
>!isPrivate && (isProperty || isExported || isGlobal || isStatic) : any
>!isPrivate : boolean
>isPrivate : any (error)
>(isProperty || isExported || isGlobal || isStatic) : any
>isProperty || isExported || isGlobal || isStatic : any
>isProperty || isExported || isGlobal : any
>isProperty || isExported : any
>isProperty : any (error)
>isExported : any (error)
>isGlobal : boolean
>isStatic : any (error)

                                                false,
>false : false

                                                isAmbient);
>isAmbient : any (error)
            }

            if (hasFlag(varDecl.varFlags, VarFlags.Exported)) {
>hasFlag(varDecl.varFlags, VarFlags.Exported) : any (error)
>hasFlag : any (error)
>varDecl.varFlags : any (error)
>varDecl : any (error)
>varFlags : any (error)
>VarFlags.Exported : any (error)
>VarFlags : any (error)
>Exported : any (error)

                fieldSymbol.flags |= SymbolFlags.Exported;
>fieldSymbol.flags |= SymbolFlags.Exported : number
>fieldSymbol.flags : any (error)
>fieldSymbol : any (error)
>flags : any (error)
>SymbolFlags.Exported : any (error)
>SymbolFlags : any (error)
>Exported : any (error)
            }

            field.typeLink = getTypeLink(varDecl.typeExpr, context.checker,
>field.typeLink = getTypeLink(varDecl.typeExpr, context.checker,                                        varDecl.init == null) : any (error)
>field.typeLink : any (error)
>field : any (error)
>typeLink : any (error)
>getTypeLink(varDecl.typeExpr, context.checker,                                        varDecl.init == null) : any (error)
>getTypeLink : any (error)
>varDecl.typeExpr : any (error)
>varDecl : any (error)
>typeExpr : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)

                                        varDecl.init == null);
>varDecl.init == null : boolean
>varDecl.init : any (error)
>varDecl : any (error)
>init : any (error)
>null : null

            varDecl.sym = fieldSymbol;
>varDecl.sym = fieldSymbol : any (error)
>varDecl.sym : any (error)
>varDecl : any (error)
>sym : any (error)
>fieldSymbol : any (error)
        }
        return false;
>false : false
    }

    export function preCollectFuncDeclTypes(ast: AST, parent: AST, context: TypeCollectionContext) {
>preCollectFuncDeclTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        // REVIEW: This will have to change when we move to "export"
        if (context.scopeChain.moduleDecl) {
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

            context.scopeChain.moduleDecl.recordNonInterface();
>context.scopeChain.moduleDecl.recordNonInterface() : any (error)
>context.scopeChain.moduleDecl.recordNonInterface : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
>recordNonInterface : any (error)
        }

        var funcDecl = <FuncDecl>ast;
>funcDecl : any (error)
><FuncDecl>ast : any (error)
>ast : any (error)

        var fgSym: TypeSymbol = null;
>fgSym : any (error)
>null : null

        var nameText = funcDecl.getNameText();
>nameText : any (error)
>funcDecl.getNameText() : any (error)
>funcDecl.getNameText : any (error)
>funcDecl : any (error)
>getNameText : any (error)

        var isExported = hasFlag(funcDecl.fncFlags, FncFlags.Exported | FncFlags.ClassPropertyMethodExported);
>isExported : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.Exported | FncFlags.ClassPropertyMethodExported) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.Exported | FncFlags.ClassPropertyMethodExported : number
>FncFlags.Exported : any (error)
>FncFlags : any (error)
>Exported : any (error)
>FncFlags.ClassPropertyMethodExported : any (error)
>FncFlags : any (error)
>ClassPropertyMethodExported : any (error)

        var isStatic = hasFlag(funcDecl.fncFlags, FncFlags.Static);
>isStatic : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.Static) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.Static : any (error)
>FncFlags : any (error)
>Static : any (error)

        var isPrivate = hasFlag(funcDecl.fncFlags, FncFlags.Private);
>isPrivate : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.Private) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.Private : any (error)
>FncFlags : any (error)
>Private : any (error)

        var isConstructor = funcDecl.isConstructMember() || funcDecl.isConstructor;
>isConstructor : any
>funcDecl.isConstructMember() || funcDecl.isConstructor : any
>funcDecl.isConstructMember() : any (error)
>funcDecl.isConstructMember : any (error)
>funcDecl : any (error)
>isConstructMember : any (error)
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)

        var containerSym:TypeSymbol = <TypeSymbol> (((funcDecl.isMethod() && isStatic) || funcDecl.isAccessor()) && context.scopeChain.classType ? context.scopeChain.classType.symbol : context.scopeChain.container);
>containerSym : any (error)
><TypeSymbol> (((funcDecl.isMethod() && isStatic) || funcDecl.isAccessor()) && context.scopeChain.classType ? context.scopeChain.classType.symbol : context.scopeChain.container) : any (error)
>(((funcDecl.isMethod() && isStatic) || funcDecl.isAccessor()) && context.scopeChain.classType ? context.scopeChain.classType.symbol : context.scopeChain.container) : any
>((funcDecl.isMethod() && isStatic) || funcDecl.isAccessor()) && context.scopeChain.classType ? context.scopeChain.classType.symbol : context.scopeChain.container : any
>((funcDecl.isMethod() && isStatic) || funcDecl.isAccessor()) && context.scopeChain.classType : any
>((funcDecl.isMethod() && isStatic) || funcDecl.isAccessor()) : any
>(funcDecl.isMethod() && isStatic) || funcDecl.isAccessor() : any
>(funcDecl.isMethod() && isStatic) : any
>funcDecl.isMethod() && isStatic : any
>funcDecl.isMethod() : any (error)
>funcDecl.isMethod : any (error)
>funcDecl : any (error)
>isMethod : any (error)
>isStatic : any (error)
>funcDecl.isAccessor() : any (error)
>funcDecl.isAccessor : any (error)
>funcDecl : any (error)
>isAccessor : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>classType : any (error)
>context.scopeChain.classType.symbol : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>classType : any (error)
>symbol : any (error)
>context.scopeChain.container : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>container : any (error)

        var containerScope: SymbolScope = context.scopeChain.scope;
>containerScope : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>scope : any (error)

        var isGlobal = containerSym == context.checker.gloMod;
>isGlobal : boolean
>containerSym == context.checker.gloMod : boolean
>containerSym : any (error)
>context.checker.gloMod : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>gloMod : any (error)

        var isOptional = funcDecl.name && hasFlag(funcDecl.name.flags, ASTFlags.OptionalName);
>isOptional : any
>funcDecl.name && hasFlag(funcDecl.name.flags, ASTFlags.OptionalName) : any
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>hasFlag(funcDecl.name.flags, ASTFlags.OptionalName) : any (error)
>hasFlag : any (error)
>funcDecl.name.flags : any (error)
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>flags : any (error)
>ASTFlags.OptionalName : any (error)
>ASTFlags : any (error)
>OptionalName : any (error)

        var go = false;
>go : boolean
>false : false

        var foundSymbol = false; 
>foundSymbol : boolean
>false : false

        // If this is a class constructor, the "container" is actually the class declaration
        if (isConstructor && hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) {
>isConstructor && hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any
>isConstructor : any
>hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.ClassMethod : any (error)
>FncFlags : any (error)
>ClassMethod : any (error)

            containerSym = <TypeSymbol>containerSym.container;
>containerSym = <TypeSymbol>containerSym.container : any (error)
>containerSym : any (error)
><TypeSymbol>containerSym.container : any (error)
>containerSym.container : any (error)
>containerSym : any (error)
>container : any (error)

            containerScope = scopeChain.previous.scope;
>containerScope = scopeChain.previous.scope : any (error)
>containerScope : any (error)
>scopeChain.previous.scope : any (error)
>scopeChain.previous : any (error)
>scopeChain : any (error)
>previous : any (error)
>scope : any (error)
        }

        funcDecl.unitIndex = context.checker.locationInfo.unitIndex;
>funcDecl.unitIndex = context.checker.locationInfo.unitIndex : any (error)
>funcDecl.unitIndex : any (error)
>funcDecl : any (error)
>unitIndex : any (error)
>context.checker.locationInfo.unitIndex : any (error)
>context.checker.locationInfo : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)
        
        // If the parent is the constructor, and this isn't an instance method, skip it.
        // That way, we'll set the type during scope assignment, and can be sure that the
        // function will be placed in the constructor-local scope
        if (!funcDecl.isConstructor &&
>!funcDecl.isConstructor &&            containerSym &&            containerSym.declAST &&            containerSym.declAST.nodeType == NodeType.FuncDecl &&            (<FuncDecl>containerSym.declAST).isConstructor &&            !funcDecl.isMethod() : boolean
>!funcDecl.isConstructor &&            containerSym &&            containerSym.declAST &&            containerSym.declAST.nodeType == NodeType.FuncDecl &&            (<FuncDecl>containerSym.declAST).isConstructor : any
>!funcDecl.isConstructor &&            containerSym &&            containerSym.declAST &&            containerSym.declAST.nodeType == NodeType.FuncDecl : boolean
>!funcDecl.isConstructor &&            containerSym &&            containerSym.declAST : any
>!funcDecl.isConstructor &&            containerSym : any
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)

            containerSym &&
>containerSym : any (error)

            containerSym.declAST &&
>containerSym.declAST : any (error)
>containerSym : any (error)
>declAST : any (error)

            containerSym.declAST.nodeType == NodeType.FuncDecl &&
>containerSym.declAST.nodeType == NodeType.FuncDecl : boolean
>containerSym.declAST.nodeType : any (error)
>containerSym.declAST : any (error)
>containerSym : any (error)
>declAST : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)

            (<FuncDecl>containerSym.declAST).isConstructor &&
>(<FuncDecl>containerSym.declAST).isConstructor : any (error)
>(<FuncDecl>containerSym.declAST) : any (error)
><FuncDecl>containerSym.declAST : any (error)
>containerSym.declAST : any (error)
>containerSym : any (error)
>declAST : any (error)
>isConstructor : any (error)

            !funcDecl.isMethod()) {
>!funcDecl.isMethod() : boolean
>funcDecl.isMethod() : any (error)
>funcDecl.isMethod : any (error)
>funcDecl : any (error)
>isMethod : any (error)

            return go;
>go : false
        }        

        // Interfaces and overloads
        if (hasFlag(funcDecl.fncFlags, FncFlags.Signature)) {
>hasFlag(funcDecl.fncFlags, FncFlags.Signature) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.Signature : any (error)
>FncFlags : any (error)
>Signature : any (error)

            var instType = context.scopeChain.thisType;                       
>instType : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>thisType : any (error)

            // If the function is static, search in the class type's
            if (nameText && nameText != "__missing") {
>nameText && nameText != "__missing" : boolean
>nameText : any (error)
>nameText != "__missing" : boolean
>nameText : any (error)
>"__missing" : "__missing"

                if (isStatic) {
>isStatic : any (error)

                    fgSym = containerSym.type.members.allMembers.lookup(nameText);
>fgSym = containerSym.type.members.allMembers.lookup(nameText) : any (error)
>fgSym : any (error)
>containerSym.type.members.allMembers.lookup(nameText) : any (error)
>containerSym.type.members.allMembers.lookup : any (error)
>containerSym.type.members.allMembers : any (error)
>containerSym.type.members : any (error)
>containerSym.type : any (error)
>containerSym : any (error)
>type : any (error)
>members : any (error)
>allMembers : any (error)
>lookup : any (error)
>nameText : any (error)
                }
                else {
                    // REVIEW: This logic should be symmetric with preCollectClassTypes
                    fgSym = <TypeSymbol>containerScope.findLocal(nameText, false, false);
>fgSym = <TypeSymbol>containerScope.findLocal(nameText, false, false) : any (error)
>fgSym : any (error)
><TypeSymbol>containerScope.findLocal(nameText, false, false) : any (error)
>containerScope.findLocal(nameText, false, false) : any (error)
>containerScope.findLocal : any (error)
>containerScope : any (error)
>findLocal : any (error)
>nameText : any (error)
>false : false
>false : false
                    
                    // If we could not find the function symbol in the value context, look
                    // in the type context.
                    // This would be the case, for example, if a class constructor override
                    // were declared before a call override for a given class
                    if (fgSym == null) {
>fgSym == null : boolean
>fgSym : any (error)
>null : null

                        fgSym = <TypeSymbol>containerScope.findLocal(nameText, false, true);
>fgSym = <TypeSymbol>containerScope.findLocal(nameText, false, true) : any (error)
>fgSym : any (error)
><TypeSymbol>containerScope.findLocal(nameText, false, true) : any (error)
>containerScope.findLocal(nameText, false, true) : any (error)
>containerScope.findLocal : any (error)
>containerScope : any (error)
>findLocal : any (error)
>nameText : any (error)
>false : false
>true : true
                    }
                }
                
                if (fgSym) {
>fgSym : any (error)

                    foundSymbol = true;
>foundSymbol = true : true
>foundSymbol : boolean
>true : true
                    
                    // We'll combine ambient and non-ambient funcdecls during typecheck (for contextual typing).,
                    // So, if they don't agree, don't use the symbol we've found                    
                    if (!funcDecl.isSignature() && (hasFlag(funcDecl.fncFlags, FncFlags.Ambient) != hasFlag(fgSym.flags, SymbolFlags.Ambient))) {
>!funcDecl.isSignature() && (hasFlag(funcDecl.fncFlags, FncFlags.Ambient) != hasFlag(fgSym.flags, SymbolFlags.Ambient)) : boolean
>!funcDecl.isSignature() : boolean
>funcDecl.isSignature() : any (error)
>funcDecl.isSignature : any (error)
>funcDecl : any (error)
>isSignature : any (error)
>(hasFlag(funcDecl.fncFlags, FncFlags.Ambient) != hasFlag(fgSym.flags, SymbolFlags.Ambient)) : boolean
>hasFlag(funcDecl.fncFlags, FncFlags.Ambient) != hasFlag(fgSym.flags, SymbolFlags.Ambient) : boolean
>hasFlag(funcDecl.fncFlags, FncFlags.Ambient) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.Ambient : any (error)
>FncFlags : any (error)
>Ambient : any (error)
>hasFlag(fgSym.flags, SymbolFlags.Ambient) : any (error)
>hasFlag : any (error)
>fgSym.flags : any (error)
>fgSym : any (error)
>flags : any (error)
>SymbolFlags.Ambient : any (error)
>SymbolFlags : any (error)
>Ambient : any (error)

                       fgSym = null;
>fgSym = null : null
>fgSym : any (error)
>null : null
                    }
                }                
            }
            
            // a function with this symbol has not yet been declared in this scope
            // REVIEW: In the code below, we need to ensure that only function overloads are considered
            //  (E.g., if a vardecl has the same id as a function or class, we may use the vardecl symbol
            //  as the overload.)  Defensively, however, the vardecl won't have a type yet, so it should
            //  suffice to just check for a null type when considering the overload symbol in
            //  createFunctionSignature
            if (fgSym == null) {
>fgSym == null : boolean
>fgSym : any (error)
>null : null

                if (!(funcDecl.isSpecialFn())) {                    
>!(funcDecl.isSpecialFn()) : boolean
>(funcDecl.isSpecialFn()) : any (error)
>funcDecl.isSpecialFn() : any (error)
>funcDecl.isSpecialFn : any (error)
>funcDecl : any (error)
>isSpecialFn : any (error)

                    fgSym = context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, null, !foundSymbol).declAST.type.symbol;
>fgSym = context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, null, !foundSymbol).declAST.type.symbol : any (error)
>fgSym : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, null, !foundSymbol).declAST.type.symbol : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, null, !foundSymbol).declAST.type : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, null, !foundSymbol).declAST : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, null, !foundSymbol) : any (error)
>context.checker.createFunctionSignature : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>createFunctionSignature : any (error)
>funcDecl : any (error)
>containerSym : any (error)
>containerScope : any (error)
>null : null
>!foundSymbol : boolean
>foundSymbol : boolean
>declAST : any (error)
>type : any (error)
>symbol : any (error)
                }
                else {
                    fgSym = context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, containerSym, false).declAST.type.symbol;                                                                         
>fgSym = context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, containerSym, false).declAST.type.symbol : any (error)
>fgSym : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, containerSym, false).declAST.type.symbol : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, containerSym, false).declAST.type : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, containerSym, false).declAST : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, containerSym, false) : any (error)
>context.checker.createFunctionSignature : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>createFunctionSignature : any (error)
>funcDecl : any (error)
>containerSym : any (error)
>containerScope : any (error)
>containerSym : any (error)
>false : false
>declAST : any (error)
>type : any (error)
>symbol : any (error)
                }
                
                // set the symbol's declAST, which will point back to the first declaration (symbol or otherwise)
                // related to this symbol
                if (fgSym.declAST == null || !funcDecl.isSpecialFn()) {
>fgSym.declAST == null || !funcDecl.isSpecialFn() : boolean
>fgSym.declAST == null : boolean
>fgSym.declAST : any (error)
>fgSym : any (error)
>declAST : any (error)
>null : null
>!funcDecl.isSpecialFn() : boolean
>funcDecl.isSpecialFn() : any (error)
>funcDecl.isSpecialFn : any (error)
>funcDecl : any (error)
>isSpecialFn : any (error)

                    fgSym.declAST = ast;
>fgSym.declAST = ast : any (error)
>fgSym.declAST : any (error)
>fgSym : any (error)
>declAST : any (error)
>ast : any (error)
                }
            }
            else { // there exists a symbol with this name
                
                if ((fgSym.kind() == SymbolKind.Type)) {
>(fgSym.kind() == SymbolKind.Type) : boolean
>fgSym.kind() == SymbolKind.Type : boolean
>fgSym.kind() : any (error)
>fgSym.kind : any (error)
>fgSym : any (error)
>kind : any (error)
>SymbolKind.Type : any (error)
>SymbolKind : any (error)
>Type : any (error)

                    fgSym = context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, false).declAST.type.symbol;
>fgSym = context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, false).declAST.type.symbol : any (error)
>fgSym : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, false).declAST.type.symbol : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, false).declAST.type : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, false).declAST : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, false) : any (error)
>context.checker.createFunctionSignature : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>createFunctionSignature : any (error)
>funcDecl : any (error)
>containerSym : any (error)
>containerScope : any (error)
>fgSym : any (error)
>false : false
>declAST : any (error)
>type : any (error)
>symbol : any (error)
                }
                else {
                    context.checker.errorReporter.simpleError(funcDecl, "Function or method '" + funcDecl.name.actualText + "' already declared as a property");
>context.checker.errorReporter.simpleError(funcDecl, "Function or method '" + funcDecl.name.actualText + "' already declared as a property") : any (error)
>context.checker.errorReporter.simpleError : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>simpleError : any (error)
>funcDecl : any (error)
>"Function or method '" + funcDecl.name.actualText + "' already declared as a property" : string
>"Function or method '" + funcDecl.name.actualText : string
>"Function or method '" : "Function or method '"
>funcDecl.name.actualText : any (error)
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>actualText : any (error)
>"' already declared as a property" : "' already declared as a property"
                }
            }
         
            if (funcDecl.isSpecialFn() && !isStatic) {
>funcDecl.isSpecialFn() && !isStatic : boolean
>funcDecl.isSpecialFn() : any (error)
>funcDecl.isSpecialFn : any (error)
>funcDecl : any (error)
>isSpecialFn : any (error)
>!isStatic : boolean
>isStatic : any (error)

                funcDecl.type = instType ? instType : fgSym.type; 
>funcDecl.type = instType ? instType : fgSym.type : any
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>instType ? instType : fgSym.type : any
>instType : any (error)
>instType : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
            }
            else {
                funcDecl.type = fgSym.type;
>funcDecl.type = fgSym.type : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
            }            
        }
        else {
            // declarations
            
            if (nameText) {
>nameText : any (error)

                if (isStatic) {
>isStatic : any (error)

                    fgSym = containerSym.type.members.allMembers.lookup(nameText);
>fgSym = containerSym.type.members.allMembers.lookup(nameText) : any (error)
>fgSym : any (error)
>containerSym.type.members.allMembers.lookup(nameText) : any (error)
>containerSym.type.members.allMembers.lookup : any (error)
>containerSym.type.members.allMembers : any (error)
>containerSym.type.members : any (error)
>containerSym.type : any (error)
>containerSym : any (error)
>type : any (error)
>members : any (error)
>allMembers : any (error)
>lookup : any (error)
>nameText : any (error)
                }
                else {
                    // in the constructor case, we want to check the parent scope for overloads
                    if (funcDecl.isConstructor && context.scopeChain.previous) {
>funcDecl.isConstructor && context.scopeChain.previous : any
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)
>context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>previous : any (error)

                        fgSym = <TypeSymbol>context.scopeChain.previous.scope.findLocal(nameText, false, false);
>fgSym = <TypeSymbol>context.scopeChain.previous.scope.findLocal(nameText, false, false) : any (error)
>fgSym : any (error)
><TypeSymbol>context.scopeChain.previous.scope.findLocal(nameText, false, false) : any (error)
>context.scopeChain.previous.scope.findLocal(nameText, false, false) : any (error)
>context.scopeChain.previous.scope.findLocal : any (error)
>context.scopeChain.previous.scope : any (error)
>context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>previous : any (error)
>scope : any (error)
>findLocal : any (error)
>nameText : any (error)
>false : false
>false : false
                    }
                    
                    if (fgSym == null) {
>fgSym == null : boolean
>fgSym : any (error)
>null : null

                        fgSym = <TypeSymbol>containerScope.findLocal(nameText, false, false);
>fgSym = <TypeSymbol>containerScope.findLocal(nameText, false, false) : any (error)
>fgSym : any (error)
><TypeSymbol>containerScope.findLocal(nameText, false, false) : any (error)
>containerScope.findLocal(nameText, false, false) : any (error)
>containerScope.findLocal : any (error)
>containerScope : any (error)
>findLocal : any (error)
>nameText : any (error)
>false : false
>false : false
                    }
                }
                if (fgSym) {
>fgSym : any (error)

                    foundSymbol = true;
>foundSymbol = true : true
>foundSymbol : boolean
>true : true
                    
                    if (!isConstructor && fgSym.declAST.nodeType == NodeType.FuncDecl && !(<FuncDecl>fgSym.declAST).isAccessor() && !(<FuncDecl>fgSym.declAST).isSignature()) {
>!isConstructor && fgSym.declAST.nodeType == NodeType.FuncDecl && !(<FuncDecl>fgSym.declAST).isAccessor() && !(<FuncDecl>fgSym.declAST).isSignature() : boolean
>!isConstructor && fgSym.declAST.nodeType == NodeType.FuncDecl && !(<FuncDecl>fgSym.declAST).isAccessor() : boolean
>!isConstructor && fgSym.declAST.nodeType == NodeType.FuncDecl : boolean
>!isConstructor : boolean
>isConstructor : any
>fgSym.declAST.nodeType == NodeType.FuncDecl : boolean
>fgSym.declAST.nodeType : any (error)
>fgSym.declAST : any (error)
>fgSym : any (error)
>declAST : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)
>!(<FuncDecl>fgSym.declAST).isAccessor() : boolean
>(<FuncDecl>fgSym.declAST).isAccessor() : any (error)
>(<FuncDecl>fgSym.declAST).isAccessor : any (error)
>(<FuncDecl>fgSym.declAST) : any (error)
><FuncDecl>fgSym.declAST : any (error)
>fgSym.declAST : any (error)
>fgSym : any (error)
>declAST : any (error)
>isAccessor : any (error)
>!(<FuncDecl>fgSym.declAST).isSignature() : boolean
>(<FuncDecl>fgSym.declAST).isSignature() : any (error)
>(<FuncDecl>fgSym.declAST).isSignature : any (error)
>(<FuncDecl>fgSym.declAST) : any (error)
><FuncDecl>fgSym.declAST : any (error)
>fgSym.declAST : any (error)
>fgSym : any (error)
>declAST : any (error)
>isSignature : any (error)

                        fgSym = null;
>fgSym = null : null
>fgSym : any (error)
>null : null

                        foundSymbol = false;
>foundSymbol = false : false
>foundSymbol : boolean
>false : false
                    }
                }                
            }

            // REVIEW: Move this check into the typecheck phase?  It's only being run over properties...
            if (fgSym &&
>fgSym &&                !fgSym.isAccessor() &&                fgSym.type &&                fgSym.type.construct &&                fgSym.type.construct.signatures != [] &&                (fgSym.type.construct.signatures[0].declAST == null ||                    !hasFlag(fgSym.type.construct.signatures[0].declAST.fncFlags, FncFlags.Ambient)) &&                !funcDecl.isConstructor : boolean
>fgSym &&                !fgSym.isAccessor() &&                fgSym.type &&                fgSym.type.construct &&                fgSym.type.construct.signatures != [] &&                (fgSym.type.construct.signatures[0].declAST == null ||                    !hasFlag(fgSym.type.construct.signatures[0].declAST.fncFlags, FncFlags.Ambient)) : boolean
>fgSym &&                !fgSym.isAccessor() &&                fgSym.type &&                fgSym.type.construct &&                fgSym.type.construct.signatures != [] : boolean
>fgSym &&                !fgSym.isAccessor() &&                fgSym.type &&                fgSym.type.construct : any
>fgSym &&                !fgSym.isAccessor() &&                fgSym.type : any
>fgSym &&                !fgSym.isAccessor() : boolean
>fgSym : any (error)

                !fgSym.isAccessor() &&
>!fgSym.isAccessor() : boolean
>fgSym.isAccessor() : any (error)
>fgSym.isAccessor : any (error)
>fgSym : any (error)
>isAccessor : any (error)

                fgSym.type &&
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)

                fgSym.type.construct &&
>fgSym.type.construct : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
>construct : any (error)

                fgSym.type.construct.signatures != [] &&
>fgSym.type.construct.signatures != [] : boolean
>fgSym.type.construct.signatures : any (error)
>fgSym.type.construct : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
>construct : any (error)
>signatures : any (error)
>[] : undefined[]

                (fgSym.type.construct.signatures[0].declAST == null ||
>(fgSym.type.construct.signatures[0].declAST == null ||                    !hasFlag(fgSym.type.construct.signatures[0].declAST.fncFlags, FncFlags.Ambient)) : boolean
>fgSym.type.construct.signatures[0].declAST == null ||                    !hasFlag(fgSym.type.construct.signatures[0].declAST.fncFlags, FncFlags.Ambient) : boolean
>fgSym.type.construct.signatures[0].declAST == null : boolean
>fgSym.type.construct.signatures[0].declAST : any (error)
>fgSym.type.construct.signatures[0] : any (error)
>fgSym.type.construct.signatures : any (error)
>fgSym.type.construct : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
>construct : any (error)
>signatures : any (error)
>0 : 0
>declAST : any (error)
>null : null

                    !hasFlag(fgSym.type.construct.signatures[0].declAST.fncFlags, FncFlags.Ambient)) &&
>!hasFlag(fgSym.type.construct.signatures[0].declAST.fncFlags, FncFlags.Ambient) : boolean
>hasFlag(fgSym.type.construct.signatures[0].declAST.fncFlags, FncFlags.Ambient) : any (error)
>hasFlag : any (error)
>fgSym.type.construct.signatures[0].declAST.fncFlags : any (error)
>fgSym.type.construct.signatures[0].declAST : any (error)
>fgSym.type.construct.signatures[0] : any (error)
>fgSym.type.construct.signatures : any (error)
>fgSym.type.construct : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
>construct : any (error)
>signatures : any (error)
>0 : 0
>declAST : any (error)
>fncFlags : any (error)
>FncFlags.Ambient : any (error)
>FncFlags : any (error)
>Ambient : any (error)

                !funcDecl.isConstructor) {
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)

                context.checker.errorReporter.simpleError(funcDecl, "Functions may not have class overloads");
>context.checker.errorReporter.simpleError(funcDecl, "Functions may not have class overloads") : any (error)
>context.checker.errorReporter.simpleError : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>simpleError : any (error)
>funcDecl : any (error)
>"Functions may not have class overloads" : "Functions may not have class overloads"
            }

            if (fgSym && !(fgSym.kind() == SymbolKind.Type) && funcDecl.isMethod() && !funcDecl.isAccessor() && !funcDecl.isConstructor) {
>fgSym && !(fgSym.kind() == SymbolKind.Type) && funcDecl.isMethod() && !funcDecl.isAccessor() && !funcDecl.isConstructor : boolean
>fgSym && !(fgSym.kind() == SymbolKind.Type) && funcDecl.isMethod() && !funcDecl.isAccessor() : boolean
>fgSym && !(fgSym.kind() == SymbolKind.Type) && funcDecl.isMethod() : any
>fgSym && !(fgSym.kind() == SymbolKind.Type) : boolean
>fgSym : any (error)
>!(fgSym.kind() == SymbolKind.Type) : boolean
>(fgSym.kind() == SymbolKind.Type) : boolean
>fgSym.kind() == SymbolKind.Type : boolean
>fgSym.kind() : any (error)
>fgSym.kind : any (error)
>fgSym : any (error)
>kind : any (error)
>SymbolKind.Type : any (error)
>SymbolKind : any (error)
>Type : any (error)
>funcDecl.isMethod() : any (error)
>funcDecl.isMethod : any (error)
>funcDecl : any (error)
>isMethod : any (error)
>!funcDecl.isAccessor() : boolean
>funcDecl.isAccessor() : any (error)
>funcDecl.isAccessor : any (error)
>funcDecl : any (error)
>isAccessor : any (error)
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)

                context.checker.errorReporter.simpleError(funcDecl, "Function or method '" + funcDecl.name.actualText + "' already declared as a property");
>context.checker.errorReporter.simpleError(funcDecl, "Function or method '" + funcDecl.name.actualText + "' already declared as a property") : any (error)
>context.checker.errorReporter.simpleError : any (error)
>context.checker.errorReporter : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>errorReporter : any (error)
>simpleError : any (error)
>funcDecl : any (error)
>"Function or method '" + funcDecl.name.actualText + "' already declared as a property" : string
>"Function or method '" + funcDecl.name.actualText : string
>"Function or method '" : "Function or method '"
>funcDecl.name.actualText : any (error)
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>actualText : any (error)
>"' already declared as a property" : "' already declared as a property"

                fgSym.type = context.checker.anyType;
>fgSym.type = context.checker.anyType : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
>context.checker.anyType : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>anyType : any (error)
            }
            var sig = context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, !foundSymbol);
>sig : any (error)
>context.checker.createFunctionSignature(funcDecl, containerSym, containerScope, fgSym, !foundSymbol) : any (error)
>context.checker.createFunctionSignature : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>createFunctionSignature : any (error)
>funcDecl : any (error)
>containerSym : any (error)
>containerScope : any (error)
>fgSym : any (error)
>!foundSymbol : boolean
>foundSymbol : boolean

            // it's a getter or setter function                                   
            if (((!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor()) || (fgSym && fgSym.isAccessor())) {
>((!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor()) || (fgSym && fgSym.isAccessor()) : any
>((!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor()) : any
>(!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor() : any
>(!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) : boolean
>!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl : boolean
>!fgSym : boolean
>fgSym : any (error)
>fgSym.declAST.nodeType != NodeType.FuncDecl : boolean
>fgSym.declAST.nodeType : any (error)
>fgSym.declAST : any (error)
>fgSym : any (error)
>declAST : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)
>funcDecl.isAccessor() : any (error)
>funcDecl.isAccessor : any (error)
>funcDecl : any (error)
>isAccessor : any (error)
>(fgSym && fgSym.isAccessor()) : any
>fgSym && fgSym.isAccessor() : any
>fgSym : any (error)
>fgSym.isAccessor() : any (error)
>fgSym.isAccessor : any (error)
>fgSym : any (error)
>isAccessor : any (error)

                funcDecl.accessorSymbol = context.checker.createAccessorSymbol(funcDecl, fgSym, containerSym.type, (funcDecl.isMethod() && isStatic), true, containerScope, containerSym);
>funcDecl.accessorSymbol = context.checker.createAccessorSymbol(funcDecl, fgSym, containerSym.type, (funcDecl.isMethod() && isStatic), true, containerScope, containerSym) : any (error)
>funcDecl.accessorSymbol : any (error)
>funcDecl : any (error)
>accessorSymbol : any (error)
>context.checker.createAccessorSymbol(funcDecl, fgSym, containerSym.type, (funcDecl.isMethod() && isStatic), true, containerScope, containerSym) : any (error)
>context.checker.createAccessorSymbol : any (error)
>context.checker : any (error)
>context : any (error)
>checker : any (error)
>createAccessorSymbol : any (error)
>funcDecl : any (error)
>fgSym : any (error)
>containerSym.type : any (error)
>containerSym : any (error)
>type : any (error)
>(funcDecl.isMethod() && isStatic) : any
>funcDecl.isMethod() && isStatic : any
>funcDecl.isMethod() : any (error)
>funcDecl.isMethod : any (error)
>funcDecl : any (error)
>isMethod : any (error)
>isStatic : any (error)
>true : true
>containerScope : any (error)
>containerSym : any (error)
            }

            funcDecl.type.symbol.declAST = ast;
>funcDecl.type.symbol.declAST = ast : any (error)
>funcDecl.type.symbol.declAST : any (error)
>funcDecl.type.symbol : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>symbol : any (error)
>declAST : any (error)
>ast : any (error)

            if (funcDecl.isConstructor) { // REVIEW: Remove when classes completely replace oldclass
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)

                go = true;
>go = true : true
>go : boolean
>true : true

            };
        }
        if (isExported) {
>isExported : any (error)

            if (funcDecl.type.call) {
>funcDecl.type.call : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>call : any (error)

                funcDecl.type.symbol.flags |= SymbolFlags.Exported;
>funcDecl.type.symbol.flags |= SymbolFlags.Exported : number
>funcDecl.type.symbol.flags : any (error)
>funcDecl.type.symbol : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>symbol : any (error)
>flags : any (error)
>SymbolFlags.Exported : any (error)
>SymbolFlags : any (error)
>Exported : any (error)
            }
            
            // Accessors are set to 'exported' above
            if (fgSym && !fgSym.isAccessor() && fgSym.kind() == SymbolKind.Type && fgSym.type.call) {
>fgSym && !fgSym.isAccessor() && fgSym.kind() == SymbolKind.Type && fgSym.type.call : any
>fgSym && !fgSym.isAccessor() && fgSym.kind() == SymbolKind.Type : boolean
>fgSym && !fgSym.isAccessor() : boolean
>fgSym : any (error)
>!fgSym.isAccessor() : boolean
>fgSym.isAccessor() : any (error)
>fgSym.isAccessor : any (error)
>fgSym : any (error)
>isAccessor : any (error)
>fgSym.kind() == SymbolKind.Type : boolean
>fgSym.kind() : any (error)
>fgSym.kind : any (error)
>fgSym : any (error)
>kind : any (error)
>SymbolKind.Type : any (error)
>SymbolKind : any (error)
>Type : any (error)
>fgSym.type.call : any (error)
>fgSym.type : any (error)
>fgSym : any (error)
>type : any (error)
>call : any (error)

                fgSym.flags |= SymbolFlags.Exported;
>fgSym.flags |= SymbolFlags.Exported : number
>fgSym.flags : any (error)
>fgSym : any (error)
>flags : any (error)
>SymbolFlags.Exported : any (error)
>SymbolFlags : any (error)
>Exported : any (error)
            }
        }
        if (context.scopeChain.moduleDecl && !funcDecl.isSpecialFn()) {
>context.scopeChain.moduleDecl && !funcDecl.isSpecialFn() : boolean
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
>!funcDecl.isSpecialFn() : boolean
>funcDecl.isSpecialFn() : any (error)
>funcDecl.isSpecialFn : any (error)
>funcDecl : any (error)
>isSpecialFn : any (error)

            funcDecl.type.symbol.flags |= SymbolFlags.ModuleMember;
>funcDecl.type.symbol.flags |= SymbolFlags.ModuleMember : number
>funcDecl.type.symbol.flags : any (error)
>funcDecl.type.symbol : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>symbol : any (error)
>flags : any (error)
>SymbolFlags.ModuleMember : any (error)
>SymbolFlags : any (error)
>ModuleMember : any (error)

            funcDecl.type.symbol.declModule = context.scopeChain.moduleDecl;
>funcDecl.type.symbol.declModule = context.scopeChain.moduleDecl : any (error)
>funcDecl.type.symbol.declModule : any (error)
>funcDecl.type.symbol : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>symbol : any (error)
>declModule : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
        }

        if (fgSym && isOptional) {
>fgSym && isOptional : any
>fgSym : any (error)
>isOptional : any

            fgSym.flags |= SymbolFlags.Optional;
>fgSym.flags |= SymbolFlags.Optional : number
>fgSym.flags : any (error)
>fgSym : any (error)
>flags : any (error)
>SymbolFlags.Optional : any (error)
>SymbolFlags : any (error)
>Optional : any (error)
        }

        return go;
>go : boolean
    }

    export function preCollectTypes(ast: AST, parent: AST, walker: IAstWalker) {
>preCollectTypes : (ast: any (error), parent: any (error), walker: any (error)) => any (error)
>ast : any (error)
>parent : any (error)
>walker : any (error)

        var context: TypeCollectionContext = walker.state;
>context : any (error)
>walker.state : any (error)
>walker : any (error)
>state : any (error)

        var go = false;
>go : boolean
>false : false

        var scopeChain = context.scopeChain;
>scopeChain : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)

        if (ast.nodeType == NodeType.Script) {
>ast.nodeType == NodeType.Script : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.Script : any (error)
>NodeType : any (error)
>Script : any (error)

            var script: Script = <Script>ast;
>script : any (error)
><Script>ast : any (error)
>ast : any (error)

            context.script = script;
>context.script = script : any (error)
>context.script : any (error)
>context : any (error)
>script : any (error)
>script : any (error)

            go = true;
>go = true : true
>go : boolean
>true : true
        }
        else if (ast.nodeType == NodeType.List) {
>ast.nodeType == NodeType.List : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.List : any (error)
>NodeType : any (error)
>List : any (error)

            go = true;
>go = true : true
>go : boolean
>true : true
        }
        else if (ast.nodeType == NodeType.ImportDeclaration) {
>ast.nodeType == NodeType.ImportDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ImportDeclaration : any (error)
>NodeType : any (error)
>ImportDeclaration : any (error)

            go = preCollectImportTypes(ast, parent, context);
>go = preCollectImportTypes(ast, parent, context) : boolean
>go : boolean
>preCollectImportTypes(ast, parent, context) : boolean
>preCollectImportTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)
        }
        else if (ast.nodeType == NodeType.With) {
>ast.nodeType == NodeType.With : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.With : any (error)
>NodeType : any (error)
>With : any (error)

            go = false;
>go = false : false
>go : boolean
>false : false
        }
        else if (ast.nodeType == NodeType.ModuleDeclaration) {
>ast.nodeType == NodeType.ModuleDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ModuleDeclaration : any (error)
>NodeType : any (error)
>ModuleDeclaration : any (error)

            go = preCollectModuleTypes(ast, parent, context);
>go = preCollectModuleTypes(ast, parent, context) : boolean
>go : boolean
>preCollectModuleTypes(ast, parent, context) : boolean
>preCollectModuleTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)
        }
        else if (ast.nodeType == NodeType.ClassDeclaration) {
>ast.nodeType == NodeType.ClassDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ClassDeclaration : any (error)
>NodeType : any (error)
>ClassDeclaration : any (error)

            go = preCollectClassTypes(ast, parent, context);
>go = preCollectClassTypes(ast, parent, context) : boolean
>go : boolean
>preCollectClassTypes(ast, parent, context) : boolean
>preCollectClassTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)
        }
        else if (ast.nodeType == NodeType.Block) {
>ast.nodeType == NodeType.Block : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.Block : any (error)
>NodeType : any (error)
>Block : any (error)

            go = true;
>go = true : true
>go : boolean
>true : true
        }
        else if (ast.nodeType == NodeType.InterfaceDeclaration) {
>ast.nodeType == NodeType.InterfaceDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.InterfaceDeclaration : any (error)
>NodeType : any (error)
>InterfaceDeclaration : any (error)

            go = preCollectInterfaceTypes(ast, parent, context);
>go = preCollectInterfaceTypes(ast, parent, context) : boolean
>go : boolean
>preCollectInterfaceTypes(ast, parent, context) : boolean
>preCollectInterfaceTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)
        }
        // This will be a constructor arg because this pass only traverses
        // constructor arg lists
        else if (ast.nodeType == NodeType.ArgDecl) {
>ast.nodeType == NodeType.ArgDecl : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ArgDecl : any (error)
>NodeType : any (error)
>ArgDecl : any (error)

            go = preCollectArgDeclTypes(ast, parent, context);
>go = preCollectArgDeclTypes(ast, parent, context) : boolean
>go : boolean
>preCollectArgDeclTypes(ast, parent, context) : boolean
>preCollectArgDeclTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)
        }
        else if (ast.nodeType == NodeType.VarDecl) {
>ast.nodeType == NodeType.VarDecl : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.VarDecl : any (error)
>NodeType : any (error)
>VarDecl : any (error)

            go = preCollectVarDeclTypes(ast, parent, context);
>go = preCollectVarDeclTypes(ast, parent, context) : boolean
>go : boolean
>preCollectVarDeclTypes(ast, parent, context) : boolean
>preCollectVarDeclTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)
        }
        else if (ast.nodeType == NodeType.FuncDecl) {
>ast.nodeType == NodeType.FuncDecl : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)

            go = preCollectFuncDeclTypes(ast, parent, context);
>go = preCollectFuncDeclTypes(ast, parent, context) : boolean
>go : boolean
>preCollectFuncDeclTypes(ast, parent, context) : boolean
>preCollectFuncDeclTypes : (ast: any (error), parent: any (error), context: any (error)) => boolean
>ast : any (error)
>parent : any (error)
>context : any (error)
        }
        else {
            if (ast.isStatementOrExpression() && context.scopeChain.moduleDecl) {
>ast.isStatementOrExpression() && context.scopeChain.moduleDecl : any
>ast.isStatementOrExpression() : any (error)
>ast.isStatementOrExpression : any (error)
>ast : any (error)
>isStatementOrExpression : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)

                context.scopeChain.moduleDecl.recordNonInterface();
>context.scopeChain.moduleDecl.recordNonInterface() : any (error)
>context.scopeChain.moduleDecl.recordNonInterface : any (error)
>context.scopeChain.moduleDecl : any (error)
>context.scopeChain : any (error)
>context : any (error)
>scopeChain : any (error)
>moduleDecl : any (error)
>recordNonInterface : any (error)
            }
        }
        walker.options.goChildren = go;
>walker.options.goChildren = go : boolean
>walker.options.goChildren : any (error)
>walker.options : any (error)
>walker : any (error)
>options : any (error)
>goChildren : any (error)
>go : boolean

        return ast;
>ast : any (error)
    }

    export function postCollectTypes(ast: AST, parent: AST, walker: IAstWalker) {
>postCollectTypes : (ast: any (error), parent: any (error), walker: any (error)) => any (error)
>ast : any (error)
>parent : any (error)
>walker : any (error)

        var context: TypeCollectionContext = walker.state;
>context : any (error)
>walker.state : any (error)
>walker : any (error)
>state : any (error)

        if (ast.nodeType == NodeType.ModuleDeclaration) {
>ast.nodeType == NodeType.ModuleDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ModuleDeclaration : any (error)
>NodeType : any (error)
>ModuleDeclaration : any (error)

            popTypeCollectionScope(context);
>popTypeCollectionScope(context) : any (error)
>popTypeCollectionScope : any (error)
>context : any (error)
        }
        else if (ast.nodeType == NodeType.ClassDeclaration) {
>ast.nodeType == NodeType.ClassDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ClassDeclaration : any (error)
>NodeType : any (error)
>ClassDeclaration : any (error)

            popTypeCollectionScope(context);
>popTypeCollectionScope(context) : any (error)
>popTypeCollectionScope : any (error)
>context : any (error)
        }
        else if (ast.nodeType == NodeType.InterfaceDeclaration) {
>ast.nodeType == NodeType.InterfaceDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.InterfaceDeclaration : any (error)
>NodeType : any (error)
>InterfaceDeclaration : any (error)

            popTypeCollectionScope(context);
>popTypeCollectionScope(context) : any (error)
>popTypeCollectionScope : any (error)
>context : any (error)
        }
        return ast;
>ast : any (error)
    }

}
