=== tests/cases/conformance/parser/ecmascript5/parserRealSource8.ts ===
// Copyright (c) Microsoft. All rights reserved. Licensed under the Apache License, Version 2.0. 
// See LICENSE.txt in the project root for complete license information.

///<reference path='typescript.ts' />

module TypeScript {
>TypeScript : typeof TypeScript

    export class AssignScopeContext {
>AssignScopeContext : AssignScopeContext

        constructor (public scopeChain: ScopeChain,
>scopeChain : any (error)

                     public typeFlow: TypeFlow,
>typeFlow : any (error)

                     public modDeclChain: ModuleDeclaration[]) {
>modDeclChain : any (error)[]
        }
    }

    export function pushAssignScope(scope: SymbolScope,
>pushAssignScope : (scope: any (error), context: AssignScopeContext, type: any (error), classType: any (error), fnc: any (error)) => void
>scope : any (error)

        context: AssignScopeContext,
>context : AssignScopeContext

        type: Type,
>type : any (error)

        classType: Type,
>classType : any (error)

        fnc: FuncDecl) {
>fnc : any (error)

        var chain = new ScopeChain(null, context.scopeChain, scope);
>chain : any (error)
>new ScopeChain(null, context.scopeChain, scope) : any (error)
>ScopeChain : any (error)
>null : null
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)

        chain.thisType = type;
>chain.thisType = type : any (error)
>chain.thisType : any (error)
>chain : any (error)
>thisType : any (error)
>type : any (error)

        chain.classType = classType;
>chain.classType = classType : any (error)
>chain.classType : any (error)
>chain : any (error)
>classType : any (error)
>classType : any (error)

        chain.fnc = fnc;
>chain.fnc = fnc : any (error)
>chain.fnc : any (error)
>chain : any (error)
>fnc : any (error)
>fnc : any (error)

        context.scopeChain = chain;
>context.scopeChain = chain : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>chain : any (error)
    }

    export function popAssignScope(context: AssignScopeContext) {
>popAssignScope : (context: AssignScopeContext) => void
>context : AssignScopeContext

        context.scopeChain = context.scopeChain.previous;
>context.scopeChain = context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>previous : any (error)
    }

    export function instanceCompare(a: Symbol, b: Symbol) {
>instanceCompare : (a: Symbol, b: Symbol) => Symbol
>a : Symbol
>b : Symbol

        if (((a == null) || (!a.isInstanceProperty()))) {
>((a == null) || (!a.isInstanceProperty())) : boolean
>(a == null) || (!a.isInstanceProperty()) : boolean
>(a == null) : boolean
>a == null : boolean
>a : Symbol
>null : null
>(!a.isInstanceProperty()) : boolean
>!a.isInstanceProperty() : boolean
>a.isInstanceProperty() : any (error)
>a.isInstanceProperty : any (error)
>a : Symbol
>isInstanceProperty : any (error)

            return b;
>b : Symbol
        }
        else {
            return a;
>a : Symbol
        }
    }

    export function instanceFilterStop(s: Symbol) {
>instanceFilterStop : (s: Symbol) => any (error)
>s : Symbol

        return s.isInstanceProperty();
>s.isInstanceProperty() : any (error)
>s.isInstanceProperty : any (error)
>s : Symbol
>isInstanceProperty : any (error)
    }

    export class ScopeSearchFilter {
>ScopeSearchFilter : ScopeSearchFilter

        constructor (public select: (a: Symbol, b: Symbol) =>Symbol,
>select : (a: Symbol, b: Symbol) => Symbol
>a : Symbol
>b : Symbol

                            public stop: (s: Symbol) =>boolean) { }
>stop : (s: Symbol) => boolean
>s : Symbol

        public result: Symbol = null;
>result : Symbol
>null : null

        public reset() {
>reset : () => void

            this.result = null;
>this.result = null : null
>this.result : Symbol
>this : this
>result : Symbol
>null : null
        }

        public update(b: Symbol): boolean {
>update : (b: Symbol) => boolean
>b : Symbol

            this.result = this.select(this.result, b);
>this.result = this.select(this.result, b) : Symbol
>this.result : Symbol
>this : this
>result : Symbol
>this.select(this.result, b) : Symbol
>this.select : (a: Symbol, b: Symbol) => Symbol
>this : this
>select : (a: Symbol, b: Symbol) => Symbol
>this.result : Symbol
>this : this
>result : Symbol
>b : Symbol

            if (this.result) {
>this.result : Symbol
>this : this
>result : Symbol

                return this.stop(this.result);
>this.stop(this.result) : boolean
>this.stop : (s: Symbol) => boolean
>this : this
>stop : (s: Symbol) => boolean
>this.result : Symbol
>this : this
>result : Symbol
            }
            else {
                return false;
>false : false
            }
        }
    }

    export var instanceFilter = new ScopeSearchFilter(instanceCompare, instanceFilterStop);
>instanceFilter : ScopeSearchFilter
>new ScopeSearchFilter(instanceCompare, instanceFilterStop) : ScopeSearchFilter
>ScopeSearchFilter : typeof ScopeSearchFilter
>instanceCompare : (a: Symbol, b: Symbol) => Symbol
>instanceFilterStop : (s: Symbol) => any (error)

    export function preAssignModuleScopes(ast: AST, context: AssignScopeContext) {
>preAssignModuleScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext

        var moduleDecl = <ModuleDeclaration>ast;
>moduleDecl : any (error)
><ModuleDeclaration>ast : any (error)
>ast : any (error)

        var memberScope: SymbolTableScope = null;
>memberScope : any (error)
>null : null

        var aggScope: SymbolAggregateScope = null;
>aggScope : any (error)
>null : null

        if (moduleDecl.name && moduleDecl.mod) {
>moduleDecl.name && moduleDecl.mod : any
>moduleDecl.name : any (error)
>moduleDecl : any (error)
>name : any (error)
>moduleDecl.mod : any (error)
>moduleDecl : any (error)
>mod : any (error)

            moduleDecl.name.sym = moduleDecl.mod.symbol;
>moduleDecl.name.sym = moduleDecl.mod.symbol : any (error)
>moduleDecl.name.sym : any (error)
>moduleDecl.name : any (error)
>moduleDecl : any (error)
>name : any (error)
>sym : any (error)
>moduleDecl.mod.symbol : any (error)
>moduleDecl.mod : any (error)
>moduleDecl : any (error)
>mod : any (error)
>symbol : any (error)
        }

        var mod = moduleDecl.mod;
>mod : any (error)
>moduleDecl.mod : any (error)
>moduleDecl : any (error)
>mod : any (error)

        // We're likely here because of error recovery
        if (!mod) {
>!mod : boolean
>mod : any (error)

            return;
        }

        memberScope = new SymbolTableScope(mod.members, mod.ambientMembers, mod.enclosedTypes, mod.ambientEnclosedTypes, mod.symbol);
>memberScope = new SymbolTableScope(mod.members, mod.ambientMembers, mod.enclosedTypes, mod.ambientEnclosedTypes, mod.symbol) : any (error)
>memberScope : any (error)
>new SymbolTableScope(mod.members, mod.ambientMembers, mod.enclosedTypes, mod.ambientEnclosedTypes, mod.symbol) : any (error)
>SymbolTableScope : any (error)
>mod.members : any (error)
>mod : any (error)
>members : any (error)
>mod.ambientMembers : any (error)
>mod : any (error)
>ambientMembers : any (error)
>mod.enclosedTypes : any (error)
>mod : any (error)
>enclosedTypes : any (error)
>mod.ambientEnclosedTypes : any (error)
>mod : any (error)
>ambientEnclosedTypes : any (error)
>mod.symbol : any (error)
>mod : any (error)
>symbol : any (error)

        mod.memberScope = memberScope;
>mod.memberScope = memberScope : any (error)
>mod.memberScope : any (error)
>mod : any (error)
>memberScope : any (error)
>memberScope : any (error)

        context.modDeclChain.push(moduleDecl);
>context.modDeclChain.push(moduleDecl) : number
>context.modDeclChain.push : (...items: any (error)[]) => number
>context.modDeclChain : any (error)[]
>context : AssignScopeContext
>modDeclChain : any (error)[]
>push : (...items: any (error)[]) => number
>moduleDecl : any (error)

        context.typeFlow.checker.currentModDecl = moduleDecl;
>context.typeFlow.checker.currentModDecl = moduleDecl : any (error)
>context.typeFlow.checker.currentModDecl : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>currentModDecl : any (error)
>moduleDecl : any (error)

        aggScope = new SymbolAggregateScope(mod.symbol);
>aggScope = new SymbolAggregateScope(mod.symbol) : any (error)
>aggScope : any (error)
>new SymbolAggregateScope(mod.symbol) : any (error)
>SymbolAggregateScope : any (error)
>mod.symbol : any (error)
>mod : any (error)
>symbol : any (error)

        aggScope.addParentScope(memberScope);
>aggScope.addParentScope(memberScope) : any (error)
>aggScope.addParentScope : any (error)
>aggScope : any (error)
>addParentScope : any (error)
>memberScope : any (error)

        aggScope.addParentScope(context.scopeChain.scope);
>aggScope.addParentScope(context.scopeChain.scope) : any (error)
>aggScope.addParentScope : any (error)
>aggScope : any (error)
>addParentScope : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)

        pushAssignScope(aggScope, context, null, null, null);
>pushAssignScope(aggScope, context, null, null, null) : void
>pushAssignScope : (scope: any (error), context: AssignScopeContext, type: any (error), classType: any (error), fnc: any (error)) => void
>aggScope : any (error)
>context : AssignScopeContext
>null : null
>null : null
>null : null

        mod.containedScope = aggScope;
>mod.containedScope = aggScope : any (error)
>mod.containedScope : any (error)
>mod : any (error)
>containedScope : any (error)
>aggScope : any (error)

        if (mod.symbol) {
>mod.symbol : any (error)
>mod : any (error)
>symbol : any (error)

            context.typeFlow.addLocalsFromScope(mod.containedScope, mod.symbol, moduleDecl.vars, mod.members.privateMembers, true);
>context.typeFlow.addLocalsFromScope(mod.containedScope, mod.symbol, moduleDecl.vars, mod.members.privateMembers, true) : any (error)
>context.typeFlow.addLocalsFromScope : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>addLocalsFromScope : any (error)
>mod.containedScope : any (error)
>mod : any (error)
>containedScope : any (error)
>mod.symbol : any (error)
>mod : any (error)
>symbol : any (error)
>moduleDecl.vars : any (error)
>moduleDecl : any (error)
>vars : any (error)
>mod.members.privateMembers : any (error)
>mod.members : any (error)
>mod : any (error)
>members : any (error)
>privateMembers : any (error)
>true : true
        }
    }

    export function preAssignClassScopes(ast: AST, context: AssignScopeContext) {
>preAssignClassScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext

        var classDecl = <InterfaceDeclaration>ast;
>classDecl : any (error)
><InterfaceDeclaration>ast : any (error)
>ast : any (error)

        var memberScope: SymbolTableScope = null;
>memberScope : any (error)
>null : null

        var aggScope: SymbolAggregateScope = null;
>aggScope : any (error)
>null : null

        if (classDecl.name && classDecl.type) {
>classDecl.name && classDecl.type : any
>classDecl.name : any (error)
>classDecl : any (error)
>name : any (error)
>classDecl.type : any (error)
>classDecl : any (error)
>type : any (error)

            classDecl.name.sym = classDecl.type.symbol;
>classDecl.name.sym = classDecl.type.symbol : any (error)
>classDecl.name.sym : any (error)
>classDecl.name : any (error)
>classDecl : any (error)
>name : any (error)
>sym : any (error)
>classDecl.type.symbol : any (error)
>classDecl.type : any (error)
>classDecl : any (error)
>type : any (error)
>symbol : any (error)
        }

        var classType = ast.type;
>classType : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)

        if (classType) {
>classType : any (error)

            var classSym = classType.symbol;
>classSym : any (error)
>classType.symbol : any (error)
>classType : any (error)
>symbol : any (error)

            memberScope = <SymbolTableScope>context.typeFlow.checker.scopeOf(classType);
>memberScope = <SymbolTableScope>context.typeFlow.checker.scopeOf(classType) : any (error)
>memberScope : any (error)
><SymbolTableScope>context.typeFlow.checker.scopeOf(classType) : any (error)
>context.typeFlow.checker.scopeOf(classType) : any (error)
>context.typeFlow.checker.scopeOf : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>scopeOf : any (error)
>classType : any (error)

            aggScope = new SymbolAggregateScope(classType.symbol);
>aggScope = new SymbolAggregateScope(classType.symbol) : any (error)
>aggScope : any (error)
>new SymbolAggregateScope(classType.symbol) : any (error)
>SymbolAggregateScope : any (error)
>classType.symbol : any (error)
>classType : any (error)
>symbol : any (error)

            aggScope.addParentScope(memberScope);
>aggScope.addParentScope(memberScope) : any (error)
>aggScope.addParentScope : any (error)
>aggScope : any (error)
>addParentScope : any (error)
>memberScope : any (error)

            aggScope.addParentScope(context.scopeChain.scope);
>aggScope.addParentScope(context.scopeChain.scope) : any (error)
>aggScope.addParentScope : any (error)
>aggScope : any (error)
>addParentScope : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)

            classType.containedScope = aggScope;
>classType.containedScope = aggScope : any (error)
>classType.containedScope : any (error)
>classType : any (error)
>containedScope : any (error)
>aggScope : any (error)

            classType.memberScope = memberScope;
>classType.memberScope = memberScope : any (error)
>classType.memberScope : any (error)
>classType : any (error)
>memberScope : any (error)
>memberScope : any (error)

            var instanceType = classType.instanceType;
>instanceType : any (error)
>classType.instanceType : any (error)
>classType : any (error)
>instanceType : any (error)

            memberScope = <SymbolTableScope>context.typeFlow.checker.scopeOf(instanceType);
>memberScope = <SymbolTableScope>context.typeFlow.checker.scopeOf(instanceType) : any (error)
>memberScope : any (error)
><SymbolTableScope>context.typeFlow.checker.scopeOf(instanceType) : any (error)
>context.typeFlow.checker.scopeOf(instanceType) : any (error)
>context.typeFlow.checker.scopeOf : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>scopeOf : any (error)
>instanceType : any (error)

            instanceType.memberScope = memberScope;
>instanceType.memberScope = memberScope : any (error)
>instanceType.memberScope : any (error)
>instanceType : any (error)
>memberScope : any (error)
>memberScope : any (error)

            aggScope = new SymbolAggregateScope(instanceType.symbol);
>aggScope = new SymbolAggregateScope(instanceType.symbol) : any (error)
>aggScope : any (error)
>new SymbolAggregateScope(instanceType.symbol) : any (error)
>SymbolAggregateScope : any (error)
>instanceType.symbol : any (error)
>instanceType : any (error)
>symbol : any (error)

            aggScope.addParentScope(context.scopeChain.scope);
>aggScope.addParentScope(context.scopeChain.scope) : any (error)
>aggScope.addParentScope : any (error)
>aggScope : any (error)
>addParentScope : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)

            pushAssignScope(aggScope, context, instanceType, classType, null);
>pushAssignScope(aggScope, context, instanceType, classType, null) : void
>pushAssignScope : (scope: any (error), context: AssignScopeContext, type: any (error), classType: any (error), fnc: any (error)) => void
>aggScope : any (error)
>context : AssignScopeContext
>instanceType : any (error)
>classType : any (error)
>null : null

            instanceType.containedScope = aggScope;
>instanceType.containedScope = aggScope : any (error)
>instanceType.containedScope : any (error)
>instanceType : any (error)
>containedScope : any (error)
>aggScope : any (error)
        }
        else {
            ast.type = context.typeFlow.anyType;
>ast.type = context.typeFlow.anyType : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)
>context.typeFlow.anyType : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>anyType : any (error)
        }
    }

    export function preAssignInterfaceScopes(ast: AST, context: AssignScopeContext) {
>preAssignInterfaceScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext

        var interfaceDecl = <InterfaceDeclaration>ast;
>interfaceDecl : any (error)
><InterfaceDeclaration>ast : any (error)
>ast : any (error)

        var memberScope: SymbolTableScope = null;
>memberScope : any (error)
>null : null

        var aggScope: SymbolAggregateScope = null;
>aggScope : any (error)
>null : null

        if (interfaceDecl.name && interfaceDecl.type) {
>interfaceDecl.name && interfaceDecl.type : any
>interfaceDecl.name : any (error)
>interfaceDecl : any (error)
>name : any (error)
>interfaceDecl.type : any (error)
>interfaceDecl : any (error)
>type : any (error)

            interfaceDecl.name.sym = interfaceDecl.type.symbol;
>interfaceDecl.name.sym = interfaceDecl.type.symbol : any (error)
>interfaceDecl.name.sym : any (error)
>interfaceDecl.name : any (error)
>interfaceDecl : any (error)
>name : any (error)
>sym : any (error)
>interfaceDecl.type.symbol : any (error)
>interfaceDecl.type : any (error)
>interfaceDecl : any (error)
>type : any (error)
>symbol : any (error)
        }

        var interfaceType = ast.type;
>interfaceType : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)

        memberScope = <SymbolTableScope>context.typeFlow.checker.scopeOf(interfaceType);
>memberScope = <SymbolTableScope>context.typeFlow.checker.scopeOf(interfaceType) : any (error)
>memberScope : any (error)
><SymbolTableScope>context.typeFlow.checker.scopeOf(interfaceType) : any (error)
>context.typeFlow.checker.scopeOf(interfaceType) : any (error)
>context.typeFlow.checker.scopeOf : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>scopeOf : any (error)
>interfaceType : any (error)

        interfaceType.memberScope = memberScope;
>interfaceType.memberScope = memberScope : any (error)
>interfaceType.memberScope : any (error)
>interfaceType : any (error)
>memberScope : any (error)
>memberScope : any (error)

        aggScope = new SymbolAggregateScope(interfaceType.symbol);
>aggScope = new SymbolAggregateScope(interfaceType.symbol) : any (error)
>aggScope : any (error)
>new SymbolAggregateScope(interfaceType.symbol) : any (error)
>SymbolAggregateScope : any (error)
>interfaceType.symbol : any (error)
>interfaceType : any (error)
>symbol : any (error)

        aggScope.addParentScope(memberScope);
>aggScope.addParentScope(memberScope) : any (error)
>aggScope.addParentScope : any (error)
>aggScope : any (error)
>addParentScope : any (error)
>memberScope : any (error)

        aggScope.addParentScope(context.scopeChain.scope);
>aggScope.addParentScope(context.scopeChain.scope) : any (error)
>aggScope.addParentScope : any (error)
>aggScope : any (error)
>addParentScope : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)

        pushAssignScope(aggScope, context, null, null, null);
>pushAssignScope(aggScope, context, null, null, null) : void
>pushAssignScope : (scope: any (error), context: AssignScopeContext, type: any (error), classType: any (error), fnc: any (error)) => void
>aggScope : any (error)
>context : AssignScopeContext
>null : null
>null : null
>null : null

        interfaceType.containedScope = aggScope;
>interfaceType.containedScope = aggScope : any (error)
>interfaceType.containedScope : any (error)
>interfaceType : any (error)
>containedScope : any (error)
>aggScope : any (error)
    }

    export function preAssignWithScopes(ast: AST, context: AssignScopeContext) {
>preAssignWithScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext

        var withStmt = <WithStatement>ast;
>withStmt : any (error)
><WithStatement>ast : any (error)
>ast : any (error)

        var withType = withStmt.type;
>withType : any (error)
>withStmt.type : any (error)
>withStmt : any (error)
>type : any (error)

        var members = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>members : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var ambientMembers = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable()));
>ambientMembers : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var withType = new Type();
>withType : any (error)
>new Type() : any (error)
>Type : any (error)

        var withSymbol = new WithSymbol(withStmt.minChar, context.typeFlow.checker.locationInfo.unitIndex, withType);
>withSymbol : any (error)
>new WithSymbol(withStmt.minChar, context.typeFlow.checker.locationInfo.unitIndex, withType) : any (error)
>WithSymbol : any (error)
>withStmt.minChar : any (error)
>withStmt : any (error)
>minChar : any (error)
>context.typeFlow.checker.locationInfo.unitIndex : any (error)
>context.typeFlow.checker.locationInfo : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)
>withType : any (error)

        withType.members = members;
>withType.members = members : any (error)
>withType.members : any (error)
>withType : any (error)
>members : any (error)
>members : any (error)

        withType.ambientMembers = ambientMembers;
>withType.ambientMembers = ambientMembers : any (error)
>withType.ambientMembers : any (error)
>withType : any (error)
>ambientMembers : any (error)
>ambientMembers : any (error)

        withType.symbol = withSymbol;
>withType.symbol = withSymbol : any (error)
>withType.symbol : any (error)
>withType : any (error)
>symbol : any (error)
>withSymbol : any (error)

        withType.setHasImplementation();
>withType.setHasImplementation() : any (error)
>withType.setHasImplementation : any (error)
>withType : any (error)
>setHasImplementation : any (error)

        withStmt.type = withType;
>withStmt.type = withType : any (error)
>withStmt.type : any (error)
>withStmt : any (error)
>type : any (error)
>withType : any (error)

        var withScope = new TypeScript.SymbolScopeBuilder(withType.members, withType.ambientMembers, null, null, context.scopeChain.scope, withType.symbol);
>withScope : any (error)
>new TypeScript.SymbolScopeBuilder(withType.members, withType.ambientMembers, null, null, context.scopeChain.scope, withType.symbol) : any (error)
>TypeScript.SymbolScopeBuilder : any (error)
>TypeScript : typeof TypeScript
>SymbolScopeBuilder : any (error)
>withType.members : any (error)
>withType : any (error)
>members : any (error)
>withType.ambientMembers : any (error)
>withType : any (error)
>ambientMembers : any (error)
>null : null
>null : null
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)
>withType.symbol : any (error)
>withType : any (error)
>symbol : any (error)

        pushAssignScope(withScope, context, null, null, null);
>pushAssignScope(withScope, context, null, null, null) : void
>pushAssignScope : (scope: any (error), context: AssignScopeContext, type: any (error), classType: any (error), fnc: any (error)) => void
>withScope : any (error)
>context : AssignScopeContext
>null : null
>null : null
>null : null

        withType.containedScope = withScope;
>withType.containedScope = withScope : any (error)
>withType.containedScope : any (error)
>withType : any (error)
>containedScope : any (error)
>withScope : any (error)
    }

    export function preAssignFuncDeclScopes(ast: AST, context: AssignScopeContext) {
>preAssignFuncDeclScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext

        var funcDecl = <FuncDecl>ast;
>funcDecl : any (error)
><FuncDecl>ast : any (error)
>ast : any (error)

        var container: Symbol = null;
>container : Symbol
>null : null

        var localContainer: Symbol = null;
>localContainer : Symbol
>null : null

        if (funcDecl.type) {
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)

            localContainer = ast.type.symbol;
>localContainer = ast.type.symbol : any (error)
>localContainer : Symbol
>ast.type.symbol : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)
>symbol : any (error)
        }

        var isStatic = hasFlag(funcDecl.fncFlags, FncFlags.Static);
>isStatic : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.Static) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.Static : any (error)
>FncFlags : any (error)
>Static : any (error)

        var isInnerStatic = isStatic && context.scopeChain.fnc != null;
>isInnerStatic : boolean
>isStatic && context.scopeChain.fnc != null : boolean
>isStatic : any (error)
>context.scopeChain.fnc != null : boolean
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)
>null : null

        // for inner static functions, use the parent's member scope, so local vars cannot be captured
        var parentScope = isInnerStatic ? context.scopeChain.fnc.type.memberScope : context.scopeChain.scope;
>parentScope : any
>isInnerStatic ? context.scopeChain.fnc.type.memberScope : context.scopeChain.scope : any
>isInnerStatic : boolean
>context.scopeChain.fnc.type.memberScope : any (error)
>context.scopeChain.fnc.type : any (error)
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)
>type : any (error)
>memberScope : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)

        // if this is not a method, but enclosed by class, use constructor as
        // the enclosing scope
        // REVIEW: Some twisted logic here - this needs to be cleaned up once old classes are removed
        //  - if it's a new class, always use the contained scope, since we initialize the constructor scope below
        if (context.scopeChain.thisType &&
>context.scopeChain.thisType &&            (!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) : any
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)

            (!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod))) {
>(!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) : any
>!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.ClassMethod : any (error)
>FncFlags : any (error)
>ClassMethod : any (error)

            var instType = context.scopeChain.thisType;
>instType : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)

            if (!(instType.typeFlags & TypeFlags.IsClass) && !hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) {
>!(instType.typeFlags & TypeFlags.IsClass) && !hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : boolean
>!(instType.typeFlags & TypeFlags.IsClass) : boolean
>(instType.typeFlags & TypeFlags.IsClass) : number
>instType.typeFlags & TypeFlags.IsClass : number
>instType.typeFlags : any (error)
>instType : any (error)
>typeFlags : any (error)
>TypeFlags.IsClass : any (error)
>TypeFlags : any (error)
>IsClass : any (error)
>!hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : boolean
>hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.ClassMethod : any (error)
>FncFlags : any (error)
>ClassMethod : any (error)

                if (!funcDecl.isMethod() || isStatic) {
>!funcDecl.isMethod() || isStatic : any
>!funcDecl.isMethod() : boolean
>funcDecl.isMethod() : any (error)
>funcDecl.isMethod : any (error)
>funcDecl : any (error)
>isMethod : any (error)
>isStatic : any (error)

                    parentScope = instType.constructorScope;
>parentScope = instType.constructorScope : any (error)
>parentScope : any
>instType.constructorScope : any (error)
>instType : any (error)
>constructorScope : any (error)
                }
                else {
                    // use constructor scope if a method as well
                    parentScope = instType.containedScope;
>parentScope = instType.containedScope : any (error)
>parentScope : any
>instType.containedScope : any (error)
>instType : any (error)
>containedScope : any (error)
                }
            }
            else {
                if (context.scopeChain.previous.scope.container &&
>context.scopeChain.previous.scope.container &&                    context.scopeChain.previous.scope.container.declAST &&                    context.scopeChain.previous.scope.container.declAST.nodeType == NodeType.FuncDecl &&                    (<FuncDecl>context.scopeChain.previous.scope.container.declAST).isConstructor : any
>context.scopeChain.previous.scope.container &&                    context.scopeChain.previous.scope.container.declAST &&                    context.scopeChain.previous.scope.container.declAST.nodeType == NodeType.FuncDecl : boolean
>context.scopeChain.previous.scope.container &&                    context.scopeChain.previous.scope.container.declAST : any
>context.scopeChain.previous.scope.container : any (error)
>context.scopeChain.previous.scope : any (error)
>context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>previous : any (error)
>scope : any (error)
>container : any (error)

                    context.scopeChain.previous.scope.container.declAST &&
>context.scopeChain.previous.scope.container.declAST : any (error)
>context.scopeChain.previous.scope.container : any (error)
>context.scopeChain.previous.scope : any (error)
>context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>previous : any (error)
>scope : any (error)
>container : any (error)
>declAST : any (error)

                    context.scopeChain.previous.scope.container.declAST.nodeType == NodeType.FuncDecl &&
>context.scopeChain.previous.scope.container.declAST.nodeType == NodeType.FuncDecl : boolean
>context.scopeChain.previous.scope.container.declAST.nodeType : any (error)
>context.scopeChain.previous.scope.container.declAST : any (error)
>context.scopeChain.previous.scope.container : any (error)
>context.scopeChain.previous.scope : any (error)
>context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>previous : any (error)
>scope : any (error)
>container : any (error)
>declAST : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)

                    (<FuncDecl>context.scopeChain.previous.scope.container.declAST).isConstructor) {
>(<FuncDecl>context.scopeChain.previous.scope.container.declAST).isConstructor : any (error)
>(<FuncDecl>context.scopeChain.previous.scope.container.declAST) : any (error)
><FuncDecl>context.scopeChain.previous.scope.container.declAST : any (error)
>context.scopeChain.previous.scope.container.declAST : any (error)
>context.scopeChain.previous.scope.container : any (error)
>context.scopeChain.previous.scope : any (error)
>context.scopeChain.previous : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>previous : any (error)
>scope : any (error)
>container : any (error)
>declAST : any (error)
>isConstructor : any (error)

                        // if the parent is the class constructor, use the constructor scope
                    parentScope = instType.constructorScope;
>parentScope = instType.constructorScope : any (error)
>parentScope : any
>instType.constructorScope : any (error)
>instType : any (error)
>constructorScope : any (error)
                }
                else if (isStatic && context.scopeChain.classType) {
>isStatic && context.scopeChain.classType : any
>isStatic : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>classType : any (error)

                    parentScope = context.scopeChain.classType.containedScope;
>parentScope = context.scopeChain.classType.containedScope : any (error)
>parentScope : any
>context.scopeChain.classType.containedScope : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>classType : any (error)
>containedScope : any (error)
                }
                else {
                    // else, use the contained scope
                    parentScope = instType.containedScope;
>parentScope = instType.containedScope : any (error)
>parentScope : any
>instType.containedScope : any (error)
>instType : any (error)
>containedScope : any (error)
                }
            }
            container = instType.symbol;
>container = instType.symbol : any (error)
>container : Symbol
>instType.symbol : any (error)
>instType : any (error)
>symbol : any (error)
        }
        else if (funcDecl.isConstructor && context.scopeChain.thisType) {
>funcDecl.isConstructor && context.scopeChain.thisType : any
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)

            // sets the container to the class type's symbol (which is shared by the instance type)
            container = context.scopeChain.thisType.symbol;
>container = context.scopeChain.thisType.symbol : any (error)
>container : Symbol
>context.scopeChain.thisType.symbol : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)
>symbol : any (error)
        }

        if (funcDecl.type == null || hasFlag(funcDecl.type.symbol.flags, SymbolFlags.TypeSetDuringScopeAssignment)) {
>funcDecl.type == null || hasFlag(funcDecl.type.symbol.flags, SymbolFlags.TypeSetDuringScopeAssignment) : any
>funcDecl.type == null : boolean
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>null : null
>hasFlag(funcDecl.type.symbol.flags, SymbolFlags.TypeSetDuringScopeAssignment) : any (error)
>hasFlag : any (error)
>funcDecl.type.symbol.flags : any (error)
>funcDecl.type.symbol : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>symbol : any (error)
>flags : any (error)
>SymbolFlags.TypeSetDuringScopeAssignment : any (error)
>SymbolFlags : any (error)
>TypeSetDuringScopeAssignment : any (error)

            if (context.scopeChain.fnc && context.scopeChain.fnc.type) {
>context.scopeChain.fnc && context.scopeChain.fnc.type : any
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)
>context.scopeChain.fnc.type : any (error)
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)
>type : any (error)

                container = context.scopeChain.fnc.type.symbol;
>container = context.scopeChain.fnc.type.symbol : any (error)
>container : Symbol
>context.scopeChain.fnc.type.symbol : any (error)
>context.scopeChain.fnc.type : any (error)
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)
>type : any (error)
>symbol : any (error)
            }

            var funcScope = null;
>funcScope : any
>null : null

            var outerFnc: FuncDecl = context.scopeChain.fnc;
>outerFnc : any (error)
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)

            var nameText = funcDecl.name ? funcDecl.name.actualText : null;
>nameText : any
>funcDecl.name ? funcDecl.name.actualText : null : any
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>funcDecl.name.actualText : any (error)
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>actualText : any (error)
>null : null

            var fgSym: TypeSymbol = null;
>fgSym : any (error)
>null : null

            if (isStatic) {
>isStatic : any (error)

                // In the case of function-nested statics, no member list will have bee initialized for the function, so we need
                // to copy it over.  We don't set this by default because having a non-null member list will throw off assignment
                // compatibility tests
                if (outerFnc.type.members == null && container.getType().memberScope) {
>outerFnc.type.members == null && container.getType().memberScope : any
>outerFnc.type.members == null : boolean
>outerFnc.type.members : any (error)
>outerFnc.type : any (error)
>outerFnc : any (error)
>type : any (error)
>members : any (error)
>null : null
>container.getType().memberScope : any (error)
>container.getType() : any (error)
>container.getType : any (error)
>container : Symbol
>getType : any (error)
>memberScope : any (error)

                    outerFnc.type.members = (<SymbolScopeBuilder>(<TypeSymbol>container).type.memberScope).valueMembers;
>outerFnc.type.members = (<SymbolScopeBuilder>(<TypeSymbol>container).type.memberScope).valueMembers : any (error)
>outerFnc.type.members : any (error)
>outerFnc.type : any (error)
>outerFnc : any (error)
>type : any (error)
>members : any (error)
>(<SymbolScopeBuilder>(<TypeSymbol>container).type.memberScope).valueMembers : any (error)
>(<SymbolScopeBuilder>(<TypeSymbol>container).type.memberScope) : any (error)
><SymbolScopeBuilder>(<TypeSymbol>container).type.memberScope : any (error)
>(<TypeSymbol>container).type.memberScope : any (error)
>(<TypeSymbol>container).type : any (error)
>(<TypeSymbol>container) : any (error)
><TypeSymbol>container : any (error)
>container : Symbol
>type : any (error)
>memberScope : any (error)
>valueMembers : any (error)
                }
                funcScope = context.scopeChain.fnc.type.memberScope;
>funcScope = context.scopeChain.fnc.type.memberScope : any (error)
>funcScope : any
>context.scopeChain.fnc.type.memberScope : any (error)
>context.scopeChain.fnc.type : any (error)
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)
>type : any (error)
>memberScope : any (error)

                outerFnc.innerStaticFuncs[outerFnc.innerStaticFuncs.length] = funcDecl;
>outerFnc.innerStaticFuncs[outerFnc.innerStaticFuncs.length] = funcDecl : any (error)
>outerFnc.innerStaticFuncs[outerFnc.innerStaticFuncs.length] : any (error)
>outerFnc.innerStaticFuncs : any (error)
>outerFnc : any (error)
>innerStaticFuncs : any (error)
>outerFnc.innerStaticFuncs.length : any (error)
>outerFnc.innerStaticFuncs : any (error)
>outerFnc : any (error)
>innerStaticFuncs : any (error)
>length : any (error)
>funcDecl : any (error)
            }
            else {

                if (!funcDecl.isConstructor &&
>!funcDecl.isConstructor &&                    container &&                    container.declAST &&                    container.declAST.nodeType == NodeType.FuncDecl &&                    (<FuncDecl>container.declAST).isConstructor &&                    !funcDecl.isMethod() : boolean
>!funcDecl.isConstructor &&                    container &&                    container.declAST &&                    container.declAST.nodeType == NodeType.FuncDecl &&                    (<FuncDecl>container.declAST).isConstructor : any
>!funcDecl.isConstructor &&                    container &&                    container.declAST &&                    container.declAST.nodeType == NodeType.FuncDecl : boolean
>!funcDecl.isConstructor &&                    container &&                    container.declAST : any
>!funcDecl.isConstructor &&                    container : Symbol
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)

                    container &&
>container : Symbol

                    container.declAST &&
>container.declAST : any (error)
>container : Symbol
>declAST : any (error)

                    container.declAST.nodeType == NodeType.FuncDecl &&
>container.declAST.nodeType == NodeType.FuncDecl : boolean
>container.declAST.nodeType : any (error)
>container.declAST : any (error)
>container : Symbol
>declAST : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)

                    (<FuncDecl>container.declAST).isConstructor &&
>(<FuncDecl>container.declAST).isConstructor : any (error)
>(<FuncDecl>container.declAST) : any (error)
><FuncDecl>container.declAST : any (error)
>container.declAST : any (error)
>container : Symbol
>declAST : any (error)
>isConstructor : any (error)

                    !funcDecl.isMethod()) {
>!funcDecl.isMethod() : boolean
>funcDecl.isMethod() : any (error)
>funcDecl.isMethod : any (error)
>funcDecl : any (error)
>isMethod : any (error)

                    funcScope = context.scopeChain.thisType.constructorScope;//locals;
>funcScope = context.scopeChain.thisType.constructorScope : any (error)
>funcScope : any
>context.scopeChain.thisType.constructorScope : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)
>constructorScope : any (error)
                }
                else {
                    funcScope = context.scopeChain.scope;
>funcScope = context.scopeChain.scope : any (error)
>funcScope : any
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)
                }
            }

            // REVIEW: We don't search for another sym for accessors to prevent us from
            // accidentally coalescing function signatures with the same name (E.g., a function
            // 'f' the outer scope and a setter 'f' in an object literal within that scope)
            if (nameText && nameText != "__missing" && !funcDecl.isAccessor()) {
>nameText && nameText != "__missing" && !funcDecl.isAccessor() : boolean
>nameText && nameText != "__missing" : boolean
>nameText : any
>nameText != "__missing" : boolean
>nameText : any
>"__missing" : "__missing"
>!funcDecl.isAccessor() : boolean
>funcDecl.isAccessor() : any (error)
>funcDecl.isAccessor : any (error)
>funcDecl : any (error)
>isAccessor : any (error)

                if (isStatic) {
>isStatic : any (error)

                    fgSym = funcScope.findLocal(nameText, false, false);
>fgSym = funcScope.findLocal(nameText, false, false) : any
>fgSym : any (error)
>funcScope.findLocal(nameText, false, false) : any
>funcScope.findLocal : any
>funcScope : any
>findLocal : any
>nameText : any
>false : false
>false : false
                }
                else {
                    // REVIEW: This logic should be symmetric with preCollectClassTypes
                    fgSym = funcScope.findLocal(nameText, false, false);
>fgSym = funcScope.findLocal(nameText, false, false) : any
>fgSym : any (error)
>funcScope.findLocal(nameText, false, false) : any
>funcScope.findLocal : any
>funcScope : any
>findLocal : any
>nameText : any
>false : false
>false : false
                }
            }

            context.typeFlow.checker.createFunctionSignature(funcDecl, container,
>context.typeFlow.checker.createFunctionSignature(funcDecl, container,                                                            funcScope, fgSym, fgSym == null) : any (error)
>context.typeFlow.checker.createFunctionSignature : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>createFunctionSignature : any (error)
>funcDecl : any (error)
>container : Symbol

                                                            funcScope, fgSym, fgSym == null);
>funcScope : any
>fgSym : any (error)
>fgSym == null : boolean
>fgSym : any (error)
>null : null

            // it's a getter or setter for a class property                     
            if (!funcDecl.accessorSymbol && 
>!funcDecl.accessorSymbol &&                 (funcDecl.fncFlags & FncFlags.ClassMethod) &&                container &&                 ((!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor()) ||                     (fgSym && fgSym.isAccessor()) : any
>!funcDecl.accessorSymbol &&                 (funcDecl.fncFlags & FncFlags.ClassMethod) &&                container &&                 ((!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor()) : any
>!funcDecl.accessorSymbol &&                 (funcDecl.fncFlags & FncFlags.ClassMethod) &&                container : Symbol
>!funcDecl.accessorSymbol &&                 (funcDecl.fncFlags & FncFlags.ClassMethod) : number
>!funcDecl.accessorSymbol : boolean
>funcDecl.accessorSymbol : any (error)
>funcDecl : any (error)
>accessorSymbol : any (error)

                (funcDecl.fncFlags & FncFlags.ClassMethod) &&
>(funcDecl.fncFlags & FncFlags.ClassMethod) : number
>funcDecl.fncFlags & FncFlags.ClassMethod : number
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.ClassMethod : any (error)
>FncFlags : any (error)
>ClassMethod : any (error)

                container && 
>container : Symbol

                ((!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor()) || 
>((!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor()) : any
>(!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) && funcDecl.isAccessor() : any
>(!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl) : boolean
>!fgSym || fgSym.declAST.nodeType != NodeType.FuncDecl : boolean
>!fgSym : boolean
>fgSym : any (error)
>fgSym.declAST.nodeType != NodeType.FuncDecl : boolean
>fgSym.declAST.nodeType : any (error)
>fgSym.declAST : any (error)
>fgSym : any (error)
>declAST : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)
>funcDecl.isAccessor() : any (error)
>funcDecl.isAccessor : any (error)
>funcDecl : any (error)
>isAccessor : any (error)

                    (fgSym && fgSym.isAccessor())) 
>(fgSym && fgSym.isAccessor()) : any
>fgSym && fgSym.isAccessor() : any
>fgSym : any (error)
>fgSym.isAccessor() : any (error)
>fgSym.isAccessor : any (error)
>fgSym : any (error)
>isAccessor : any (error)
            {
                funcDecl.accessorSymbol = context.typeFlow.checker.createAccessorSymbol(funcDecl, fgSym, container.getType(), (funcDecl.isMethod() && isStatic), true, funcScope, container);
>funcDecl.accessorSymbol = context.typeFlow.checker.createAccessorSymbol(funcDecl, fgSym, container.getType(), (funcDecl.isMethod() && isStatic), true, funcScope, container) : any (error)
>funcDecl.accessorSymbol : any (error)
>funcDecl : any (error)
>accessorSymbol : any (error)
>context.typeFlow.checker.createAccessorSymbol(funcDecl, fgSym, container.getType(), (funcDecl.isMethod() && isStatic), true, funcScope, container) : any (error)
>context.typeFlow.checker.createAccessorSymbol : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>createAccessorSymbol : any (error)
>funcDecl : any (error)
>fgSym : any (error)
>container.getType() : any (error)
>container.getType : any (error)
>container : Symbol
>getType : any (error)
>(funcDecl.isMethod() && isStatic) : any
>funcDecl.isMethod() && isStatic : any
>funcDecl.isMethod() : any (error)
>funcDecl.isMethod : any (error)
>funcDecl : any (error)
>isMethod : any (error)
>isStatic : any (error)
>true : true
>funcScope : any
>container : Symbol
            }

            funcDecl.type.symbol.flags |= SymbolFlags.TypeSetDuringScopeAssignment;
>funcDecl.type.symbol.flags |= SymbolFlags.TypeSetDuringScopeAssignment : number
>funcDecl.type.symbol.flags : any (error)
>funcDecl.type.symbol : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>symbol : any (error)
>flags : any (error)
>SymbolFlags.TypeSetDuringScopeAssignment : any (error)
>SymbolFlags : any (error)
>TypeSetDuringScopeAssignment : any (error)
        }

        // Set the symbol for functions and their overloads
        if (funcDecl.name && funcDecl.type) {
>funcDecl.name && funcDecl.type : any
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)

            funcDecl.name.sym = funcDecl.type.symbol;
>funcDecl.name.sym = funcDecl.type.symbol : any (error)
>funcDecl.name.sym : any (error)
>funcDecl.name : any (error)
>funcDecl : any (error)
>name : any (error)
>sym : any (error)
>funcDecl.type.symbol : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)
>symbol : any (error)
        }

        // Keep track of the original scope type, because target typing might override
        // the "type" member. We need the original "Scope type" for completion list, etc.
        funcDecl.scopeType = funcDecl.type;
>funcDecl.scopeType = funcDecl.type : any (error)
>funcDecl.scopeType : any (error)
>funcDecl : any (error)
>scopeType : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)

        // Overloads have no scope, so bail here
        if (funcDecl.isOverload) {
>funcDecl.isOverload : any (error)
>funcDecl : any (error)
>isOverload : any (error)

            return;
        }

        var funcTable = new StringHashTable();
>funcTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var funcMembers = new ScopedMembers(new DualStringHashTable(funcTable, new StringHashTable()));
>funcMembers : any (error)
>new ScopedMembers(new DualStringHashTable(funcTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(funcTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>funcTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var ambientFuncTable = new StringHashTable();
>ambientFuncTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var ambientFuncMembers = new ScopedMembers(new DualStringHashTable(ambientFuncTable, new StringHashTable()));
>ambientFuncMembers : any (error)
>new ScopedMembers(new DualStringHashTable(ambientFuncTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(ambientFuncTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>ambientFuncTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var funcStaticTable = new StringHashTable();
>funcStaticTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var funcStaticMembers = new ScopedMembers(new DualStringHashTable(funcStaticTable, new StringHashTable()));
>funcStaticMembers : any (error)
>new ScopedMembers(new DualStringHashTable(funcStaticTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(funcStaticTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>funcStaticTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var ambientFuncStaticTable = new StringHashTable();
>ambientFuncStaticTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        var ambientFuncStaticMembers = new ScopedMembers(new DualStringHashTable(ambientFuncStaticTable, new StringHashTable()));
>ambientFuncStaticMembers : any (error)
>new ScopedMembers(new DualStringHashTable(ambientFuncStaticTable, new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(ambientFuncStaticTable, new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>ambientFuncStaticTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

        // REVIEW: Is it a problem that this is being set twice for properties and constructors?
        funcDecl.unitIndex = context.typeFlow.checker.locationInfo.unitIndex;
>funcDecl.unitIndex = context.typeFlow.checker.locationInfo.unitIndex : any (error)
>funcDecl.unitIndex : any (error)
>funcDecl : any (error)
>unitIndex : any (error)
>context.typeFlow.checker.locationInfo.unitIndex : any (error)
>context.typeFlow.checker.locationInfo : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>locationInfo : any (error)
>unitIndex : any (error)

        var locals = new SymbolScopeBuilder(funcMembers, ambientFuncMembers, null, null, parentScope, localContainer);
>locals : any (error)
>new SymbolScopeBuilder(funcMembers, ambientFuncMembers, null, null, parentScope, localContainer) : any (error)
>SymbolScopeBuilder : any (error)
>funcMembers : any (error)
>ambientFuncMembers : any (error)
>null : null
>null : null
>parentScope : any
>localContainer : Symbol

        var statics = new SymbolScopeBuilder(funcStaticMembers, ambientFuncStaticMembers, null, null, parentScope, null);
>statics : any (error)
>new SymbolScopeBuilder(funcStaticMembers, ambientFuncStaticMembers, null, null, parentScope, null) : any (error)
>SymbolScopeBuilder : any (error)
>funcStaticMembers : any (error)
>ambientFuncStaticMembers : any (error)
>null : null
>null : null
>parentScope : any
>null : null

        if (funcDecl.isConstructor && context.scopeChain.thisType) {
>funcDecl.isConstructor && context.scopeChain.thisType : any
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)

            context.scopeChain.thisType.constructorScope = locals;
>context.scopeChain.thisType.constructorScope = locals : any (error)
>context.scopeChain.thisType.constructorScope : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)
>constructorScope : any (error)
>locals : any (error)
        }

        // basically, there are two problems
        // - Above, for new classes, we were overwriting the constructor scope with the containing scope.  This caused constructor params to be
        // in scope everywhere
        // - Below, we're setting the contained scope table to the same table we were overwriting the constructor scope with, which we need to
        // fish lambda params, etc, out (see funcTable below)
        //
        // A good first approach to solving this would be to change addLocalsFromScope to take a scope instead of a table, and add to the
        // constructor scope as appropriate

        funcDecl.symbols = funcTable;
>funcDecl.symbols = funcTable : any (error)
>funcDecl.symbols : any (error)
>funcDecl : any (error)
>symbols : any (error)
>funcTable : any (error)

        if (!funcDecl.isSpecialFn()) {
>!funcDecl.isSpecialFn() : boolean
>funcDecl.isSpecialFn() : any (error)
>funcDecl.isSpecialFn : any (error)
>funcDecl : any (error)
>isSpecialFn : any (error)

            var group = funcDecl.type;
>group : any (error)
>funcDecl.type : any (error)
>funcDecl : any (error)
>type : any (error)

            var signature = funcDecl.signature;
>signature : any (error)
>funcDecl.signature : any (error)
>funcDecl : any (error)
>signature : any (error)

            if (!funcDecl.isConstructor) {
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)

                group.containedScope = locals;
>group.containedScope = locals : any (error)
>group.containedScope : any (error)
>group : any (error)
>containedScope : any (error)
>locals : any (error)

                locals.container = group.symbol;
>locals.container = group.symbol : any (error)
>locals.container : any (error)
>locals : any (error)
>container : any (error)
>group.symbol : any (error)
>group : any (error)
>symbol : any (error)

                group.memberScope = statics;
>group.memberScope = statics : any (error)
>group.memberScope : any (error)
>group : any (error)
>memberScope : any (error)
>statics : any (error)

                statics.container = group.symbol;
>statics.container = group.symbol : any (error)
>statics.container : any (error)
>statics : any (error)
>container : any (error)
>group.symbol : any (error)
>group : any (error)
>symbol : any (error)
            }
            funcDecl.enclosingFnc = context.scopeChain.fnc;
>funcDecl.enclosingFnc = context.scopeChain.fnc : any (error)
>funcDecl.enclosingFnc : any (error)
>funcDecl : any (error)
>enclosingFnc : any (error)
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)

            group.enclosingType = isStatic ? context.scopeChain.classType : context.scopeChain.thisType;
>group.enclosingType = isStatic ? context.scopeChain.classType : context.scopeChain.thisType : any
>group.enclosingType : any (error)
>group : any (error)
>enclosingType : any (error)
>isStatic ? context.scopeChain.classType : context.scopeChain.thisType : any
>isStatic : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>classType : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)

            // for mapping when type checking
            var fgSym = <TypeSymbol>ast.type.symbol;
>fgSym : any (error)
><TypeSymbol>ast.type.symbol : any (error)
>ast.type.symbol : any (error)
>ast.type : any (error)
>ast : any (error)
>type : any (error)
>symbol : any (error)

            if (((funcDecl.fncFlags & FncFlags.Signature) == FncFlags.None) && funcDecl.vars) {
>((funcDecl.fncFlags & FncFlags.Signature) == FncFlags.None) && funcDecl.vars : any
>((funcDecl.fncFlags & FncFlags.Signature) == FncFlags.None) : boolean
>(funcDecl.fncFlags & FncFlags.Signature) == FncFlags.None : boolean
>(funcDecl.fncFlags & FncFlags.Signature) : number
>funcDecl.fncFlags & FncFlags.Signature : number
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.Signature : any (error)
>FncFlags : any (error)
>Signature : any (error)
>FncFlags.None : any (error)
>FncFlags : any (error)
>None : any (error)
>funcDecl.vars : any (error)
>funcDecl : any (error)
>vars : any (error)

                context.typeFlow.addLocalsFromScope(locals, fgSym, funcDecl.vars,
>context.typeFlow.addLocalsFromScope(locals, fgSym, funcDecl.vars,                                                    funcTable, false) : any (error)
>context.typeFlow.addLocalsFromScope : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>addLocalsFromScope : any (error)
>locals : any (error)
>fgSym : any (error)
>funcDecl.vars : any (error)
>funcDecl : any (error)
>vars : any (error)

                                                    funcTable, false);
>funcTable : any (error)
>false : false

                context.typeFlow.addLocalsFromScope(statics, fgSym, funcDecl.statics,
>context.typeFlow.addLocalsFromScope(statics, fgSym, funcDecl.statics,                                                    funcStaticTable, false) : any (error)
>context.typeFlow.addLocalsFromScope : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>addLocalsFromScope : any (error)
>statics : any (error)
>fgSym : any (error)
>funcDecl.statics : any (error)
>funcDecl : any (error)
>statics : any (error)

                                                    funcStaticTable, false);
>funcStaticTable : any (error)
>false : false
            }
            if (signature.parameters) {
>signature.parameters : any (error)
>signature : any (error)
>parameters : any (error)

                var len = signature.parameters.length;
>len : any (error)
>signature.parameters.length : any (error)
>signature.parameters : any (error)
>signature : any (error)
>parameters : any (error)
>length : any (error)

                for (var i = 0; i < len; i++) {
>i : number
>0 : 0
>i < len : boolean
>i : number
>len : any (error)
>i++ : number
>i : number

                    var paramSym: ParameterSymbol = signature.parameters[i];
>paramSym : any (error)
>signature.parameters[i] : any (error)
>signature.parameters : any (error)
>signature : any (error)
>parameters : any (error)
>i : number

                    context.typeFlow.checker.resolveTypeLink(locals,
>context.typeFlow.checker.resolveTypeLink(locals,                                                                paramSym.parameter.typeLink, true) : any (error)
>context.typeFlow.checker.resolveTypeLink : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>resolveTypeLink : any (error)
>locals : any (error)

                                                                paramSym.parameter.typeLink, true);
>paramSym.parameter.typeLink : any (error)
>paramSym.parameter : any (error)
>paramSym : any (error)
>parameter : any (error)
>typeLink : any (error)
>true : true
                }
            }
            context.typeFlow.checker.resolveTypeLink(locals, signature.returnType,
>context.typeFlow.checker.resolveTypeLink(locals, signature.returnType,                                                        funcDecl.isSignature()) : any (error)
>context.typeFlow.checker.resolveTypeLink : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>resolveTypeLink : any (error)
>locals : any (error)
>signature.returnType : any (error)
>signature : any (error)
>returnType : any (error)

                                                        funcDecl.isSignature());
>funcDecl.isSignature() : any (error)
>funcDecl.isSignature : any (error)
>funcDecl : any (error)
>isSignature : any (error)
        }

        if (!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) {
>!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.ClassMethod : any (error)
>FncFlags : any (error)
>ClassMethod : any (error)

            var thisType = (funcDecl.isConstructor && hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) ? context.scopeChain.thisType : null;
>thisType : any
>(funcDecl.isConstructor && hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) ? context.scopeChain.thisType : null : any
>(funcDecl.isConstructor && hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) : any
>funcDecl.isConstructor && hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.ClassMethod : any (error)
>FncFlags : any (error)
>ClassMethod : any (error)
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)
>null : null

            pushAssignScope(locals, context, thisType, null, funcDecl);
>pushAssignScope(locals, context, thisType, null, funcDecl) : void
>pushAssignScope : (scope: any (error), context: AssignScopeContext, type: any (error), classType: any (error), fnc: any (error)) => void
>locals : any (error)
>context : AssignScopeContext
>thisType : any
>null : null
>funcDecl : any (error)
        }
    }

    export function preAssignCatchScopes(ast: AST, context: AssignScopeContext) {
>preAssignCatchScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext

        var catchBlock = <Catch>ast;
>catchBlock : any (error)
><Catch>ast : any (error)
>ast : any (error)

        if (catchBlock.param) {
>catchBlock.param : any (error)
>catchBlock : any (error)
>param : any (error)

            var catchTable = new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())); // REVIEW: Should we be allocating a public table instead of a private one?
>catchTable : any (error)
>new ScopedMembers(new DualStringHashTable(new StringHashTable(), new StringHashTable())) : any (error)
>ScopedMembers : any (error)
>new DualStringHashTable(new StringHashTable(), new StringHashTable()) : any (error)
>DualStringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)
>new StringHashTable() : any (error)
>StringHashTable : any (error)

            var catchLocals = new SymbolScopeBuilder(catchTable, null, null, null, context.scopeChain.scope,
>catchLocals : any (error)
>new SymbolScopeBuilder(catchTable, null, null, null, context.scopeChain.scope,                                                   context.scopeChain.scope.container) : any (error)
>SymbolScopeBuilder : any (error)
>catchTable : any (error)
>null : null
>null : null
>null : null
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)

                                                   context.scopeChain.scope.container);
>context.scopeChain.scope.container : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)
>container : any (error)

            catchBlock.containedScope = catchLocals;
>catchBlock.containedScope = catchLocals : any (error)
>catchBlock.containedScope : any (error)
>catchBlock : any (error)
>containedScope : any (error)
>catchLocals : any (error)

            pushAssignScope(catchLocals, context, context.scopeChain.thisType, context.scopeChain.classType, context.scopeChain.fnc);
>pushAssignScope(catchLocals, context, context.scopeChain.thisType, context.scopeChain.classType, context.scopeChain.fnc) : void
>pushAssignScope : (scope: any (error), context: AssignScopeContext, type: any (error), classType: any (error), fnc: any (error)) => void
>catchLocals : any (error)
>context : AssignScopeContext
>context.scopeChain.thisType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>thisType : any (error)
>context.scopeChain.classType : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>classType : any (error)
>context.scopeChain.fnc : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>fnc : any (error)
        }
    }

    export function preAssignScopes(ast: AST, parent: AST, walker: IAstWalker) {
>preAssignScopes : (ast: any (error), parent: any (error), walker: any (error)) => any (error)
>ast : any (error)
>parent : any (error)
>walker : any (error)

        var context:AssignScopeContext = walker.state;
>context : AssignScopeContext
>walker.state : any (error)
>walker : any (error)
>state : any (error)

        var go = true;
>go : boolean
>true : true

        if (ast) {
>ast : any (error)

            if (ast.nodeType == NodeType.List) {
>ast.nodeType == NodeType.List : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.List : any (error)
>NodeType : any (error)
>List : any (error)

                var list = <ASTList>ast;
>list : any (error)
><ASTList>ast : any (error)
>ast : any (error)

                list.enclosingScope = context.scopeChain.scope;
>list.enclosingScope = context.scopeChain.scope : any (error)
>list.enclosingScope : any (error)
>list : any (error)
>enclosingScope : any (error)
>context.scopeChain.scope : any (error)
>context.scopeChain : any (error)
>context : AssignScopeContext
>scopeChain : any (error)
>scope : any (error)
            }
            else if (ast.nodeType == NodeType.ModuleDeclaration) {
>ast.nodeType == NodeType.ModuleDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ModuleDeclaration : any (error)
>NodeType : any (error)
>ModuleDeclaration : any (error)

                preAssignModuleScopes(ast, context);
>preAssignModuleScopes(ast, context) : void
>preAssignModuleScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.ClassDeclaration) {
>ast.nodeType == NodeType.ClassDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ClassDeclaration : any (error)
>NodeType : any (error)
>ClassDeclaration : any (error)

                preAssignClassScopes(ast, context);
>preAssignClassScopes(ast, context) : void
>preAssignClassScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.InterfaceDeclaration) {
>ast.nodeType == NodeType.InterfaceDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.InterfaceDeclaration : any (error)
>NodeType : any (error)
>InterfaceDeclaration : any (error)

                preAssignInterfaceScopes(ast, context);
>preAssignInterfaceScopes(ast, context) : void
>preAssignInterfaceScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.With) {
>ast.nodeType == NodeType.With : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.With : any (error)
>NodeType : any (error)
>With : any (error)

                preAssignWithScopes(ast, context);
>preAssignWithScopes(ast, context) : void
>preAssignWithScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.FuncDecl) {
>ast.nodeType == NodeType.FuncDecl : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)

                preAssignFuncDeclScopes(ast, context);
>preAssignFuncDeclScopes(ast, context) : void
>preAssignFuncDeclScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.Catch) {
>ast.nodeType == NodeType.Catch : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.Catch : any (error)
>NodeType : any (error)
>Catch : any (error)

                preAssignCatchScopes(ast, context);
>preAssignCatchScopes(ast, context) : void
>preAssignCatchScopes : (ast: any (error), context: AssignScopeContext) => void
>ast : any (error)
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.TypeRef) {
>ast.nodeType == NodeType.TypeRef : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.TypeRef : any (error)
>NodeType : any (error)
>TypeRef : any (error)

                go = false;
>go = false : false
>go : boolean
>false : false
            }
        }
        walker.options.goChildren = go;
>walker.options.goChildren = go : boolean
>walker.options.goChildren : any (error)
>walker.options : any (error)
>walker : any (error)
>options : any (error)
>goChildren : any (error)
>go : boolean

        return ast;
>ast : any (error)
    }

    export function postAssignScopes(ast: AST, parent: AST, walker: IAstWalker) {
>postAssignScopes : (ast: any (error), parent: any (error), walker: any (error)) => any (error)
>ast : any (error)
>parent : any (error)
>walker : any (error)

        var context:AssignScopeContext = walker.state;
>context : AssignScopeContext
>walker.state : any (error)
>walker : any (error)
>state : any (error)

        var go = true;
>go : boolean
>true : true

        if (ast) {
>ast : any (error)

            if (ast.nodeType == NodeType.ModuleDeclaration) {
>ast.nodeType == NodeType.ModuleDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ModuleDeclaration : any (error)
>NodeType : any (error)
>ModuleDeclaration : any (error)

                var prevModDecl = <ModuleDeclaration>ast;
>prevModDecl : any (error)
><ModuleDeclaration>ast : any (error)
>ast : any (error)

                popAssignScope(context);
>popAssignScope(context) : void
>popAssignScope : (context: AssignScopeContext) => void
>context : AssignScopeContext

                context.modDeclChain.pop();
>context.modDeclChain.pop() : any (error)
>context.modDeclChain.pop : () => any (error)
>context.modDeclChain : any (error)[]
>context : AssignScopeContext
>modDeclChain : any (error)[]
>pop : () => any (error)

                if (context.modDeclChain.length >= 1) {
>context.modDeclChain.length >= 1 : boolean
>context.modDeclChain.length : number
>context.modDeclChain : any (error)[]
>context : AssignScopeContext
>modDeclChain : any (error)[]
>length : number
>1 : 1

                    context.typeFlow.checker.currentModDecl = context.modDeclChain[context.modDeclChain.length - 1];
>context.typeFlow.checker.currentModDecl = context.modDeclChain[context.modDeclChain.length - 1] : any (error)
>context.typeFlow.checker.currentModDecl : any (error)
>context.typeFlow.checker : any (error)
>context.typeFlow : any (error)
>context : AssignScopeContext
>typeFlow : any (error)
>checker : any (error)
>currentModDecl : any (error)
>context.modDeclChain[context.modDeclChain.length - 1] : any (error)
>context.modDeclChain : any (error)[]
>context : AssignScopeContext
>modDeclChain : any (error)[]
>context.modDeclChain.length - 1 : number
>context.modDeclChain.length : number
>context.modDeclChain : any (error)[]
>context : AssignScopeContext
>modDeclChain : any (error)[]
>length : number
>1 : 1
                }
            }
            else if (ast.nodeType == NodeType.ClassDeclaration) {
>ast.nodeType == NodeType.ClassDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.ClassDeclaration : any (error)
>NodeType : any (error)
>ClassDeclaration : any (error)

                popAssignScope(context);
>popAssignScope(context) : void
>popAssignScope : (context: AssignScopeContext) => void
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.InterfaceDeclaration) {
>ast.nodeType == NodeType.InterfaceDeclaration : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.InterfaceDeclaration : any (error)
>NodeType : any (error)
>InterfaceDeclaration : any (error)

                popAssignScope(context);
>popAssignScope(context) : void
>popAssignScope : (context: AssignScopeContext) => void
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.With) {
>ast.nodeType == NodeType.With : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.With : any (error)
>NodeType : any (error)
>With : any (error)

                popAssignScope(context);
>popAssignScope(context) : void
>popAssignScope : (context: AssignScopeContext) => void
>context : AssignScopeContext
            }
            else if (ast.nodeType == NodeType.FuncDecl) {
>ast.nodeType == NodeType.FuncDecl : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.FuncDecl : any (error)
>NodeType : any (error)
>FuncDecl : any (error)

                var funcDecl = <FuncDecl>ast;
>funcDecl : any (error)
><FuncDecl>ast : any (error)
>ast : any (error)

                if ((!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) && !funcDecl.isOverload) {
>(!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) && !funcDecl.isOverload : boolean
>(!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod)) : any
>!funcDecl.isConstructor || hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any
>!funcDecl.isConstructor : boolean
>funcDecl.isConstructor : any (error)
>funcDecl : any (error)
>isConstructor : any (error)
>hasFlag(funcDecl.fncFlags, FncFlags.ClassMethod) : any (error)
>hasFlag : any (error)
>funcDecl.fncFlags : any (error)
>funcDecl : any (error)
>fncFlags : any (error)
>FncFlags.ClassMethod : any (error)
>FncFlags : any (error)
>ClassMethod : any (error)
>!funcDecl.isOverload : boolean
>funcDecl.isOverload : any (error)
>funcDecl : any (error)
>isOverload : any (error)

                    popAssignScope(context);
>popAssignScope(context) : void
>popAssignScope : (context: AssignScopeContext) => void
>context : AssignScopeContext
                }
            }
            else if (ast.nodeType == NodeType.Catch) {
>ast.nodeType == NodeType.Catch : boolean
>ast.nodeType : any (error)
>ast : any (error)
>nodeType : any (error)
>NodeType.Catch : any (error)
>NodeType : any (error)
>Catch : any (error)

                var catchBlock = <Catch>ast;
>catchBlock : any (error)
><Catch>ast : any (error)
>ast : any (error)

                if (catchBlock.param) {
>catchBlock.param : any (error)
>catchBlock : any (error)
>param : any (error)

                    popAssignScope(context);
>popAssignScope(context) : void
>popAssignScope : (context: AssignScopeContext) => void
>context : AssignScopeContext
                }
            }
            else {
                go = false;
>go = false : false
>go : boolean
>false : false
            }
        }
        walker.options.goChildren = go;
>walker.options.goChildren = go : boolean
>walker.options.goChildren : any (error)
>walker.options : any (error)
>walker : any (error)
>options : any (error)
>goChildren : any (error)
>go : boolean

        return ast;
>ast : any (error)
    }
}
