=== tests/cases/conformance/parser/ecmascript5/RegularExpressions/parserRegularExpression2.ts ===
href.match(/:\/\/(.[^/]+)/)[1];
>href.match(/:\/\/(.[^/]+)/)[1] : any (error)
>href.match(/:\/\/(.[^/]+)/) : any (error)
>href.match : any (error)
>href : any (error)
>match : any (error)
>/:\/\/(.[^/]+)/ : RegExp
>1 : 1

