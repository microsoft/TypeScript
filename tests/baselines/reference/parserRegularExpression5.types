=== tests/cases/conformance/parser/ecmascript5/RegularExpressions/parserRegularExpression5.ts ===
if (a) / (\\ ? | & ) rct = j / .test(c.href);
>a : any
>/ (\\ ? | & ) rct = j / .test(c.href) : boolean
>/ (\\ ? | & ) rct = j / .test : (string: string) => string is / (\\ ? | & ) rct = j /
>/ (\\ ? | & ) rct = j / : RegExp</ (\\ ? | & ) rct = j />
>test : (string: string) => string is / (\\ ? | & ) rct = j /
>c.href : any
>c : any
>href : any

