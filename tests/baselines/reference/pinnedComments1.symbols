=== tests/cases/compiler/pinnedComments1.ts ===

/* unpinned comment */
/*! pinned comment */
class C {
>C : Symbol(C, Decl(pinnedComments1.ts, 0, 0))
}

function foo() {
>foo : Symbol(foo, Decl(pinnedComments1.ts, 4, 1))

	/* unpinned comment */
	/*! pinned comment */
}
