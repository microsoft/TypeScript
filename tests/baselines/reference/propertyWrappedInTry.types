=== tests/cases/compiler/propertyWrappedInTry.ts ===
class Foo {
>Foo : Foo

    try {

        public bar = someInitThatMightFail();
>bar = someInitThatMightFail() : any (error)
>bar : any (error)
>someInitThatMightFail() : any (error)
>someInitThatMightFail : any (error)

    } catch(e) {}
>e : any



    public baz() {
>baz() : any (error)
>baz : any (error)

        return this.bar; // doesn't get rewritten to Foo.bar.
>this.bar : any
>this : any
>bar : any

    }

}


