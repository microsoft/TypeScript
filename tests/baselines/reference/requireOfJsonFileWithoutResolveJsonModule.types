=== tests/cases/compiler/file1.ts ===
import b1 = require('./b.json'); // error
>b1 : any (error)

let x = b1.a;
>x : any (error)
>b1.a : any (error)
>b1 : any (error)
>a : any (error)

import b2 = require('./b.json'); // error
>b2 : any (error)

if (x) {
>x : any (error)

    let b = b2.b;
>b : any (error)
>b2.b : any (error)
>b2 : any (error)
>b : any (error)

    x = (b1.b === b);
>x = (b1.b === b) : boolean
>x : any (error)
>(b1.b === b) : boolean
>b1.b === b : boolean
>b1.b : any (error)
>b1 : any (error)
>b : any (error)
>b : any (error)
}

