=== tests/cases/compiler/reversedWordsWithBindingElement.ts ===
interface Foo {
>Foo : Foo

    enum: string;
>enum : string

    case: number;
>case : number
}

function bar({ enum, case }: Foo) {
>bar : ({ enum, case }: Foo) => void
>enum : string
>case : number
>Foo : Foo

    if (enum && case) {
> : any
> : (Missing)
>&& : any
> : any
> : any

        //
    }
}

