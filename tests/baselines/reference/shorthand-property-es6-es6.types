=== tests/cases/compiler/test.ts ===
import {foo} from './foo';
>foo : any (error)

const baz = 42;
>baz : 42
>42 : 42

const bar = { foo, baz };
>bar : { foo: any (error); baz: number; }
>{ foo, baz } : { foo: any (error); baz: number; }
>foo : any (error)
>baz : number

