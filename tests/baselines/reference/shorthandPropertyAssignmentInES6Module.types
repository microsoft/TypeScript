=== tests/cases/compiler/existingModule.ts ===
export var x = 1;
>x : number
>1 : 1

=== tests/cases/compiler/test.ts ===
import {x} from './existingModule';
>x : number

import {foo} from './missingModule';
>foo : any (error)

declare function use(a: any): void;
>use : (a: any) => void
>a : any

const test = { x, foo };
>test : { x: number; foo: any (error); }
>{ x, foo } : { x: number; foo: any (error); }
>x : number
>foo : any (error)

use(x);
>use(x) : void
>use : (a: any) => void
>x : number

use(foo);
>use(foo) : void
>use : (a: any) => void
>foo : any (error)

