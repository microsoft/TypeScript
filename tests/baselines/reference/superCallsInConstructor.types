=== tests/cases/compiler/superCallsInConstructor.ts ===
class C {
>C : C

    foo() {}
>foo : () => void

    bar() {}
>bar : () => void
}

class Base {
>Base : Base

    x: string;
>x : string
}
 
class Derived extends Base {
>Derived : Derived
>Base : Base

    constructor() {
        with(new C()) {
>new C() : C
>C : typeof C

            foo();
>foo() : any (error)
>foo : any (error)

            super();
>super() : any (error)
>super : any (error)

            bar();
>bar() : any (error)
>bar : any (error)
        }

        try {} catch(e) { super(); }
>e : any
>super() : void
>super : typeof Base
    }
}
