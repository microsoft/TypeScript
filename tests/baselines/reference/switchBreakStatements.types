=== tests/cases/conformance/statements/breakStatements/switchBreakStatements.ts ===
switch ('') {
>'' : ""

    case 'a':
>'a' : "a"

        break;
}

ONE:
>ONE : any (error)

switch ('') {
>'' : ""

    case 'a':
>'a' : "a"

        break ONE;
>ONE : any (error)
}

TWO:
>TWO : any (error)

THREE:
>THREE : any (error)

switch ('') {
>'' : ""

    case 'a':
>'a' : "a"

        break THREE;
>THREE : any (error)
}

FOUR:
>FOUR : any (error)

switch ('') {
>'' : ""

    case 'a':
>'a' : "a"

        FIVE:
>FIVE : any (error)

        switch ('') {
>'' : ""

            case 'a':
>'a' : "a"

                break FOUR;
>FOUR : any (error)
        }
}

switch ('') {
>'' : ""

    case 'a':
>'a' : "a"

        SIX:
>SIX : any (error)

        switch ('') {
>'' : ""

            case 'a':
>'a' : "a"

                break SIX;
>SIX : any (error)
        }
}

SEVEN:
>SEVEN : any (error)

switch ('') {
>'' : ""

    case 'a':
>'a' : "a"

        switch ('') {
>'' : ""

            case 'a':
>'a' : "a"

                switch ('') {
>'' : ""

                    case 'a':
>'a' : "a"

                        break SEVEN;
>SEVEN : any (error)

                        EIGHT:
>EIGHT : any (error)

                        switch ('') {
>'' : ""

                            case 'a':
>'a' : "a"

                                var fn = function () { }
>fn : () => void
>function () { } : () => void

                                break EIGHT;
>EIGHT : any (error)
                        }
                }
        }
}

