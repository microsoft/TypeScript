=== tests/cases/compiler/systemModule10.ts ===
import n, {x} from 'file1'
>n : any (error)
>x : any (error)

import n2 = require('file2');
>n2 : any (error)

export {x}
>x : any (error)

export {x as y}
>x : any (error)
>y : any (error)

export {n}
>n : any (error)

export {n as n1}
>n : any (error)
>n1 : any (error)

export {n2}
>n2 : any (error)

export {n2 as n3}
>n2 : any (error)
>n3 : any (error)

