=== tests/cases/compiler/systemModule14.ts ===
function foo() {
>foo : () => any (error)

    return a;
>a : any (error)
}

import {a} from "foo";
>a : any (error)

export {foo}
>foo : () => any (error)

var x = 1;
>x : number
>1 : 1

export {foo as b}
>foo : () => any (error)
>b : () => any (error)

