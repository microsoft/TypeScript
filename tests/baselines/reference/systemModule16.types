=== tests/cases/compiler/systemModule16.ts ===
import * as x from "foo";
>x : any (error)

import * as y from "bar";
>y : any (error)

export * from "foo";
export * from "bar"
export {x}
>x : any (error)

export {y}
>y : any (error)

import {a1, b1, c1 as d1} from "foo";
>a1 : any (error)
>b1 : any (error)
>c1 : any (error)
>d1 : any (error)

export {a2, b2, c2 as d2} from "bar";
>a2 : any (error)
>b2 : any (error)
>c2 : any (error)
>d2 : any (error)

x,y,a1,b1,d1;
>x,y,a1,b1,d1 : any (error)
>x,y,a1,b1 : any (error)
>x,y,a1 : any (error)
>x,y : any (error)
>x : any (error)
>y : any (error)
>a1 : any (error)
>b1 : any (error)
>d1 : any (error)

