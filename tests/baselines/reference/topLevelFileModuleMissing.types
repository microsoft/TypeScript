=== tests/cases/conformance/externalModules/foo_1.ts ===
import foo = require("vs/foo");
>foo : any (error)

var z = foo.x + 10;
>z : any (error)
>foo.x + 10 : any (error)
>foo.x : any (error)
>foo : any (error)
>x : any (error)
>10 : 10

