//// [tests/cases/compiler/topLevelLambda.ts] ////

=== topLevelLambda.ts ===
namespace M {
>M : Symbol(M, Decl(topLevelLambda.ts, 0, 0))

	var f = () => {this.window;}
>f : Symbol(f, Decl(topLevelLambda.ts, 1, 4))
}

