=== tests/cases/compiler/typeParametersInStaticMethods.ts ===
class foo<T> {
>foo : foo<T>

    static M(x: (x: T) => { x: { y: T } }) {
>M : (x: (x: any (error)) => { x: { y: any (error); }; }) => void
>x : (x: any (error)) => { x: { y: any (error); }; }
>x : any (error)
>x : { y: any (error); }
>y : any (error)
    }
} 
