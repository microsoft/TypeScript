//// [typedArrays-es5.ts]
const float32Array = new Float32Array(1);
[...float32Array];

const float64Array = new Float64Array(1);
[...float64Array];

const int16Array = new Int16Array(1);
[...int16Array];

const int32Array = new Int32Array(1);
[...int32Array];

const int8Array = new Int8Array(1);
[...int8Array];

const nodeList = new NodeList();
[...nodeList];

const uint16Array = new Uint16Array(1);
[...uint16Array];

const uint32Array = new Uint32Array(1);
[...uint32Array];

const uint8Array = new Uint8Array(1);
[...uint8Array];

const uint8ClampedArray = new Uint8ClampedArray(1);
[...uint8ClampedArray];




//// [typedArrays-es5.js]
var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
var float32Array = new Float32Array(1);
__spreadArrays(float32Array);
var float64Array = new Float64Array(1);
__spreadArrays(float64Array);
var int16Array = new Int16Array(1);
__spreadArrays(int16Array);
var int32Array = new Int32Array(1);
__spreadArrays(int32Array);
var int8Array = new Int8Array(1);
__spreadArrays(int8Array);
var nodeList = new NodeList();
__spreadArrays(nodeList);
var uint16Array = new Uint16Array(1);
__spreadArrays(uint16Array);
var uint32Array = new Uint32Array(1);
__spreadArrays(uint32Array);
var uint8Array = new Uint8Array(1);
__spreadArrays(uint8Array);
var uint8ClampedArray = new Uint8ClampedArray(1);
__spreadArrays(uint8ClampedArray);
