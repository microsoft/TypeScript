=== tests/cases/conformance/externalModules/a.ts ===
Foo.fn();
>Foo.fn() : any (error)
>Foo.fn : any (error)
>Foo : any (error)
>fn : any (error)

let x: Foo.Thing;
>x : any (error)
>Foo : any (error)

let y: number = x.n;
>y : number
>x.n : any (error)
>x : any (error)
>n : any (error)

