=== tests/cases/compiler/unaryOperatorsInStrictMode.ts ===
"use strict"
>"use strict" : "use strict"

++eval;
>++eval : number
>eval : any (error)

--eval;
>--eval : number
>eval : any (error)

++arguments;
>++arguments : number
>arguments : any (error)

--arguments;
>--arguments : number
>arguments : any (error)

eval++;
>eval++ : number
>eval : any (error)

eval--;
>eval-- : number
>eval : any (error)

arguments++;
>arguments++ : number
>arguments : any (error)

arguments--;
>arguments-- : number
>arguments : any (error)

