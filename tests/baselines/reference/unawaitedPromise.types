=== tests/cases/compiler/unawaitedPromise.ts ===
declare function doThing(): Promise<string>;
>doThing : () => Promise<string>

function f() {
>f : () => void

    // Should error
    doThing();
>doThing() : Promise<string>
>doThing : () => Promise<string>

    let m: Promise<string>;
>m : Promise<string>

    // Should not error
    m = doThing();
>m = doThing() : Promise<string>
>m : Promise<string>
>doThing() : Promise<string>
>doThing : () => Promise<string>

    // Should not error
    void doThing();
>void doThing() : undefined
>doThing() : Promise<string>
>doThing : () => Promise<string>
}
