=== tests/cases/compiler/uncaughtCompilerError2.ts ===
function getObj() {
>getObj : () => void

   ().toString();
>().toString() : any (error)
>().toString : any (error)
>() : any (error)
> : any (error)
>toString : any (error)
}

