=== tests/cases/compiler/unknownSymbols1.ts ===
var x = asdf;
>x : any (error)
>asdf : any (error)

var y: asdf;
>y : any (error)

function foo(x: asdf, y: number): asdf { }
>foo : (x: any (error), y: number) => any (error)
>x : any (error)
>y : number

function foo2() {
>foo2 : () => any (error)

    return asdf;
>asdf : any (error)
}

var z = <asdf>x; // should be an error
>z : any (error)
><asdf>x : any (error)
>x : any (error)

class C<T> {
>C : C<T>

    foo: asdf;
>foo : any (error)

    bar: C<asdf>;
>bar : C<any (error)>
}

class C2 implements asdf { }
>C2 : C2

interface I extends adsf { }

class C3 { constructor(x: any) { } }
>C3 : C3
>x : any

class C4 extends C3 {
>C4 : C4
>C3 : C3

    constructor() {
        super(asdf);
>super(asdf) : void
>super : typeof C3
>asdf : any (error)
    }
}

var x2 = this.asdf; // no error, this is any
>x2 : any
>this.asdf : any
>this : any
>asdf : any

class C5 {
>C5 : C5

    constructor() {
        this.asdf = asdf;
>this.asdf = asdf : any (error)
>this.asdf : any (error)
>this : this
>asdf : any (error)
>asdf : any (error)
    }
}
