//// [unknownSymbols2.js]
var M;
(function (M) {
    var x;
    var y = x + asdf;
    var z = x;
    if (asdf) {
    } else if (qwerty) {
    }

    try  {
    } catch (asdf) {
    }

    switch (asdf) {
        case qwerty:
            break;
        default:
            break;
    }

    var a = function () {
        return asdf;
    };
    var b = function (asdf) {
        return qwerty;
    };

    var N;
    (function (N) {
        var x = 1;
    })(N || (N = {}));
})(M || (M = {}));
