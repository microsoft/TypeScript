=== tests/cases/compiler/unknownSymbols2.ts ===
module M {
>M : typeof M

    var x: asdf;
>x : any (error)

    var y = x + asdf;
>y : any (error)
>x + asdf : any (error)
>x : any (error)
>asdf : any (error)

    var z = <asdf>x; // should be an error
>z : any (error)
><asdf>x : any (error)
>x : any (error)

    if (asdf) {
>asdf : any (error)
    }
    else if (qwerty) {
>qwerty : any (error)
    }

    try {
    }
    catch (asdf) { // no error
>asdf : any
    }

    switch (asdf) {
>asdf : any (error)

        case qwerty:
>qwerty : any (error)

            break;
        default:
            break;
    }

    var a = () => asdf;
>a : () => any (error)
>() => asdf : () => any (error)
>asdf : any (error)

    var b = (asdf) => { return qwerty };
>b : (asdf: any) => any (error)
>(asdf) => { return qwerty } : (asdf: any) => any (error)
>asdf : any
>qwerty : any (error)

    module N {
>N : typeof N

        var x = 1;
>x : number
>1 : 1
    }
    import c = N;
>c : typeof N
>N : typeof N

    import d = asdf;
>d : any (error)
>asdf : any (error)
}
