=== /c.ts ===
import foo, { bar } from "foo";
>foo : any (error)
>bar : any (error)

import "./a";
import "./b";
foo(bar());
>foo(bar()) : any (error)
>foo : any (error)
>bar() : any (error)
>bar : any (error)

=== /a.ts ===
import * as foo from "foo";
>foo : any (error)

foo.bar();
>foo.bar() : any (error)
>foo.bar : any (error)
>foo : any (error)
>bar : any (error)

=== /b.ts ===
import foo = require("foo");
>foo : any (error)

foo();
>foo() : any (error)
>foo : any (error)

