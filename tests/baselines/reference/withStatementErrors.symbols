=== tests/cases/compiler/withStatementErrors.ts ===
declare var ooo:any;
>ooo : Symbol(ooo, Decl(withStatementErrors.ts, 0, 11))

with (ooo.eee.oo.ah_ah.ting.tang.walla.walla) { // error
>ooo : Symbol(ooo, Decl(withStatementErrors.ts, 0, 11))

    bing = true; // no error
    bang = true; // no error
    
    function bar() {} // no error

    bar(); // no error

    class C {} // error   
    
    interface I {} // error
    
    module M {} // error
        
}

