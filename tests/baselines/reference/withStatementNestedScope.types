=== tests/cases/compiler/withStatementNestedScope.ts ===
var x = 1;
>x : number
>1 : 1

with (x) {
>x : number

    function f(a: number) {
>f : any (error)
>a : any (error)

        return 1;
>1 : any (error)
    }
    // should be any
    var r = f(1);
>r : any (error)
>f(1) : any (error)
>f : any (error)
>1 : any (error)
}
