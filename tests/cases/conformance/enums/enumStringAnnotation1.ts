// @declaration: true

enum A: string {
    A,
    B
}

enum B: string {
    A,
    B = '1',
    C,
}

enum C: string {
    A = 1,
    B = '2',
    C
}

enum D: number {
    A,
    B
}