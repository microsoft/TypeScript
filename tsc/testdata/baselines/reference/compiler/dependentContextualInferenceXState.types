//// [tests/cases/compiler/dependentContextualInferenceXState.ts] ////

=== dependentContextualInferenceXState.ts ===
declare const runMachine:
>runMachine : <T extends { initial: { state: keyof T["transitions"]; context: object; }; transitions: { [S_1 in keyof T["transitions"]]: (current: { context: { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S_1; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S_1 ? T["initial"]["context"] : never); }) => void | { state: keyof T["transitions"]; context: object; }; }; }, S = keyof T["transitions"]>(machine: T) => S extends unknown ? { state: S; context: { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S ? T["initial"]["context"] : never); } : never

  <T extends
    { initial:
>initial : { state: keyof T["transitions"]; context: object; }

        { state: keyof T["transitions"]
>state : keyof T["transitions"]

        , context: object
>context : object
        }
    , transitions: 
>transitions : { [S_1 in keyof T["transitions"]]: (current: { context: { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S_1; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S_1 ? T["initial"]["context"] : never); }) => void | { state: keyof T["transitions"]; context: object; }; }

        { [S in keyof T["transitions"]]:
            ( current:
>current : { context: { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S ? T["initial"]["context"] : never); }

                  { context:
>context : { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S ? T["initial"]["context"] : never)

                    | { [S1 in keyof T["transitions"]]:
                          ReturnType<T["transitions"][S1]> extends infer N
                            ? N extends unknown
                                ? N extends { state: S, context: infer C }
>state : S
>context : C

                                    ? C
                                    : never
                                : never
                            : never
                      }[keyof T["transitions"]]
                    | ( T["initial"]["state"] extends S
                          ? T["initial"]["context"]
                          : never
                      )
                  }
            ) =>
              | void
              | { state: keyof T["transitions"]
>state : keyof T["transitions"]

                , context: object
>context : object
                }
        }
    },
    S = keyof T["transitions"]
  >
    (machine: T) =>
>machine : T

      S extends unknown
        ? { state: S
>state : S

          , context: 
>context : { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S ? T["initial"]["context"] : never)

            | { [S1 in keyof T["transitions"]]:
                  ReturnType<T["transitions"][S1]> extends infer N
                    ? N extends unknown
                        ? N extends { state: S, context: infer C }
>state : S
>context : C

                            ? C
                            : never
                        : never
                    : never
              }[keyof T["transitions"]]
            | ( T["initial"]["state"] extends S
                  ? T["initial"]["context"]
                  : never
              )
          }
        : never

const result = runMachine({
>result : { state: "loggedIn"; context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; } | { state: "loggedOut"; context: {} | { failedOnce: boolean; }; } | { state: "loggingIn"; context: { username: string; password: string; }; }
>runMachine({  initial: {    state: "loggedOut",    context: {}  },  transitions: {    loggedOut: ({ context }) => {      return { state: "loggingIn", context: { username: "devanshj", password: "1234" } }    },    loggingIn: ({ context }) => {      if (context.username === "devanshj" && context.password === "1234") {        return { state: "loggedIn", context: { ...context, accessToken: "whatever" } }      } else {        return { state: "loggedOut", context: { failedOnce: true } }      }    },    loggedIn: ({ context }) => {      console.log(context.accessToken)    }  }}) : { state: "loggedIn"; context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; } | { state: "loggedOut"; context: {} | { failedOnce: boolean; }; } | { state: "loggingIn"; context: { username: string; password: string; }; }
>runMachine : <T extends { initial: { state: keyof T["transitions"]; context: object; }; transitions: { [S_1 in keyof T["transitions"]]: (current: { context: { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S_1; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S_1 ? T["initial"]["context"] : never); }) => void | { state: keyof T["transitions"]; context: object; }; }; }, S = keyof T["transitions"]>(machine: T) => S extends unknown ? { state: S; context: { [S1 in keyof T["transitions"]]: ReturnType<T["transitions"][S1]> extends infer N ? N extends unknown ? N extends { state: S; context: infer C; } ? C : never : never : never; }[keyof T["transitions"]] | (T["initial"]["state"] extends S ? T["initial"]["context"] : never); } : never
>{  initial: {    state: "loggedOut",    context: {}  },  transitions: {    loggedOut: ({ context }) => {      return { state: "loggingIn", context: { username: "devanshj", password: "1234" } }    },    loggingIn: ({ context }) => {      if (context.username === "devanshj" && context.password === "1234") {        return { state: "loggedIn", context: { ...context, accessToken: "whatever" } }      } else {        return { state: "loggedOut", context: { failedOnce: true } }      }    },    loggedIn: ({ context }) => {      console.log(context.accessToken)    }  }} : { initial: { state: "loggedOut"; context: {}; }; transitions: { loggedOut: ({ context }: { context: {} | { failedOnce: boolean; }; }) => { state: "loggingIn"; context: { username: string; password: string; }; }; loggingIn: ({ context }: { context: { username: string; password: string; }; }) => { state: "loggedIn"; context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; } | { state: "loggedOut"; context: { failedOnce: boolean; }; }; loggedIn: ({ context }: { context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; }) => void; }; }

  initial: {
>initial : { state: "loggedOut"; context: {}; }
>{    state: "loggedOut",    context: {}  } : { state: "loggedOut"; context: {}; }

    state: "loggedOut",
>state : "loggedOut"
>"loggedOut" : "loggedOut"

    context: {}
>context : {}
>{} : {}

  },
  transitions: {
>transitions : { loggedOut: ({ context }: { context: {} | { failedOnce: boolean; }; }) => { state: "loggingIn"; context: { username: string; password: string; }; }; loggingIn: ({ context }: { context: { username: string; password: string; }; }) => { state: "loggedIn"; context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; } | { state: "loggedOut"; context: { failedOnce: boolean; }; }; loggedIn: ({ context }: { context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; }) => void; }
>{    loggedOut: ({ context }) => {      return { state: "loggingIn", context: { username: "devanshj", password: "1234" } }    },    loggingIn: ({ context }) => {      if (context.username === "devanshj" && context.password === "1234") {        return { state: "loggedIn", context: { ...context, accessToken: "whatever" } }      } else {        return { state: "loggedOut", context: { failedOnce: true } }      }    },    loggedIn: ({ context }) => {      console.log(context.accessToken)    }  } : { loggedOut: ({ context }: { context: {} | { failedOnce: boolean; }; }) => { state: "loggingIn"; context: { username: string; password: string; }; }; loggingIn: ({ context }: { context: { username: string; password: string; }; }) => { state: "loggedIn"; context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; } | { state: "loggedOut"; context: { failedOnce: boolean; }; }; loggedIn: ({ context }: { context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; }) => void; }

    loggedOut: ({ context }) => {
>loggedOut : ({ context }: { context: {} | { failedOnce: boolean; }; }) => { state: "loggingIn"; context: { username: string; password: string; }; }
>({ context }) => {      return { state: "loggingIn", context: { username: "devanshj", password: "1234" } }    } : ({ context }: { context: {} | { failedOnce: boolean; }; }) => { state: "loggingIn"; context: { username: string; password: string; }; }
>context : {} | { failedOnce: boolean; }

      return { state: "loggingIn", context: { username: "devanshj", password: "1234" } }
>{ state: "loggingIn", context: { username: "devanshj", password: "1234" } } : { state: "loggingIn"; context: { username: string; password: string; }; }
>state : "loggingIn"
>"loggingIn" : "loggingIn"
>context : { username: string; password: string; }
>{ username: "devanshj", password: "1234" } : { username: string; password: string; }
>username : string
>"devanshj" : "devanshj"
>password : string
>"1234" : "1234"

    },
    loggingIn: ({ context }) => {
>loggingIn : ({ context }: { context: { username: string; password: string; }; }) => { state: "loggedIn"; context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; } | { state: "loggedOut"; context: { failedOnce: boolean; }; }
>({ context }) => {      if (context.username === "devanshj" && context.password === "1234") {        return { state: "loggedIn", context: { ...context, accessToken: "whatever" } }      } else {        return { state: "loggedOut", context: { failedOnce: true } }      }    } : ({ context }: { context: { username: string; password: string; }; }) => { state: "loggedIn"; context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; } | { state: "loggedOut"; context: { failedOnce: boolean; }; }
>context : { username: string; password: string; }

      if (context.username === "devanshj" && context.password === "1234") {
>context.username === "devanshj" && context.password === "1234" : boolean
>context.username === "devanshj" : boolean
>context.username : string
>context : { username: string; password: string; }
>username : string
>"devanshj" : "devanshj"
>context.password === "1234" : boolean
>context.password : string
>context : { username: string; password: string; }
>password : string
>"1234" : "1234"

        return { state: "loggedIn", context: { ...context, accessToken: "whatever" } }
>{ state: "loggedIn", context: { ...context, accessToken: "whatever" } } : { state: "loggedIn"; context: { username: string; password: string; accessToken: string; }; }
>state : "loggedIn"
>"loggedIn" : "loggedIn"
>context : { username: string; password: string; accessToken: string; }
>{ ...context, accessToken: "whatever" } : { username: string; password: string; accessToken: string; }
>context : { username: string; password: string; }
>accessToken : string
>"whatever" : "whatever"

      } else {
        return { state: "loggedOut", context: { failedOnce: true } }
>{ state: "loggedOut", context: { failedOnce: true } } : { state: "loggedOut"; context: { failedOnce: true; }; }
>state : "loggedOut"
>"loggedOut" : "loggedOut"
>context : { failedOnce: true; }
>{ failedOnce: true } : { failedOnce: true; }
>failedOnce : true
>true : true
      }
    },
    loggedIn: ({ context }) => {
>loggedIn : ({ context }: { context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; }) => void
>({ context }) => {      console.log(context.accessToken)    } : ({ context }: { context: { username: string; password: string; accessToken: string; failedOnce?: undefined; }; }) => void
>context : { username: string; password: string; accessToken: string; failedOnce?: undefined; }

      console.log(context.accessToken)
>console.log(context.accessToken) : void
>console.log : (...data: any[]) => void
>console : Console
>log : (...data: any[]) => void
>context.accessToken : string
>context : { username: string; password: string; accessToken: string; failedOnce?: undefined; }
>accessToken : string
    }
  }
})

