/types.d.ts(1,16): error TS1550: An ambient module declaration with import attributes must use a pattern name with an '*' character.


==== /types.d.ts (1 errors) ====
    declare module "plainstring" with { type: "css" } {
                   ~~~~~~~~~~~~~
!!! error TS1550: An ambient module declaration with import attributes must use a pattern name with an '*' character.
        export const value: "plain";
    }
    
==== /index.ts (0 errors) ====
    import { value as withoutAttributes } from "plainstring";
    import { value as withAttributes } from "plainstring" with { type: "ignored" };
    
    const first: "plain" = withoutAttributes;
    const second: "plain" = withAttributes;