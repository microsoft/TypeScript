intlCollatorCompare.ts(12,10): error TS2540: Cannot assign to 'compare' because it is a read-only property.


==== intlCollatorCompare.ts (1 errors) ====
    const collator = new Intl.Collator("en");
    
    const compare = collator.compare;
    compare("a", "b");
    
    ["b", "a"].sort(collator.compare);
    ["b", "a"].sort(compare);
    
    collator.compare("a", "b");
    
    // error: compare is readonly
    collator.compare = (x: string, y: string) => 0;
             ~~~~~~~
!!! error TS2540: Cannot assign to 'compare' because it is a read-only property.
    