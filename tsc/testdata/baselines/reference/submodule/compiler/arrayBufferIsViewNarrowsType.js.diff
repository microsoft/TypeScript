--- old.arrayBufferIsViewNarrowsType.js
+++ new.arrayBufferIsViewNarrowsType.js
@@= skipped -9, +9 lines =@@
 //// [arrayBufferIsViewNarrowsType.js]
 var obj;
 if (ArrayBuffer.isView(obj)) {
-    // isView should be a guard that narrows type to ArrayBufferView.
     var ab = obj;
 }
