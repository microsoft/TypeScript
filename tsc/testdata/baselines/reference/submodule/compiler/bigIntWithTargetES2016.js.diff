--- old.bigIntWithTargetES2016.js
+++ new.bigIntWithTargetES2016.js
@@= skipped -7, +7 lines =@@
 
 
 //// [bigIntWithTargetES2016.js]
-BigInt(1) ** BigInt(1); // should not error
+BigInt(1) ** BigInt(1);
 let num = BigInt(2);
-num **= BigInt(2); // should not error
+num **= BigInt(2);
