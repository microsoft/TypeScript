--- old.blockScopedSameNameFunctionDeclarationES5.js
+++ new.blockScopedSameNameFunctionDeclarationES5.js
@@= skipped -20, +20 lines =@@
 //// [blockScopedSameNameFunctionDeclarationES5.js]
 function foo(a) {
     if (a === 1) {
-        function foo() { } // duplicate function
+        function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
     else {
-        function foo() { } // duplicate function
+        function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
-    foo(10); // not ok
+    foo(10);
     foo();
 }
 foo(10);
-foo(); // not ok - needs number
+foo();
