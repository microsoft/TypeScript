--- old.blockScopedSameNameFunctionDeclarationES6.js
+++ new.blockScopedSameNameFunctionDeclarationES6.js
@@= skipped -20, +20 lines =@@
 //// [blockScopedSameNameFunctionDeclarationES6.js]
 function foo(a) {
     if (a === 10) {
-        function foo() { } // duplicate
+        function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
     else {
-        function foo() { } // duplicate
+        function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
-    foo(10); // not ok
+    foo(10);
     foo();
 }
 foo(10);
-foo(); // not ok - needs number
+foo();
