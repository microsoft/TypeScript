--- old.blockScopedSameNameFunctionDeclarationStrictES5.js
+++ new.blockScopedSameNameFunctionDeclarationStrictES5.js
@@= skipped -22, +22 lines =@@
 "use strict";
 function foo(a) {
     if (a === 1) {
-        function foo() { } // Error to declare function in block scope
+        function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
     else {
-        function foo() { } // Error to declare function in block scope
+        function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
     foo(10);
-    foo(); // not ok - needs number
+    foo();
 }
 foo(10);
-foo(); // not ok - needs number
+foo();
