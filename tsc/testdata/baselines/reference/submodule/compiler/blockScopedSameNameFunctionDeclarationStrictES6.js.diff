--- old.blockScopedSameNameFunctionDeclarationStrictES6.js
+++ new.blockScopedSameNameFunctionDeclarationStrictES6.js
@@= skipped -24, +24 lines =@@
     if (a === 10) {
         function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
     else {
         function foo() { }
         foo();
-        foo(10); // not ok
+        foo(10);
     }
     foo(10);
-    foo(); // not ok
+    foo();
 }
 foo(10);
-foo(); // not ok - needs number
+foo();
