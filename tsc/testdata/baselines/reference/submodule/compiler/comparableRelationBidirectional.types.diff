--- old.comparableRelationBidirectional.types
+++ new.comparableRelationBidirectional.types
@@= skipped -21, +21 lines =@@
 }
 
 interface ThemePreset {
+>ThemePreset : ThemePreset
+
     id: string;
 >id : string
 }
 
 interface Automation {
+>Automation : Automation
+
     mode: AutomationMode;
 >mode : AutomationMode
 }
 
 interface UserSettings {
+>UserSettings : UserSettings
+
     presets: ThemePreset[];
 >presets : ThemePreset[]
 
@@= skipped -18, +24 lines =@@
 }
 
 interface ExtensionData {
+>ExtensionData : ExtensionData
+
     settings: UserSettings;
 >settings : UserSettings
 }
