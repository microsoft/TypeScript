--- old.constDeclarations-invalidContexts.js
+++ new.constDeclarations-invalidContexts.js
@@= skipped -34, +34 lines =@@
 
 
 //// [constDeclarations-invalidContexts.js]
-// Errors, const must be defined inside a block
 if (true)
     const c1 = 0;
 else
@@= skipped -12, +11 lines =@@
 while (true);
 var obj;
 with (obj)
-    const c5 = 0; // No  Error will be reported here since we turn off all type checking
+    const c5 = 0;
 for (var i = 0; i < 10; i++)
     const c6 = 0;
 for (var i2 in {})
