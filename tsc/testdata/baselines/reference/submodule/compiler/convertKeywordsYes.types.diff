--- old.convertKeywordsYes.types
+++ new.convertKeywordsYes.types
@@= skipped -78, +78 lines =@@
 >bigGeneric : <constructor, implements, interface, let, module, package, private, protected, public, set, static, get, yield, declare>(c: constructor, a: any, b2: boolean, i: implements, i2: interface, l: let, m: module, n: number, p: package, p2: private, p3: protected, p4: public, s: set, s2: static, s3: string, g: get, y: yield, d: declare) => void
 
     constructor,
+>constructor : constructor
+
     implements ,
+>implements : implements
+
     interface ,
+>interface : interface
+
     let,
+>let : let
+
     module ,
+>module : module
+
     package,
+>package : package
+
     private ,
+>private : private
+
     protected,
+>protected : protected
+
     public ,
+>public : public
+
     set ,
+>set : set
+
     static ,
+>static : static
+
     get ,
+>get : get
+
     yield,
+>yield : yield
+
     declare
+>declare : declare
+
     >(c: constructor,
 >c : constructor
 
@@= skipped -290, +318 lines =@@
 };
 
 interface bigInterface {
+>bigInterface : bigInterface
+
     constructor;
 >constructor : any
 
