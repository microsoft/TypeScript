--- old.declarationEmitLateBoundAssignments.js
+++ new.declarationEmitLateBoundAssignments.js
@@= skipped -31, +31 lines =@@
 const y = foo[strMem];
 const z = foo[numMem];
 const a = foo[dashStrMem];
-
-
-//// [declarationEmitLateBoundAssignments.d.ts]
-export declare function foo(): void;
-export declare namespace foo {
-    var bar: number;
-    var strMemName: string;
-}
