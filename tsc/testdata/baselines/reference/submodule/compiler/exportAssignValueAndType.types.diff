--- old.exportAssignValueAndType.types
+++ new.exportAssignValueAndType.types
@@= skipped -1, +1 lines =@@
 
 === exportAssignValueAndType.ts ===
 declare module http {
+>http : any
+
 	export interface Server { openPort: number; }
+>Server : Server
 >openPort : number
 }
 
 interface server {
+>server : server
+
     (): http.Server;
 >http : any
 
