--- old.functionExpressionShadowedByParams.js
+++ new.functionExpressionShadowedByParams.js
@@= skipped -16, +16 lines =@@
 
 //// [functionExpressionShadowedByParams.js]
 function b1(b1) {
-    b1.toPrecision(2); // should not error
-    b1(12); // should error
+    b1.toPrecision(2);
+    b1(12);
 }
 var x = {
     b: function b(b) {
-        b.toPrecision(2); // should not error
-        b.apply(null, null); // should error
+        b.toPrecision(2);
+        b.apply(null, null);
     }
 };
