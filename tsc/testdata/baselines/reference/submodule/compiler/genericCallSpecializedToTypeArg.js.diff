--- old.genericCallSpecializedToTypeArg.js
+++ new.genericCallSpecializedToTypeArg.js
@@= skipped -15, +15 lines =@@
     return x;
 }
 function dupeAndGetDist(x) {
-    var y = dupe(x); //<-- dupe has incorrect type here
-    y.getDist(); //<-- this requires a missing constraint, but it's not caught
+    var y = dupe(x);
+    y.getDist();
     return y;
 }
