--- old.letDeclarations-scopes2.js
+++ new.letDeclarations-scopes2.js
@@= skipped -31, +31 lines =@@
 let global = 0;
 {
     let local = 0;
-    local; // OK
-    global; // OK
-    local2; // Error
+    local;
+    global;
+    local2;
     {
         let local2 = 0;
-        local; // OK
-        global; // OK
-        local2; // OK
+        local;
+        global;
+        local2;
     }
-    local; // OK
-    global; // OK
-    local2; // Error
+    local;
+    global;
+    local2;
 }
-local; // Error
-global; // OK
-local2; // Error
+local;
+global;
+local2;
