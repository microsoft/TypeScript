--- old.moduleExportsTypeNoExcessPropertyCheckFromContainedLiteral.errors.txt
+++ new.moduleExportsTypeNoExcessPropertyCheckFromContainedLiteral.errors.txt
@@= skipped -0, +0 lines =@@
-<no content>
+eslint-plugin-react.js(9,1): error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+typescript-eslint.js(14,1): error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+
+
+==== eslint.config.js (0 errors) ====
+    const eslintReact = require('./eslint-plugin-react.js');
+    const tseslint = require('./typescript-eslint.js');
+    
+    tseslint.config(eslintReact)
+    
+==== eslint-plugin-react.js (1 errors) ====
+    const deprecatedRules = {
+      "jsx-sort-default-props": true
+    }
+    
+    const allRules = {
+      'no-unsafe': true
+    }
+    
+    module.exports = {
+    ~~~~~~~~~~~~~~~~~~
+      plugins: {
+    ~~~~~~~~~~~~
+        react: {
+    ~~~~~~~~~~~~
+          deprecatedRules,
+    ~~~~~~~~~~~~~~~~~~~~~~
+          rules: allRules,
+    ~~~~~~~~~~~~~~~~~~~~~~
+        },
+    ~~~~~~
+      },
+    ~~~~
+    };
+    ~
+!!! error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+    
+==== typescript-eslint.js (1 errors) ====
+    /**
+     * @typedef {{ rules: Record<string, boolean> }} Plugin
+     */
+    
+    /**
+     * @typedef {{ plugins: Record<string, Plugin> }} Config
+     */
+    
+    /**
+     * @type {(...configs: Config[]) => void}
+     */
+    function config(...configs) { }
+    
+    module.exports = { config };
+    ~~~~~~~~~~~~~~~~~~~~~~~~~~~
+!!! error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+    