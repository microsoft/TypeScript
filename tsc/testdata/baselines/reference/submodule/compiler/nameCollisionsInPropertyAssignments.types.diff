--- old.nameCollisionsInPropertyAssignments.types
+++ new.nameCollisionsInPropertyAssignments.types
@@= skipped -5, +5 lines =@@
 >1 : 1
 
 var y = { x() { x++; } }; 
->y : { x(): void; }
->{ x() { x++; } } : { x(): void; }
+>y : { x: () => void; }
+>{ x() { x++; } } : { x: () => void; }
 >x : () => void
 >x++ : number
 >x : number
