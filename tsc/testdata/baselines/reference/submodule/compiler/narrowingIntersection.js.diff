--- old.narrowingIntersection.js
+++ new.narrowingIntersection.js
@@= skipped -30, +30 lines =@@
     if (result.err) {
         throw result.err;
     }
-    // Error, should OK
     return result.value;
 }
 function want0(x) { }
 function test2(a) {
     if (a === 0) {
-        want0(a); // Fails, but expect to work
+        want0(a);
     }
 }
