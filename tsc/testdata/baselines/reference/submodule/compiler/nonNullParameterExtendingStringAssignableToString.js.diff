--- old.nonNullParameterExtendingStringAssignableToString.js
+++ new.nonNullParameterExtendingStringAssignableToString.js
@@= skipped -10, +10 lines =@@
 }
 
 //// [nonNullParameterExtendingStringAssignableToString.js]
-"use strict";
 function fn(one, two) {
-    var three = Boolean() ? one : two;
+    let three = Boolean() ? one : two;
     foo(one);
     foo(two);
-    foo(three); // this line is the important one
+    foo(three);
 }
