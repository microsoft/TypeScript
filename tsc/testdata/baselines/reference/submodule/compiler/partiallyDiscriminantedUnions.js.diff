--- old.partiallyDiscriminantedUnions.js
+++ new.partiallyDiscriminantedUnions.js
@@= skipped -58, +58 lines =@@
 }
 // Repro from #11185
 class Square {
+    kind;
 }
 class Circle {
+    kind;
 }
 function isShape(s) {
     return !Array.isArray(s);