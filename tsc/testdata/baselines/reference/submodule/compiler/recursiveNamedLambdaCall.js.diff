--- old.recursiveNamedLambdaCall.js
+++ new.recursiveNamedLambdaCall.js
@@= skipped -31, +31 lines =@@
                 catch (e) {
                     return setTimeout(doScrollCheck, 50);
                 }
-                // detach all dom ready events
                 detach();
             }
         })();
