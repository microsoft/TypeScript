--- old.templateLiteralsAndDecoratorMetadata.js
+++ new.templateLiteralsAndDecoratorMetadata.js
@@= skipped -17, +17 lines =@@
     if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
 };
 export class Greeter {
-    constructor() {
-        this.greeting = `employee`; //template literals on this line cause the issue
-    }
+    greeting = `employee`; //template literals on this line cause the issue
 }
 __decorate([
     format("Hello, %s"),