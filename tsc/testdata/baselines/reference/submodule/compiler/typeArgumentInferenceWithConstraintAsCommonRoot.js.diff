--- old.typeArgumentInferenceWithConstraintAsCommonRoot.js
+++ new.typeArgumentInferenceWithConstraintAsCommonRoot.js
@@= skipped -12, +12 lines =@@
 function f(x, y) { return undefined; }
 var g;
 var e;
-f(g, e); // valid because both Giraffe and Elephant satisfy the constraint. T is Animal
+f(g, e);
