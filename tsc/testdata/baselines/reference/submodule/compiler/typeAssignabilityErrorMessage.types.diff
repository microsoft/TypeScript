--- old.typeAssignabilityErrorMessage.types
+++ new.typeAssignabilityErrorMessage.types
@@= skipped -3, +3 lines =@@
 // Example: different error code altogether
 
 interface ThroughStream {
+>ThroughStream : ThroughStream
+
     a: string;
 >a : string
 }
 interface ReadStream {
+>ReadStream : ReadStream
+
     f: string;
 >f : string
 
@@= skipped -52, +56 lines =@@
 }
 type Foo<T> = {
 >Foo : Foo<T>
+>T : T
 
     foo: { what: T };
 >foo : { what: T; }
@@= skipped -7, +8 lines =@@
 }
 type Bar<T> = {
 >Bar : Bar<T>
+>T : T
 
     foo: { what: T };
 >foo : { what: T; }
