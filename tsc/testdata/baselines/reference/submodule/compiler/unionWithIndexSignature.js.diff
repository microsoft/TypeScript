--- old.unionWithIndexSignature.js
+++ new.unionWithIndexSignature.js
@@= skipped -35, +35 lines =@@
 exports.isTypedArray = isTypedArray;
 exports.flatten = flatten;
 function foo(arr) {
-    var zz = arr[1]; // Error
+    let zz = arr[1]; // Error
 }
 function isTypedArray(a) {
     return a instanceof Int32Array || a instanceof Uint8Array;
