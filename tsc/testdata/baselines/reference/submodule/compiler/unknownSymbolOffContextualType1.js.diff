--- old.unknownSymbolOffContextualType1.js
+++ new.unknownSymbolOffContextualType1.js
@@= skipped -33, +33 lines =@@
         return !e.isDisabled;
     });
     var widths = enabled.map(function (e) {
-        return e.xyxyxyx; // error expected here
+        return e.xyxyxyx;
     });
     var maxWidth = widths.reduce(function (a, b) {
         return a > b ? a : b;
