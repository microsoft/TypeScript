--- old.ES5For-of18.js
+++ new.ES5For-of18.js
@@= skipped -9, +9 lines =@@
 
 
 //// [ES5For-of18.js]
-for (var _i = 0, _a = []; _i < _a.length; _i++) {
-    var v = _a[_i];
+for (let v of []) {
     v;
 }
-for (var _b = 0, _c = []; _b < _c.length; _b++) {
-    var v = _c[_b];
+for (let v of []) {
     v;
 }
