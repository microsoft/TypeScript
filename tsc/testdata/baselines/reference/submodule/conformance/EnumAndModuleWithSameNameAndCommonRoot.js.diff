--- old.EnumAndModuleWithSameNameAndCommonRoot.js
+++ new.EnumAndModuleWithSameNameAndCommonRoot.js
@@= skipped -26, +26 lines =@@
 })(enumdule || (enumdule = {}));
 (function (enumdule) {
     class Point {
+        x;
+        y;
         constructor(x, y) {
             this.x = x;
             this.y = y;