--- old.EnumAndModuleWithSameNameAndCommonRoot.js
+++ new.EnumAndModuleWithSameNameAndCommonRoot.js
@@= skipped -24, +24 lines =@@
     enumdule[enumdule["Blue"] = 1] = "Blue";
 })(enumdule || (enumdule = {}));
 (function (enumdule) {
-    var Point = /** @class */ (function () {
-        function Point(x, y) {
+    class Point {
+        x;
+        y;
+        constructor(x, y) {
             this.x = x;
             this.y = y;
         }
-        return Point;
-    }());
+    }
     enumdule.Point = Point;
 })(enumdule || (enumdule = {}));
 var x;
