--- old.asOpEmitParens.js
+++ new.asOpEmitParens.js
@@= skipped -13, +13 lines =@@
 
 //// [asOpEmitParens.js]
 // Must emit as (x + 1) * 3
-(x + 1) * 3;
+((x + 1)) * 3;
 // Should still emit as x.y
 x.y;
 // Emit as new (x())
-new (x());
+new ((x()));
