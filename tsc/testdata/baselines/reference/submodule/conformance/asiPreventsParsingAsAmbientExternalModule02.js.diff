--- old.asiPreventsParsingAsAmbientExternalModule02.js
+++ new.asiPreventsParsingAsAmbientExternalModule02.js
@@= skipped -15, +15 lines =@@
 var module;
 var container;
 (function (container) {
-    declare; // this is the identifier 'declare'
-    module; // this is the identifier 'module'
-    "my external module"; // this is just a string
-    { } // this is a block body
+    declare;
+    module;
+    "my external module";
+    { }
 })(container || (container = {}));
