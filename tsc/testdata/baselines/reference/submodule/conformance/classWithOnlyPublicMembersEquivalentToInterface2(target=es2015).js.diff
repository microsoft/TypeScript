--- old.classWithOnlyPublicMembersEquivalentToInterface2(target=es2015).js
+++ new.classWithOnlyPublicMembersEquivalentToInterface2(target=es2015).js
@@= skipped -32, +32 lines =@@
 "use strict";
 // no errors expected
 class C {
+    x;
     y(a) { return null; }
     get z() { return 1; }
     set z(v) { }
+    0;
+    static foo; // doesn't effect equivalence
 }
 var c;
 var i;