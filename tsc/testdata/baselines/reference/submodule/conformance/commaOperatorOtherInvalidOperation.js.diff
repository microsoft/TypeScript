--- old.commaOperatorOtherInvalidOperation.js
+++ new.commaOperatorOtherInvalidOperation.js
@@= skipped -15, +15 lines =@@
 }
 
 //// [commaOperatorOtherInvalidOperation.js]
-//Expect to have compiler errors
-//Comma operator in function arguments and return
 function foo(x, y) {
     return x, y;
 }
-var resultIsString = foo(1, "123"); //error here
-//TypeParameters
+var resultIsString = foo(1, "123");
 function foo1() {
     var x;
     var y;
-    var result = (x, y); //error here
+    var result = (x, y);
 }
