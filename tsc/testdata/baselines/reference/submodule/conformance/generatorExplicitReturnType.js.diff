--- old.generatorExplicitReturnType.js
+++ new.generatorExplicitReturnType.js
@@= skipped -26, +26 lines =@@
 
 //// [generatorExplicitReturnType.js]
 function* g1() {
-    yield; // error
-    yield "a"; // error
-    const x = yield 1; // error
-    return 10; // error
+    yield;
+    yield "a";
+    const x = yield 1;
+    return 10;
 }
 function* g2() {
     const x = yield 1;
     return true;
 }
 function* g3() {
-    const x = yield* generator; // error
+    const x = yield* generator;
     return true;
 }
 function* g4() {
