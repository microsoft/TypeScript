--- old.genericCallToOverloadedMethodWithOverloadedArguments.js
+++ new.genericCallToOverloadedMethodWithOverloadedArguments.js
@@= skipped -93, +93 lines =@@
     var numPromise;
     var newPromise = numPromise.then(testFunction);
 })(m1 || (m1 = {}));
-//////////////////////////////////////
 var m2;
 (function (m2) {
     var numPromise;
     var newPromise = numPromise.then(testFunction);
 })(m2 || (m2 = {}));
-//////////////////////////////////////
 var m3;
 (function (m3) {
     var numPromise;
     var newPromise = numPromise.then(testFunction);
 })(m3 || (m3 = {}));
-//////////////////////////////////////
 var m4;
 (function (m4) {
     var numPromise;
     var newPromise = numPromise.then(testFunction);
 })(m4 || (m4 = {}));
-//////////////////////////////////////
 var m5;
 (function (m5) {
     var numPromise;
     var newPromise = numPromise.then(testFunction);
 })(m5 || (m5 = {}));
-//////////////////////////////////////
 var m6;
 (function (m6) {
     var numPromise;
