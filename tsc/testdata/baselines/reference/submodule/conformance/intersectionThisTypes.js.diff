--- old.intersectionThisTypes.js
+++ new.intersectionThisTypes.js
@@= skipped -52, +52 lines =@@
     t = t.me().self().me();
 }
 function test(label) {
-    var extended = label.extend({ id: 67 }).extend({ tag: "hello" });
+    const extended = label.extend({ id: 67 }).extend({ tag: "hello" });
     extended.id; // Ok
     extended.tag; // Ok
 }
