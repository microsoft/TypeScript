--- old.jsDeclarationsTypeReferences(target=es2015).errors.txt
+++ new.jsDeclarationsTypeReferences(target=es2015).errors.txt
@@= skipped -0, +0 lines =@@
-<no content>
+index.js(7,1): error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+
+
+==== index.js (1 errors) ====
+    /// <reference types="node" />
+    
+    const Something = require("fs").Something;
+    
+    const thing = new Something();
+    
+    module.exports = {
+    ~~~~~~~~~~~~~~~~~~
+        thing
+    ~~~~~~~~~
+    };
+    ~
+!!! error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+    
+==== node_modules/@types/node/index.d.ts (0 errors) ====
+    declare module "fs" {
+        export class Something {}
+    }