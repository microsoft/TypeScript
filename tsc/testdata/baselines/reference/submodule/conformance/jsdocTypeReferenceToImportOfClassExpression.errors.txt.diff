--- old.jsdocTypeReferenceToImportOfClassExpression.errors.txt
+++ new.jsdocTypeReferenceToImportOfClassExpression.errors.txt
@@= skipped -0, +0 lines =@@
-<no content>
+MC.js(5,1): error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+MW.js(12,1): error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+
+
+==== MC.js (1 errors) ====
+    const MW = require("./MW");
+    
+    /** @typedef {number} Cictema */
+    
+    module.exports = class MC {
+    ~~~~~~~~~~~~~~~~~~~~~~~~~~~
+      watch() {
+    ~~~~~~~~~~~
+        return new MW(this);
+    ~~~~~~~~~~~~~~~~~~~~~~~~
+      }
+    ~~~
+    };
+    ~
+!!! error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+    
+==== MW.js (1 errors) ====
+    /** @typedef {import("./MC")} MC */
+    
+    class MW {
+      /**
+       * @param {MC} compiler the compiler
+       */
+      constructor(compiler) {
+        this.compiler = compiler;
+      }
+    }
+    
+    module.exports = MW;
+    ~~~~~~~~~~~~~~~~~~~
+!!! error TS1203: Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead.
+    