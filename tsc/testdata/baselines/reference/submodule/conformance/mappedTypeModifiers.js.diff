--- old.mappedTypeModifiers.js
+++ new.mappedTypeModifiers.js
@@= skipped -155, +155 lines =@@
 var b04;
 var b04;
 function f1(x) {
-    x.prop; // ok
+    x.prop;
     (x["other"] || 0).toFixed();
 }
 function f2(x) {
-    x.prop; // ok
+    x.prop;
     x["other"].toFixed();
 }
 function f3(x) {
-    x.prop; // ok
+    x.prop;
     x["other"].x.toFixed();
 }
 function f4(x) {
-    x.prop; // ok
+    x.prop;
     x["other"].toFixed();
 }
