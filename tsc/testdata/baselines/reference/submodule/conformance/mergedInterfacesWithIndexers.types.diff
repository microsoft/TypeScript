--- old.mergedInterfacesWithIndexers.types
+++ new.mergedInterfacesWithIndexers.types
@@= skipped -3, +3 lines =@@
 // indexers should behave like other members when merging interface declarations
 
 interface A {
+>A : A
+
     [x: number]: string;
 >x : number
 }
 
 
 interface A {
+>A : A
+
     [x: string]: { length: number };
 >x : string
 >length : number
