--- old.nodeModulesTripleSlashReferenceModeOverride5(module=node16).types
+++ new.nodeModulesTripleSlashReferenceModeOverride5(module=node16).types
@@= skipped -5, +5 lines =@@
 // Both `foo` and `bar` should resolve, as _both_ entrypoints are included by the two
 // references above.
 foo;
->foo : number
+>foo : any
 
 bar;
 >bar : number
 
 export {};
-=== /node_modules/pkg/import.d.ts ===
-export {};
-declare global {
->global : typeof global
-
-    var foo: number;
->foo : number
-}
 === /node_modules/pkg/require.d.ts ===
 export {};
 declare global {
