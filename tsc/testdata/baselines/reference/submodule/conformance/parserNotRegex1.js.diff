--- old.parserNotRegex1.js
+++ new.parserNotRegex1.js
@@= skipped -6, +6 lines =@@
   }
 
 //// [parserNotRegex1.js]
-if (a.indexOf(-(4 / 3))) // We should not get a regex here because of the / in the comment.
- {
+if (a.indexOf(-(4 / 3))) {
     return true;
 }
