--- old.stringLiteralTypesInUnionTypes02.js
+++ new.stringLiteralTypesInUnionTypes02.js
@@= skipped -24, +24 lines =@@
 var x = undefined;
 var y = undefined;
 if (x === "foo") {
-    var a = x;
+    let a = x;
 }
 else if (x !== "bar") {
-    var b = x || y;
+    let b = x || y;
 }
 else {
-    var c = x;
-    var d = y;
-    var e = c || d;
+    let c = x;
+    let d = y;
+    let e = c || d;
 }
 x = y;
 y = x;
-
-
-//// [stringLiteralTypesInUnionTypes02.d.ts]
-type T = string | "foo" | "bar" | "baz";
-declare var x: "foo" | "bar" | "baz" | string;
-declare var y: T;
