--- old.stringLiteralTypesInUnionTypes03.js
+++ new.stringLiteralTypesInUnionTypes03.js
@@= skipped -24, +24 lines =@@
 var x;
 var y = undefined;
 if (x === "foo") {
-    var a = x;
+    let a = x;
 }
 else if (x !== "bar") {
-    var b = x || y;
+    let b = x || y;
 }
 else {
-    var c = x;
-    var d = y;
-    var e = c || d;
+    let c = x;
+    let d = y;
+    let e = c || d;
 }
 x = y;
 y = x;
-
-
-//// [stringLiteralTypesInUnionTypes03.d.ts]
-type T = number | "foo" | "bar";
-declare var x: "foo" | "bar" | number;
-declare var y: T;
