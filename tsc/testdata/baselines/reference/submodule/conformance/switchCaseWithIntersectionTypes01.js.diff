--- old.switchCaseWithIntersectionTypes01.js
+++ new.switchCaseWithIntersectionTypes01.js
@@= skipped -32, +32 lines =@@
 var num;
 var bool;
 switch (strAndNum) {
-    // Identical
     case strAndNum:
         break;
-    // Constituents
     case str:
     case num:
         break;
-    // Overlap in constituents
     case numAndBool:
         break;
-    // No relation
     case bool:
         break;
 }
