--- old.switchCaseWithUnionTypes01.js
+++ new.switchCaseWithUnionTypes01.js
@@= skipped -32, +32 lines =@@
 var num;
 var bool;
 switch (strOrNum) {
-    // Identical
     case strOrNum:
         break;
-    // Constituents
     case str:
     case num:
         break;
-    // Overlap in constituents
     case numOrBool:
         break;
-    // No relation
     case bool:
         break;
 }
