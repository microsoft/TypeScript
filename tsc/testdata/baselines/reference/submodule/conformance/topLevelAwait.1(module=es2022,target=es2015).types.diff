--- old.topLevelAwait.1(module=es2022,target=es2015).types
+++ new.topLevelAwait.1(module=es2022,target=es2015).types
@@= skipped -88, +88 lines =@@
 >1 : 1
 }
 ({
->({    await() {}}) : { await(): void; }
->{    await() {}} : { await(): void; }
+>({    await() {}}) : { await: () => void; }
+>{    await() {}} : { await: () => void; }
 
     await() {}
 >await : () => void
