--- old.typeArgumentInferenceTransitiveConstraints.js
+++ new.typeArgumentInferenceTransitiveConstraints.js
@@= skipped -13, +13 lines =@@
     return [a, b, c];
 }
 var d = fn(new Date(), new Date(), new Date());
-var d; // Should be OK (d should be Date[])
+var d;
