--- old.typeGuardEnums.js
+++ new.typeGuardEnums.js
@@= skipped -27, +27 lines =@@
 var V;
 (function (V) {
 })(V || (V = {}));
-var x;
+let x;
 if (typeof x === "number") {
-    x; // number|E|V
+    x;
 }
 else {
-    x; // string
+    x;
 }
 if (typeof x !== "number") {
-    x; // string
+    x;
 }
 else {
-    x; // number|E|V
+    x;
 }
