--- old.unionTypeIdentity.js
+++ new.unionTypeIdentity.js
@@= skipped -10, +10 lines =@@
 var strOrNum: number; // error
 
 //// [unionTypeIdentity.js]
-// Two types are considered identical when
-// they are union types with identical sets of constituent types, or
 var strOrNum;
 var strOrNum;
 var strOrNum;
-var strOrNum; // error
-var strOrNum; // error
-var strOrNum; // error
+var strOrNum;
+var strOrNum;
+var strOrNum;
