--- old.findAllRefsExportEquals.baseline.jsonc
+++ new.findAllRefsExportEquals.baseline.jsonc
@@= skipped -0, +0 lines =@@
 // === findAllReferences ===
 // === /a.ts ===
 // type /*FIND ALL REFS*/[|T|] = number;
-// export = [|T|];
-
-// === /b.ts ===
-// import [|T|] = require("./a");
+// export = T;



 // === findAllReferences ===
 // === /a.ts ===
-// type [|T|] = number;
+// type T = number;
 // /*FIND ALL REFS*/export = [|T|];

 // === /b.ts ===
@@= skipped -19, +16 lines =@@

 // === findAllReferences ===
 // === /a.ts ===
-// type [|T|] = number;
+// type T = number;
 // export = /*FIND ALL REFS*/[|T|];

 // === /b.ts ===