--- old.renameImportSpecifierPropertyName.baseline.jsonc
+++ new.renameImportSpecifierPropertyName.baseline.jsonc
@@= skipped -0, +0 lines =@@
 // === findRenameLocations ===
 // === /canada.ts ===
 // export interface /*RENAME*/[|GingerRENAME|] {}
-
-// === /dry.ts ===
-// import { [|GingerRENAME|] as Ale } from './canada';