--- old.definition.baseline.jsonc
+++ new.definition.baseline.jsonc
@@= skipped -0, +0 lines =@@
 // === goToDefinition ===
 // === /a.ts ===
-// [||]export class Foo {}
+//  [|export class Foo {}|]

 // === /b.ts ===
 // import n = require([|'./a/*GOTO DEF*/'|]);