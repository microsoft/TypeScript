--- old.goToDefinitionJsxNotSet.baseline.jsonc
+++ new.goToDefinitionJsxNotSet.baseline.jsonc
@@= skipped -0, +0 lines =@@
 // === goToDefinition ===
 // === /foo.jsx ===
-// const [|Foo|] = <|() => (
+// <|const [|Foo|] = () => (
 //     <div>foo</div>
-// )|>;
+// );|>
 // export default Foo;

 // === /bar.jsx ===