// @strict: true

/**
 * text
 * @example Foo
 * ```
 * @Embed[asfasdfasf]
 * ```
 * becomes
 * ```html
 * <div></div>
 * ```
 */
const x = 1;

/**
 * Some text
 * ```
 * @tag inside code
 * ```
 * @param y - a number
 */
function foo(y: number) {}
